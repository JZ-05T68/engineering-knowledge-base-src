"""Agent Foundation package (v0.6.0).

Phase 1A contains only the vendor-neutral Tool Contract and Tool Registry.
No agent runtime, planner, executor, or retry loop exists in this package yet.

Independence contract:

- no ``streamlit`` / UI imports;
- no provider / vendor AI imports;
- no database connection imports;
- no schema v13 tables or migrations.
"""
