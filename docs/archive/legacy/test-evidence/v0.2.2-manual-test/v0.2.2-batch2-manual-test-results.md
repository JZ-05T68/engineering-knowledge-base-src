# v0.2.2 批次 2 隔离人工测试结果（8503）

- 日期：2026-07-26
- 审计范围：84364e8..1a78ff8（codex/v0.2.2）
- 隔离根目录：`D:\ekb-v0.2.2-manual-test`
- 数据库：`data\database\knowledge.db`（临时）
- 端口：127.0.0.1:8503（8502 被无关进程 PID 6700 占用，改用 8503；8501 全程未触碰）
- 测试 PDF：`ocr-manual-test.pdf`，SHA-256 `3790b1d437bf2329a4a4a7f947c39c9e456506766488be14a5e09d33fa36108d`
  - 第 1 页：纯图像英文数字 `STM32H750 ADC TEST 21MPa`（无文本层）
  - 第 2 页：纯图像中文 `本地OCR测试`（无文本层）
  - 第 3 页：真实文本层 62 字符
- 驱动方式：隔离 Streamlit 实例（HTTP 200）+ Streamlit AppTest 真实页面交互（真实 RapidOcrEngine、真实 SQLite）；正式 8501 未运行、未接触。

## 结果

| 项目 | 结果 | 证据 |
| --- | --- | --- |
| MT-01 启动与非 OCR 功能 | PASS | 主页/导入页 AppTest 无异常；启动后 rapidocr/onnxruntime 均未加载（sys.modules=False/False） |
| MT-02 导入临时 PDF | PASS | 3 页导入成功，均 pending；PNG 正常生成；原件未覆盖 |
| MT-03 英文单页 OCR | PASS | 首次 2.53s（含模型初始化）；成功反馈“本页 OCR 已完成。”；OCR 初稿=`STM32H750 ADC TEST 21MPa`（精确）；初稿只读、标注“未经人工核验”；按钮执行后消失；Markdown/review_status/PNG hash 不变 |
| MT-04 中文单页 OCR | PASS | 第 2 页识别=`本地OCR测试`（精确）；第 1 页结果不变；不串页；无路径显示 |
| MT-05 可靠文本层页 | PASS | 按钮存在但点击后 info“当前页面不符合 OCR 条件…”；真实引擎调用计数未增加；extracted_text 62 字符不变；无系统错误 |
| MT-06 OCR 搜索 | PASS | `STM32` 命中 page_id=1（match_fields=[ocr_text]）；`本地` 命中 page_id=2（ocr_text）；无误命中 |
| MT-07 OCR 证据警告 | PASS | 导出含 OCR 初稿警告（未经人工核验、以原始页面图像为准）；页码/文档引用正常。注：证据包按既有设计含“原始 PDF 绝对路径/页面图像绝对路径”两行（指向隔离目录），为证据包既有行为，非本批次引入 |
| MT-08 数据保护 | PASS | pages 表只读核验：p1(ext=0,ocr=24,md=0,status=ocr_completed,review=pending)、p2(ext=0,ocr=7,md=0,ocr_completed,pending)、p3(ext=62,ocr=0,text_extracted,pending)；markdown_path 不变；PDF hash 不变；PNG hash 不变 |
| MT-09 重复保护 | PASS | 已有 OCR 页面不显示执行按钮；引擎调用数 +0；OCR 文本不变 |
| MT-10 日志与网络 | PASS | 隔离日志无 http/download/api key；引擎初始化仅 1 次（首次点击时，非启动时）；日志中两条 Traceback 为审计脚本自身错误列名探测（`no such column: processing_status`），非应用异常；8501 无监听、未访问 |

## 清理

- 隔离 Streamlit 已停止；8503 无监听；无残留测试 python 进程。
- worktree `git status` 干净；仓库零写入。
- 测试现场（数据库、PDF、日志、本报告）保留于 `D:\ekb-v0.2.2-manual-test` 供发布收口复核。
