# Legacy archive

Historical / Archived assets recovered from `D:\EKB-Historical-Archive` during the
v0.5.3 final legacy-directory consolidation (2026-08-26).

- These are historical records only. They are NOT current specifications, test plans or runnable project scripts.
- Current specifications live in `docs/` and current scripts live in `scripts/`.
