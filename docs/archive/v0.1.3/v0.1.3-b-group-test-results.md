<!-- Historical / Archived: v0.1.3-era test documentation. Not a current test specification. Moved from D:\Projects\engineering-kb during v0.5.3 workspace governance. -->

# v0.1.3 阶段 3A · B 组隔离写测试实际执行记录（B-01～B-21）

- 执行依据：`docs/v0.1.3-b-group-test-plan.md`（B-01～B-21）
- 隔离根：`D:\ekb-isolated\v0.1.3-3a\`；实例端口 8502
- 记录原则：只记录事实；不改写测试计划；每项含执行前状态、实际操作、实际界面文案、DB/文件变化、PASS/FAIL/BLOCKED
- 执行人：用户（界面人工操作）+ Kimi（环境、只读核验与记录）

## 0. 开始前核验与环境准备（2026-07-22）

### 0.1 只读恢复核验

- Git：分支 `codex/v0.1.2-batch-ops`，HEAD `c10b61e4b0ef3b78321f693a04400d934682cd28`；工作区 `M AGENTS.md`（既有未提交修改，本轮全程不触碰）+ 未跟踪 `docs/v0.1.3-b-group-test-plan.md`、`docs/v0.1.3-baseline.md`。
- 进程/端口：8501～8512 全部无监听；系统无 python.exe / streamlit.exe 进程。
- 隔离现场：存在上次残留（data 228M，含 `data/markdown/1/page_0001.md` 笔记），不满足干净基线要求 → 按计划第 3 节重建。
- 正式环境基线（供零接触比对）：正式库 `data/database/knowledge.db` 196608 字节，mtime `2026-07-21 19:56:54 +0800`；`runtime/engineering-kb.pid.json` 为陈旧文件（pid 27428 不存在）；正式日志最近改动 2026-07-22 16:41（3A 备份创建时刻）。
- 源备份：`backups/pre-expansion-v0.1.2-20260722-164155` 存在；manifest 记录 `data/database/knowledge.db` sha256=`df3bfd91c8fe1e9721df8abff581c13d711ebace5b1f0252c373a3d32cbfe8de`；对备份内 db 实测 SHA-256 与 manifest 一致。

### 0.2 重建与恢复

- 操作：确认 8501～8512 无监听、无 Python/Streamlit 进程后，删除并重建 `D:\ekb-isolated\v0.1.3-3a\{data,backups,logs,runtime}`（删除前已二次校验目标路径精确等于隔离根）。
- 恢复命令（与计划第 3 节一致）：
  ```
  .venv/Scripts/python.exe scripts/restore_backup.py \
    --backup backups/pre-expansion-v0.1.2-20260722-164155 \
    --target-data-dir "D:/ekb-isolated/v0.1.3-3a/data" \
    --target-backups-dir "D:/ekb-isolated/v0.1.3-3a/backups" --confirm RESTORE
  ```
- 恢复输出：预检查 PASS（文档 3、页面 128、FTS 128、schema v4）；恢复及恢复后验证 PASS；目标原先不存在无需恢复前备份。

### 0.3 首次哈希核验：计划判定与实际代码不一致（停止项，待用户裁决）

- 实测：恢复后、启动任何应用连接之前，`D:\ekb-isolated\v0.1.3-3a\data\database\knowledge.db` SHA-256 = `6620d439adb8ef1790d1ccd20094b06f0de1713bd7d7dedcf05cb80eeab30dce`，与 manifest 记录 `df3bfd91…fe8de` **不一致**（文件大小一致，均为 196608 字节）。
- 代码事实（只读核查，未做任何修复）：
  - 隔离恢复不是逐字节拷贝：`src/backup_service.py:750-766`（`_sqlite_snapshot`）使用 SQLite backup API 重建数据库并 `PRAGMA journal_mode = DELETE`；
  - `restore_backup` 在切换目标目录**之前**调用 `_rebase_database_paths`（`src/backup_service.py:997-1041`），以 `BEGIN IMMEDIATE` 事务将 `documents.source_path`（3 行）、`pages.image_path`（128 行）、`pages.markdown_path`（非空行）从备份源路径改写为恢复目标路径——这是隔离恢复的**既定设计行为**；
  - 因此对"恢复到不同路径"的隔离恢复，恢复后 db 文件哈希**必然**不等于 manifest 哈希。计划第 3 节第 4 步的"一致→继续"判定与当前代码实际行为不一致。
- 等价性证据（只读逻辑比对，脚本留存于 `D:\ekb-isolated\v0.1.3-3a\work\compare_restore.py`，连接方式 `mode=ro&immutable=1` + `PRAGMA query_only=ON`）：
  - 17 张表逐行比对，仅将路径列按根目录归一化后：**0 处真实差异**；
  - `sqlite_master` 完全一致；`schema_migrations` = (1,2,3,4)；
  - `PRAGMA integrity_check` = ok；`PRAGMA foreign_key_check` = 0 违规；
  - 基线统计：3 文档 / 128 页 / 128 FTS / 128 页全部 `pending`；标签 0 / 项目 0 / 证据 0。
- 字节级比对旁证：备份 db 与恢复 db 共 15686 字节不同，差异区域与路径字符串改写（路径长度不同导致页内单元重排）及 backup API 重建一致。
- 过程备注：前两次临时比对（heredoc 内联脚本）曾报告"真实差异"，原因为 JSON→bash→heredoc 三层转义使 `\v` 被解析为垂直制表符、归一化未生效的假报警；改用文件脚本后结论如上，已复核无误。
- 处置：按计划第 3 节第 4 步"不一致→立即停止上报"及执行要求第 9 条，已停止（未启动实例、未做任何写操作）并上报用户。
- **用户裁决（2026-07-22）：选项 A**——接受"逻辑级全量比对一致"作为首次核验的等价证据，本偏差留档；继续第 5 步启动 8502 实例并进入 B-01。测试计划第 3 节第 4 步的哈希相等判定建议后续修订为"隔离恢复改做逻辑级比对"（本次不改写计划文件）。

## 1. 逐场景记录

### 环境启动（计划第 3 节第 5～6 步，2026-07-22）

- 启动命令：11 个 `EKB_*` 变量全部指向隔离根（与计划第 3 节一致），`.venv/Scripts/python.exe -m streamlit run app.py --server.port 8502 --server.address 127.0.0.1 --server.headless true`，后台运行。
- 监听核验：`netstat` 仅 `127.0.0.1:8502 LISTENING`；8501 无监听；`curl http://127.0.0.1:8502/` → HTTP 200；`curl http://127.0.0.1:8501/` → 无响应（符合预期）。
- 正式环境抽查：正式日志 `logs/engineering-kb.log` mtime 仍为 `2026-07-22 16:41:56`、15133 字节，未变。
- 干净基线核验（隔离库只读）：documents=3、pages=128、page_search=128、tags=0、page_tags=0、document_tags=0、projects=0、project_documents=0、project_pages=0、evidence_baskets=1、evidence_items=0、import_records=2；128 页全部 `pending`；含笔记页 0；数据文件 133 个（db 1 + md 1 + PNG 128 + PDF 3，其中 `markdown/1/page_0001.md` 为基线自带的 0 字节文件）。
- 复核队列队首：doc 1《STM32入门(标准库)(新78页版)》page 1～8（page_id 1～8，pending）。

### 2026-07-23 环境重建与代码更新（证据篮补丁合入后）

- Git：夜间补丁经 `git merge --ff-only kimi/v0.1.3-overnight-code` 纯快进合入，主工作区 HEAD 由 `c10b61e` 推进至 `042fa94`（= c10b61e + `042fa943` 证据篮并发修复）；无 merge commit；`AGENTS.md` 未提交修改与 3 个未跟踪 v0.1.3 文档原样保留。合入后 Ruff 通过、证据篮针对性 22 passed、完整 Pytest 288 passed。
- 测试计划机械校正（已记录在计划第 1 节）：适用版本 HEAD 更正为 `042fa94`；B-17 `:533`→`:537`、`:473-484`→`:477-488`，B-18 `clear_items :491`→`:495`。仅行号与版本依据，场景语义与 PASS/FAIL 标准未变。
- 隔离环境按计划第 3 节重建：`D:\ekb-isolated\v0.1.3-3a\{data,backups,logs,runtime}` 删除重建（路径二次校验后执行），自 `backups/pre-expansion-v0.1.2-20260722-164155` 恢复，预检查/恢复/恢复后验证全部 PASS。
- 首次哈希核验：恢复库 SHA-256=`6620d439adb8ef1790d1ccd20094b06f0de1713bd7d7dedcf05cb80eeab30dce`，与 manifest `df3bfd91…fe8de` 不一致——与 2026-07-22 完全相同的可复现设计性偏差（路径重定位），按 0.3 节用户"选项 A"裁决执行逻辑级全量比对：17 表 0 处真实差异、schema 完全一致、schema_migrations=(1,2,3,4)、integrity ok、外键 0、3 文档/128 页/128 FTS/128 页全 pending。
- 运行代码核验：`git diff 042fa94 -- app.py src pages scripts pyproject.toml requirements.txt` 为空，隔离实例运行代码与 HEAD 树级一致。
- 实例启动：11 个 `EKB_*` 变量指向隔离根，端口 8502；`netstat` 仅 8502 LISTENING，8501 无监听；健康检查 HTTP 200；正式日志与正式库 mtime 未变。
- 执行前全量快照：`D:\ekb-isolated\v0.1.3-3a\work\b01-pre-20260723.txt`（基线同上：133 个内容文件，含 0 字节 `markdown/1/page_0001.md`）。

### B-01 保存草稿与幂等重复保存

- 执行前状态：如上 2026-07-23 干净基线（3 文档/128 页/全 pending/0 标签/0 项目/0 证据）。
- 选定 P1 = page_id 1（doc 1《STM32入门(标准库)(新78页版)》第 1 页）。
- 实际操作（用户，2026-07-23 07:21 +08:00）：①打开 P1，编辑框输入 `B-01 draft note`；②点「保存草稿」；③不修改内容，再次点「保存草稿」。
- 实际界面文案（用户报告）：两次均显示「Markdown 草稿已保存。」——与计划预期（`pages/4_待整理页面.py:442`）一致。
- Kimi 自动核验（2026-07-23 07:26 起，只读 `mode=ro` + `PRAGMA query_only=ON`）：
  - 首次写入完整：`markdown_content='B-01 draft note'`；`markdown_path=…\data\markdown\1\page_0001.md`（文件存在，15 字节，由基线 0 字节文件就地更新，mtime 07:21:34.98）；`review_status='draft'`；`note_updated_at=2026-07-22T23:21:36.027541+00:00`；`reviewed_at=None`；检索字段 `search_markdown_content='b 01 draft note'`；FTS 核验 `page_search MATCH 'b 01 draft note'` 命中 rowid=1。
  - 第二次幂等（决定性证据）：隔离日志中「页面 Markdown 已保存：document_id=1 page_number=1」**仅 1 行**（07:21:36.044）。真实写入路径（`src/document_service.py:335-339`）每次实写必记一行；幂等短路（:310-317）在四条件（内容、路径、目标状态、磁盘文件内容）全等时直接返回，不产生任何 DB 写、文件写与日志。用户点击两次而日志仅一行 ⇒ 第二次为零写入。`note_updated_at`、`updated_at`、`.md` 文件 mtime、数据库文件 mtime 均停在首次写入时刻。
  - 不变量：其余 127 页全部 `pending`；documents=3/pages=128/page_search=128；tags/projects/evidence_items=0；含笔记页=1（仅 P1）；内容文件 133 个不变（`page_0001.md` 就地更新）；原 PDF/PNG 未动。
  - 预期内现象：核验期间隔离库旁出现 `knowledge.db-wal`（0 字节）与 `knowledge.db-shm`（32 768 字节）——只读连接对 WAL 模式库构建索引的运行时侧文件（`v0.1.3-baseline.md` §9.1 同类行为），wal 为 0 字节、全部已提交数据在主库文件内；按计划第 6 节如实记录，不主动删除。
- **判定：PASS**（首次写入完整；第二次零变化经日志行数决定性证实；无缺陷）。
- 衔接：P1 当前为 draft，留作 B-03 清空对象。

### B-02 保存并标记已复核 / 保存、复核并进入下一页

- 执行前状态：`work/b02-pre.txt`；3 文档 / 128 页 / 128 FTS，`127 pending + 1 draft`；page_id 1 为 B-01 草稿，page_id 2、3 为 pending。
- 实际操作（Codex，2026-07-23 09:10～09:11 +08:00，仅隔离实例 8502）：
  1. 打开 page_id 2，输入 `B-02 reviewed note`，点击「保存并标记已复核」；
  2. 打开 page_id 3，输入 `B-02 next-page note`，点击「保存、复核并进入下一页」。
- 实际界面反馈：
  - page_id 2 顶部提示「页面已保存并标记为人工复核完成。」；当前状态变为「人工复核完成」。
  - page_id 3 顶部提示「本页已保存并完成人工复核，已进入下一待处理页。」；自动跳转到 page_id 4，即 doc 1《STM32入门(标准库)(新78页版)》第 4 页。
- 只读核验（`mode=ro` + `PRAGMA query_only=ON`）：page_id 2、3 均为 `reviewed`，`reviewed_at`/`note_updated_at` 非空且相等；Markdown 内容与路径准确；FTS 分别命中 rowid 2、3；对应 18 字节/19 字节 `.md` 文件落盘。
- 不变量：page_id 1 仍为 `draft` 且内容保持 `B-01 draft note`；documents=3、pages=128、page_search=128、tags=0、projects=0、evidence_items=0；其余状态为 125 pending；integrity `ok`、外键违规 0。
- 日志证据：隔离日志新增且仅新增 page 2、3 各 1 条「页面 Markdown 已保存」记录（09:10:33、09:11:21）。
- 证据：`D:\ekb-isolated\v0.1.3-3a\work\b02-pre.txt`、`b02-after.txt`、`b02-verify.txt`。
- **判定：PASS**（状态、时间戳、落盘、FTS、提示文字与跳转全部符合）。
- 衔接：page_id 1 保留给 B-03；当前界面停在 page_id 4。

### B-03 清空笔记（撤销/移除 + 确认守卫）

- 执行前状态：`work/b03-pre.txt`；page_id 1 为 `draft`，内容 `B-01 draft note`，15 字节 Markdown 文件存在。
- 实际操作（Codex，2026-07-23 09:15 +08:00，仅隔离实例 8502）：进入《STM32入门(标准库)(新78页版)》第 1 页；确认未勾选「确认清空」时「清空笔记」按钮为 disabled；勾选确认后按钮启用并点击。
- 实际界面反馈：清空后页面状态显示「待处理」、笔记显示「无」。计划/代码中的成功文案「本页笔记已清空。」未能在重跑后的稳定 DOM 中观察到；同一稳定 DOM 的编辑框仍暂存旧文本 `B-01 draft note` 并显示「● 未保存」，属于页面 widget 状态未随 `st.rerun()` 清空的观察项，未再次保存该内容。
- 只读核验（`mode=ro` + `PRAGMA query_only=ON`）：page_id 1 的 `markdown_content=''`、`markdown_path=NULL`、`review_status='pending'`、`reviewed_at=NULL`；FTS 不再命中 `b 01 draft note`；`data/markdown/1/page_0001.md` 已删除。
- 不变量：page_id 2、3 仍为 reviewed；总体状态 126 pending + 2 reviewed；integrity `ok`、外键违规 0；页面记录、原 PDF/PNG 与其他笔记未变。
- 日志证据：`2026-07-23 09:15:03,008 INFO ... 页面 Markdown 已清空：document_id=1 page_number=1`。
- 证据：`work/b03-pre.txt`、`b03-after.txt`、`b03-verify.txt`。
- **判定：PASS（含 UI OBSERVATION）**：确认守卫与 DB/文件/FTS 同步清除全部符合 PASS 标准；稳定界面未保留成功提示且编辑控件显示旧内容为观察项，不影响本场景持久化结果。
- 衔接：直接离开浏览资料页，不保存残留 widget 文本；B-04 在待整理页另选 page_id 4、5。

### B-04 暂时跳过与未保存修改守卫

- 执行前状态：`work/b04-pre.txt`；126 pending + 2 reviewed；page_id 4、5 均为 pending、无笔记。
- 实际操作（Codex，2026-07-23 09:17 +08:00，仅隔离实例 8502）：
  1. 在干净的 page_id 4 点击「暂时跳过」；
  2. 自动进入 page_id 5 后输入 `B-04 unsaved note`，点击「下一待处理页」；
  3. 守卫出现后点击「留在当前页」。
- 实际界面反馈：
  - page_id 4 提示「本页已设为暂不整理，已进入下一待处理页。」并跳到 page_id 5；
  - page_id 5 出现「当前页有未保存修改。是否放弃修改并切换到：STM32入门(标准库)(新78页版) · 第 6 页 · 待处理？」以及「保存草稿并留在当前页」「留在当前页」「放弃未保存修改并切换」三按钮；「暂时跳过」同时禁用；
  - 点击「留在当前页」后页面仍为 page_id 5，但编辑框中的 `B-04 unsaved note` 被静默清空，状态恢复为「● 内容已保存」，没有提示内容将被丢弃。
- 只读核验（停止现场）：page_id 4=`skipped`；page_id 5 仍为 pending、`markdown_content=''`、`markdown_path=NULL`，无 `.md` 文件，FTS 不命中 `b 04 unsaved note`；总体 125 pending + 2 reviewed + 1 skipped；integrity `ok`、外键违规 0。
- 未执行：因已触发本场景 HIGH 停止条件，未重新输入内容，未测试「放弃未保存修改并切换」，未开始 B-05。
- 代码定位：`pages/4_待整理页面.py:207-209` 的「留在当前页」路径仅清除 pending target 并 `st.rerun()`；实际运行时未保留编辑控件中的 dirty 内容。
- 证据：`work/b04-pre.txt`、`b04-after.txt`、`b04-verify.txt`；浏览器稳定 DOM 已记录上述警告、三按钮以及点击后的空编辑框状态。
- **判定：FAIL（HIGH，停止）**：守卫虽然出现，但「留在当前页」路径仍导致未保存内容无提示丢失，命中计划第 5/7 节明确停止条件。
- 现场处置：只读取证并记录；未修复、未重置、未继续后续场景。

## 2. 汇总表

| 编号 | 结果 | 缺陷严重度 |
|---|---|---|
| B-01 保存草稿与幂等重复保存 | **PASS**（2026-07-23，HEAD `042fa94`） | 无 |
| B-02 保存并标记已复核 / 保存复核并进入下一页 | **PASS**（2026-07-23，HEAD `042fa94`） | 无 |
| B-03 清空笔记（撤销/移除 + 确认守卫） | **PASS**（含 UI OBSERVATION） | OBSERVATION |
| B-04 暂时跳过与未保存修改守卫 | **FAIL（停止）** | HIGH |
| B-05～B-21 | 未执行（B-04 HIGH 停止条件） | — |

## 3. 缺陷清单

截至 B-04：

- **HIGH — B-04「留在当前页」静默丢失未保存内容**：守卫出现后选择「留在当前页」，页面未切换但编辑框被清空且界面恢复「内容已保存」，无丢弃提示。证据与代码定位见 B-04；按计划立即停止。
- **OBSERVATION — B-03 清空后的 UI 状态不同步**：清空持久化成功，但稳定 DOM 未保留成功提示，编辑控件暂存旧文本且显示未保存；未写回数据库。

## 4. 环境终态

（待执行）

## 5. 正式环境未变证据

（待执行）

## 6. 证据保留声明

（待执行）

## 7. 移交说明（2026-07-23，B-01 完成后）

- 用户决定将 B-02～B-21 移交 Codex 执行。本记录文件随交接保持现状；后续场景记录由 Codex 续写。
- 交接时环境保持：隔离实例 `127.0.0.1:8502` 运行中；隔离现场 `D:\ekb-isolated\v0.1.3-3a` 完整保留；主工作区 HEAD `042fa94`；正式环境（8501/正式库/日志/备份/资料）零接触。
- 交接报告全文：`D:\ekb-isolated\v0.1.3-3a\work\handover-b01-to-codex.md`（自包含，含 B-02 起点）。
