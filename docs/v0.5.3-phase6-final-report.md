# EKB v0.5.3 Phase 6 Final Report — Export Closure + Schema-v8 Backup Upgrade

- 文档性质：Phase 6 阶段报告（本阶段允许新增的文档）
- 生成日期：2026-08-25
- 前置：Phase 1–5 全部 PASS；三项能力（6A/6B/6C）全部完成
- 阶段门禁：本阶段已停止；请求审核；未 commit、未 push
- 状态：**请求审核（PASS / CONDITIONAL PASS / REWORK / FAIL）**

---

## 1. 开始时 HEAD、分支、git status

| 项 | 值 |
|---|---|
| `git rev-parse HEAD` | `57cecef1379b04c8cd5c31693873678119e4911c` |
| `git branch --show-current` | `main` |
| `git status --short` | 54 行（Phase 2-A ~ Phase 5 工作树修改与新增文件） |
| `git diff --check` | clean |

未执行 git reset / checkout 丢弃 / clean / stash / commit / push / merge / tag / Release。

## 2. 四类版本维度及最终值

| 维度 | 值 |
|---|---|
| application_version | `0.5.2`（本阶段未改） |
| schema_version | `12`（未新增 v13） |
| backup_format_version | `1`（未改） |
| config_format_version | `1`（未改） |
| knowledge_export_format_version | `1`（新增） |
| ai_ledger_export_format_version | `1`（新增） |

冻结契约见 `docs/v0.5.3-phase6-export-backup-contract.md`：四个版本不可互相替代；
schema v8 升级 ≠ 修改 backup_format_version；正式 restore 保持 schema 精确匹配。

## 3. Knowledge Export 文件结构、格式版本、字段和哈希契约

`src/knowledge_export_service.py`，格式版本 1：

- 文件：`manifest.json`、`knowledge_objects.json`、`knowledge_object_sources.json`、
  `knowledge_relations.json`、`knowledge_memory_entries.json`、
  `knowledge_object_revisions.json`、`markdown/<stable_id 安全化>.md`（每对象/记忆独立）、
  `files.json`（每文件 SHA-256 清单）。
- 字段：业务值使用稳定英文 enum；stable_id 复用 `build_stable_id`；relation 保留
  source/target stable_id；superseded_by 保留目标 stable_id；source 保留 type/id/note/
  fingerprint/captured_at/state_at_export(available/missing/unknown)；revision 保留
  object_stable_id_snapshot 等快照字段（已删除对象标记 object_deleted=true）。
- 排序确定（stable_id/id）、UTF-8、`sort_keys=True`；staging→validate→atomic publish；
  输出冲突拒绝；不含 AI 台账/embedding/密钥/提示词/输出正文（测试固化）。

## 4. AI Ledger Export 文件结构、格式版本、隐私边界和缺失字段声明

`src/ai_ledger_export_service.py`，格式版本 1：

- 文件：`manifest.json`、`ai_calls.json`（权威）、`ai_calls.jsonl`（权威无损）、
  `files.json`。不实现 CSV（可选能力，本阶段不实现）。
- 只导出 v12 真实存在的审计字段；target_refs 只导出 stable_id 字符串并附
  `target_refs_state_at_export`（available/missing），不读取目标正文。
- 隐私声明：不含 API Key、认证头、模型提示词正文、知识/页面正文、模型输出正文、
  本地敏感路径或环境变量。
- omissions 声明：provider 未持久化，逐条记录无 provider 字段；费用未持久化，
  不导出 guessed_cost/estimated_cost。
- 错误摘要脱敏截断（`transport [REDACTED]` 实证）。

## 5. schema v8 隔离升级八步状态机逐步证据

`src/legacy_backup_upgrade_service.py` + `scripts/upgrade_backup.py`。
对真实旧备份 `data/database/backups/knowledge.v8.20260823-091055-193733.db` 执行结果：

| Step | 证据 |
|---|---|
| 1 验证原始旧备份 | SHA-256 `30500015…`；schema v8；integrity ok；fk 0 |
| 2 唯一 staging | 系统临时目录 `ekb-legacy-upgrade-*`；复制前后字节哈希一致（本备份无 WAL 差异） |
| 3 验证 staging v8 | 表计数基线：documents/pages/notes/evidence_items/page_embeddings=0 |
| 4 既有迁移链 | `migrations.migrate_database` v8→12；迁移自动备份仅在 staging（`knowledge.v8.20260825-210858-914566.db`） |
| 5 验证迁移后 | schema=12；integrity ok；fk 0；基线计数不变；`ai_calls`=0（不伪造历史 AI 调用） |
| 6 生成新备份 | 无资产源 → 当前 schema 数据库快照 + `upgrade_report.json` |
| 7 严格验证 | 当前健康检查路径通过（schema 12 / integrity ok / fk 0） |
| 8 隔离恢复演练 | 第二隔离目录打开验证：schema 12 / integrity ok / fk 0 |

- 原始旧备份（.db/.db-shm/.db-wal）前后 SHA-256 完全一致。
- 正常 restore 仍拒绝 schema v8（validate_backup 对旧快照返回"备份目录不存在"）。
- 失败注入（`v12_ai_calls`）→ 原始备份不变、无有效输出残留。

## 6. 兼容矩阵

| 输入 | 行为 |
|---|---|
| 合法 schema v8 SQLite 快照 | 升级（唯一支持） |
| schema v12 / 非 v8 | 拒绝："不是 v8" |
| 非 SQLite / 损坏 | 拒绝："不是可读取的 SQLite" |
| 文件缺失 | 拒绝 |
| 输出位于正式 data 目录 | 拒绝 |
| 生产数据库作为输入 | 拒绝 |
| manifest 缺失/目录备份 v8 | 不由本入口处理；正常 restore 按 schema 精确匹配拒绝 |

## 7. 正常 restore 保持精确 schema 匹配的证据

- `BackupService.restore_backup` / `validate_backup` 未改；`_require_healthy_database`
  仍要求 `summary.schema_version == SCHEMA_VERSION`（v12）。
- 测试 `test_normal_restore_validation_still_rejects_raw_v8`：对 schema v8 旧快照，
  `validate_backup` 返回 invalid（错误含"备份目录不存在"）。

## 8. Current schema v12 backup/restore 新数据逐字段 roundtrip 证据

`tests/test_v12_backup_roundtrip.py`（隔离临时数据目录）：

- 构造：2 Knowledge Object、1 Source、1 Relation、1 Memory、1 Revision（服务创建
  自动产生）、1 成功 AI 调用（rag_answer target_refs）、1 失败 AI 调用
  （experience_model target_refs）。
- `BackupService.create_backup` → `validate_backup` valid（app 0.5.2 / schema 12）。
- 第二隔离目录 `restore_backup` → 逐项核对：KO=2、Memory=1、ai_calls=2、
  source_feature={rag_answer, experience_model}、status={success,error}、
  integrity ok、fk 0、schema 12。

## 9. 三项真人验收结果

### H6-1（真实 schema v8 旧备份）
- 输入：`data/database/backups/knowledge.v8.20260823-091055-193733.db`
- 命令：`python scripts/upgrade_backup.py "<输入>" /tmp/ekb-h61-output`
- 结果：退出码 0；升级到 schema 12；integrity ok；fk 0；ai_calls=0；
  新快照路径 `/tmp/ekb-h61-output/upgraded-v8-snapshot-*/database/knowledge.db`。
- 原旧备份三文件 SHA-256 前后一致（db `30500015…`、shm `fd4c9fda…`、wal `e3b0c442…`）。

### H6-2（Knowledge Export）
- 临时知识库：2 KO、1 Source、1 Relation、1 Memory、3 Revisions。
- 导出包 9 个文件；manifest counts={knowledge_objects:2, sources:1, relations:1,
  memory_entries:1, revisions:3}；markdown 3 个（每对象/记忆独立）；JSON 与引用关系
  人工核对可读、一致。

### H6-3（AI Ledger Export）
- 临时台账：成功 rag_answer + 失败 experience_model（含敏感 token 错误）。
- 导出 2 条记录；JSON/JSONL 一致；不含 prompt_sha256/API Key/认证头/回答；
  逐条无 provider 字段；错误摘要为 `transport [REDACTED]`。

## 10. 聚焦测试和全量测试命令、收集数、exit code

| 命令 | 结果 |
|---|---|
| `python -m ruff check .` | All checks passed |
| `git diff --check` | clean |
| 6A/6B/6C/roundtrip 聚焦 | 17 passed |
| Phase 3/4/5 回归、migration/backup/restore/release_check | 全部通过 |
| 全量 `python -m pytest -q` | **PYTEST_EXIT=0** |
| 收集数 | **1762 tests collected（≥1745）** |

## 11. 生产数据库开始/结束 size、mtime、SHA-256

| 时点 | size | mtime | SHA-256 |
|---|---|---|---|
| 开始 | 364544 | 1787630588 | `2835413883e7869dc10e3bfbbcfa6320129f847fa4f093ebbd617a56edc629ba` |
| 结束 | 364544 | 1787630588 | `2835413883e7869dc10e3bfbbcfa6320129f847fa4f093ebbd617a56edc629ba` |

## 12. 原始 schema v8 备份开始/结束目录哈希

| 文件 | SHA-256（前后一致） |
|---|---|
| knowledge.v8.20260823-091055-193733.db | `305000158792ca663d937ef7fa5e56c9127872db0f9e4ae46ecd749b90854856` |
| .db-shm | `fd4c9fda9cd3f9ae7c962b0ddf37232294d55580e1aa165aa06129b8549389eb` |
| .db-wal | `e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855` |

## 13. production 8501 untouched 证据

本阶段未运行/重启/占用 Streamlit；端口检查无 8501 监听；生产库文件零变化；
所有 UI 测试为 AppTest 内存执行。

## 14. 新增和修改文件清单

新增：`docs/v0.5.3-phase6-export-backup-contract.md`、
`src/knowledge_export_service.py`、`src/ai_ledger_export_service.py`、
`src/legacy_backup_upgrade_service.py`、`scripts/upgrade_backup.py`、
`tests/test_knowledge_export_service.py`、`tests/test_ai_ledger_export_service.py`、
`tests/test_legacy_backup_upgrade.py`、`tests/test_v12_backup_roundtrip.py`、
`docs/v0.5.3-phase6-final-report.md`。
修改：无既有文件改动（本阶段完全新增）。

## 15. 已知限制

- AI Ledger Export 未实现 CSV（契约声明为可选，JSON/JSONL 为权威）。
- 现实中的 schema v8 旧备份是数据库快照（不含 raw/pages/markdown），因此无资产源的
  升级产出为"当前 schema 数据库快照 + upgrade_report.json"；完整目录备份路径已由
  v12 roundtrip 测试与 `--assets-data-dir` 参数覆盖。
- Knowledge/AI Ledger 导出尚未接 UI 入口（本阶段按契约只交付服务与 CLI；UI 属后续
  收口阶段可选项，未在禁止项之外扩展）。

## 16. 明确未执行

- 版本一致性收口；
- production 8501 更新；
- README.md / README_EN.md 同步；
- commit / push / tag；
- GitHub Release。

## 17. 边界声明

本阶段没有：自动写知识、修改用户知识、自动运行导出、自动升级备份、直接恢复到
production、删除或覆盖旧备份、放宽正常 restore schema 校验、混淆 schema v8 与
backup format version、新增 v13、保存或导出完整 prompt/上下文/回答、导出 API Key、
猜测 provider/token/费用、Agent、工具调用、会话记忆、修改 production 8501、
修改版本号或 README。

---

**阶段门禁**：Phase 6 三项能力全部完成，已停止，等待外部审核
（PASS / CONDITIONAL PASS / REWORK / FAIL）。未进入版本一致性收口、production 8501
更新、README 同步、commit/push 或 GitHub Release。
