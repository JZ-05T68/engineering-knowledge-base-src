# EKB v0.5.1 Phase 2 — Handoff Closure（人工审核后）

- 日期：2026-08-17
- 分支：`kimi/v0.5.1-retrieval-stabilization`
- 基线 commit：`610ffa5`
- 审核结论：**PASS WITH CORRECTIONS**（本文件反映修正后的 defect disposition）

## 一、恢复与实现确认

- 生产 `src/ai/` 检索核心（`retrieval.py` / `hybrid_search.py` / `vector_recall.py`）
  相对 baseline **零改动**。
- D-01 / D-03 仅以 `scripts/retrieval_ablation.py` 的离线变体矩阵完成机制实验，
  **未落地生产**。
- D-05 已落地生产（`src/search_mode.py` + `pages/4_检索资料.py`），并带回归测试。

## 二、Defect Disposition（修正口径）

### D-01 — HIGH

- **Production status: OPEN**
- **Experimental disposition: PARTIALLY MITIGATED BY D2 CANDIDATE**
  — D2 显著缓解系统性 partial-coverage regression（7 regressions → 2 residual：
  J2 improved-not-fixed、C3 unchanged），保留 C2/H2/D3 收益，但**未消除全部 regression**。
  C3 指向 embedding coverage structural issue，非融合算法可修。

### D-02 — HIGH

- **Production status: OPEN**
  — 现行生产代码不存在 weak/no-answer product boundary。
- **Experimental disposition: PARTIALLY MITIGATED BY D2 CANDIDATE**
  — F2/F3 改善发生在尚未落地的 fixture ablation 中，不是 production 行为。

### D-03 — MEDIUM

- **Production status: OPEN**
- **Experimental disposition: vector-side expansion mitigated; final-result bounding unproven**
  — 结果数 21/25/34 → 3/9/29 仅证明 vector-side candidate explosion 显著缓解；
  **不能**证明 final hybrid result set 已稳定 bounded。

### D-05 — MEDIUM

- **Production status: FIXED**（实际执行模式 == UI presentation，回归测试通过）

### D-06 / D-10

- defer（无新证据升级 severity）。

## 三、验证

- `git diff --check`：仅 CRLF 换行警告，无 whitespace 错误。
- `python -m ruff check .`：All checks passed。
- `python -m pytest -q`：1459 tests，exit 0。
- 冻结 `benchmarks/*.json` sha256 与 baseline 一致（未篡改）。

## 四、下一步（Phase 3）

进入「Real Embedding Calibration & Production Strategy Freeze」，
见 `docs/v0.5.2-phase3-paid-call-proposal.md`。D2 不提前落地生产。
