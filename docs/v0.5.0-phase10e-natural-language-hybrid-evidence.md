# v0.5.0 Phase 10E 自然语言 Hybrid 真实验证证据归档

- 日期：2026-08-16
- 分支：`kimi/v0.5.0-controlled-real-index`
- 基线 HEAD：`5713d9c`（`5713d9c`，Phase 10E-C 验收 checkpoint）
- 模型：`qwen3.7-text-embedding`，dimensions = 1024，config_version = 1
- 环境：隔离 staging 实例（`staging-data/`，端口 8502，gitignored）
- exact query：`定时器预分频器和自动重装载寄存器的作用`（原始自然语言，未手工 normalization）

本文档只归档实验事实与必要指纹。staging DB、embedding BLOB、API 原始响应、
API Key、`.env` 与 baseline backup 均不入 Git。

## 一、Implementation

- `EMBEDDING_DIMENSIONS: Final[int] = 1024` —— canonical internal single source
  of truth，位于 `src/ai/page_indexer.py`，紧邻 `EMBEDDING_CONFIG_VERSION`。
- `application_hybrid_search_service()`（`src/runtime.py`，`@lru_cache`）——
  canonical runtime Hybrid assembly：
  - lexical = `SearchService(database)`（自然语言 normalization）
  - hydration = `database`
  - vector = `PersistentVectorRecallSource(...)`（维度来自 `EMBEDDING_DIMENSIONS`，
    config version 来自 `EMBEDDING_CONFIG_VERSION`）
  - AI disabled / manual / no-key 时 `vector=None`，纯 lexical degradation。
- 不新增 Settings 字段，不修改 `embedding_store.py` / `config.py`，不新建 contract module。
- Phase 10C/10D 历史 probe 常量未改写。

## 二、Offline regression

- Phase 10E 新增 tests：
  - `tests/test_ai_runtime_hybrid.py`（15 项：dimensions contract、manual 装配、
    自然语言矩阵、fake-provider Hybrid regression）
  - `tests/test_ai_real_natural_language_probe.py`（9 项：冻结 contract、
    paid-call guard、staging flag、RRF 数学）
- targeted tests：212 passed（AI 相关子集）
- full pytest：1359 passed
- ruff clean

## 三、Real smoke（Phase 10E-C）

exact query：`定时器预分频器和自动重装载寄存器的作用`

- fresh process：`EKB_STAGING_INSTANCE=1` 在 `import src.runtime` 之前建立
- staging identity：resolved DB = `staging-data/data/database/knowledge.db`
  （非 production `data/...`）
- canonical factory used：`application_hybrid_search_service()` = YES
- lexical results：`18, 17, 19, 40, 5`（非空）
- vector results：5 个 fresh 候选全部召回（vector_rank 1→5 = `18, 19, 17, 40, 5`）
- final Hybrid results：5/5 均为 **both** candidate（lexical_rank 与 vector_rank 均非 None）
- RRF verification（k=60）：5/5 数学一致
- DB integrity：staging 5→5、production 0→0，双库 sha256 前后不变
- HTTP accounting：embedding HTTP = 1，LLM = 0，rerank = 0，retry = 0

## 四、Final conclusion

`natural-language normalization → lexical + vector → RRF → hydration`

已经通过 canonical runtime assembly 真实验证。自然语言 lexical 恒空的 Phase 10D
design gap 已关闭。

## 五、Retry policy observation（Non-blocking follow-up）

- `application_ai_provider()` 当前 production default：`max_extra_attempts = 2`
- Phase 10E-C smoke：仅在探针中临时覆写 `_max_extra_attempts = 0`
- 因此：smoke retry = 0，production runtime default 仍 = 2
- 本 Phase 不修改正式 retry policy。记录为 follow-up / policy decision，不作为 Phase 10E failure。
