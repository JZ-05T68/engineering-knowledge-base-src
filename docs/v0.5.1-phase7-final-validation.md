# EKB v0.5.1 — Phase 7 Final Validation & Acceptance

- 日期：2026-08-17
- 分支：`kimi/v0.5.1-retrieval-stabilization`
- 性质：**validation / closure only**，未启动新 remediation。

## 1. Phase 6 push

- commit：`cf9ac8bd64f5931726c0e4ed70134bce07951793` `feat(retrieval): expose embedding coverage and weak-evidence honesty`
- push：成功，remote HEAD = `cf9ac8b`（`4b4c965..cf9ac8b`），未 merge / tag / 无关 refs。

## 2. 7A — Automated regression

| 命令 | 结果 |
|---|---|
| `git diff --check` | clean |
| `python -m ruff check .` | All checks passed |
| 聚焦检索/coverage/搜索 UI 测试 | 全通过 |
| 全量 `python -m pytest` | **1475 passed**（0 failed / 0 error，17m42s） |

新增测试 16 个：`test_coverage_service.py`（9）+ `test_search_mode.py::TestWeakEvidenceNote`（7）。
1459（Phase 5 时）→ 1475，与新增精确吻合。

## 3. 7B — Coverage acceptance（staging 实库只读验证）

对 `staging-data/data/database/knowledge.db`（STM32 staging 语料）运行只读 `coverage_summary()`：

| 项 | 值 |
|---|---|
| indexable | 5 |
| indexed（fresh） | 5 |
| missing | 0 |
| stale | 0 |
| skipped_empty | **73** |
| coverage_ratio | **1.0（100%）** |

- 这正是审查要求的 denominator regression case：若错误地用「所有页」(5+73=78) 作分母，
  覆盖率会是 5/78≈6.4%；正确语义 `indexed/indexable = 5/5 = 100%`。
- `skipped_empty`（73 页空文本）**不降低**覆盖率。
- 分类过程：**0 付费调用、0 网络 embedding 调用、0 索引写入**（纯只读查询，未构造 provider）。
- UI 展示：检索页顶部 `st.caption`「向量索引覆盖率：{indexed}/{indexable}（缺失…待更新…）」，
  `indexable=0` 时隐藏；无隐式索引。

## 4. 7C — Weak-evidence acceptance

| # | 场景 | 判定 |
|---|---|---|
| 1 | vector 健康 + 非空全词面结果 | 弱证据提示显示（`test_healthy_vector_with_all_lexical_only`） |
| 2 | vector 健康 + 至少一条双来源 | 提示抑制（`test_healthy_vector_with_dual_source_result_suppresses_note`） |
| 3 | DISABLED / UNAVAILABLE / FAILED | 复用既有降级态，无冲突弱证据（三个 `*_suppresses_note` 用例） |
| 4 | 空结果 | 无提示（`test_empty_vector_path_suppresses_note`） |
| 5 | keyword-only 模式 | stale 弱证据不泄漏：`_run_keyword_search` pop `search_weak_evidence_note` |

规则为确定性布尔（vector ∈{OK,EMPTY} 且非空 且每条 `lexical_rank is not None` 且 `vector_rank is None`），
无比例阈值、无 label、无平行状态机。

## 5. 7D — Frozen benchmark 回归

- hash 校验：`corpus_synthetic_v1.json` = `8b73a7de…65465`，`queries_v1.json` = `6e153584…7ef125`
  （与 commit `610ffa5` 完全一致，未改动）。
- 重跑离线 benchmark（fixture）：keyword Hit@1=0.8846 / Hit@3=0.9231 / MRR=0.9183；
  hybrid 0.7308 / 0.8462 / 0.8135；regression=7——**与 Phase 1 baseline 逐位一致**。
- 结论：Phase 6 **未回归**检索行为，未引入任何新 eligibility / fusion 行为。

## 6. 7E — Final defect disposition

- **D-01**：`Production OPEN / PARTIALLY MITIGATED` — coverage 可观测；索引完成仍显式/手动。
- **D-02**：`PARTIALLY MITIGATED` — 已有诚实弱证据产品边界；不声称任何可靠数值 eligibility rule。
- **D-03**：`Production OPEN / DEFERRED PRODUCT DECISION` — 最终展示 bound 未定义；v0.5.1 无行为变更。

均未标记 FIXED（无新证据）。

## 7. Regressions

- 未发现 Phase 6 引入的直接回归。检索行为、UI 现有测试、frozen benchmark 全部通过。

## 8. READY TO CLOSE

- **v0.5.1 判定为 READY TO CLOSE**（待最终关闭授权）。
