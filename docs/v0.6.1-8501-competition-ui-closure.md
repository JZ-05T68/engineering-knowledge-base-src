# v0.6.1 — 8501 Competition UI / Teammate Handoff Closure（比赛界面交接收口）

日期：2026-08-30。执行：GLM-5.3。性质：**v0.6.1 比赛演示的呈现层收口与
前端交接闭环**；不改任何演示语义。起始基线：`d6c4022`（TD-06 后，main，
origin 同步，工作区干净）。

## 1. Current State（收口时的现状）

- 演示冻结 KEEP_FROZEN（`docs/v0.6.1-competition-demo-freeze.md`），P0 = 0，
  本轮未发现任何 P0/P1。
- 8501 实机走查（1920×1080 浏览器实测）确认全部六个必截画面按冻结值呈现：
  初始态、A 回答、A 来源 viewer、B 回答（双徽章+限制横幅）、
  C 琥珀完整性块、诚实空态。八类状态转换与重置由既有测试继续锁定。
- 品牌区/模式徽章/场景卡/回答区/来源栏的既有单屏叙事保持不变。

## 2. Changes Made（本轮改动）

### 2.1 代码（1 处，展示层 CSS）

- `pages/0_知识Agent.py`：页级 CSS 新增一条
  `[data-testid="stHeader"] { background-color: transparent; }`。
- **证据**：Streamlit 的固定顶栏为不透明白色，压在页面 0.9rem 紧凑顶距上，
  把页头第一行品牌标识「EKB · Engineering Knowledge Base」完全盖住
  （元素在 DOM 中且占位，但视觉不可见——实机 elementFromPoint 证实为
  顶栏遮挡）。该行是「5 秒理解这是 EKB」的品牌要素。
- **效果**：品牌行恢复可见；顶栏内的控件在该页本来就不可见/不可点
  （0×0 尺寸，与本次改动无关），背景透明化不改变任何布局与交互。
- 语义零变化：仅背景色；`ekb-aw-*` 命名空间与页面结构未动。

### 2.2 文档（2 份）

- `docs/v0.6.1-frontend-teammate-handoff.md`：在原交接包基础上扩充为
  A–M 速查结构（启动/主页面/GREEN-YELLOW-RED 文件分级/必守红线/场景与
  模式含义/已知限制/措辞边界/polish 方向/验证方法/服务停止重启/
  零真实 AI 声明）+ 队友演示检查清单（§N，13 项）。
- 本文档（收口记录）。

### 2.3 有意不做的事（及理由）

- **不动全局版本号**（见 §3）；**不动侧边栏排序**（知识Agent 已是第 1 位）；
  **不动首页比赛入口**（已有显著主按钮）；**不动「预置演示数据」chip 位置**
  （审计 P2-1 已明确留给队友 polish，语义受测试保护）；
  **不实现延迟中的引用/完整性真实契约**（ADR-008/009 保持 PROPOSED）；
  **不触碰 v0.7 预 Remediation 成果**。

## 3. Version-Display Decision（版本展示决定）

- 全局正式版本保持 `app_version = "0.6.0"` / `app_title = "工程知识库 v0.6.0"`
  （`src/config.py`），不因比赛里程碑提前变更——避免发布门禁语义混乱。
- 比赛里程碑以**展示层标签**呈现：仅 `pages/0_知识Agent.py` 显示
  「v0.6.1 · Competition Demo Experience · 本地 · 离线 · 信任感知」；
  这与 `scripts/release_check.py` 的 `MILESTONE_PAGES` 白名单规则一致
  （该页必须含 v0.6.1，其余页面必须只显示 v0.6.0）。
- **不**创建 v0.6.1 tag / GitHub Release / CHANGELOG 条目；v0.6.1 正式发布
  是后续独立流程。

## 4. Navigation Decision（导航决定）

- 维持既有结构：侧边栏「知识Agent」第 1 位；首页顶部「比赛演示 ·
  知识 Agent 工作台」+「进入知识 Agent →」主按钮为团队/评委入口。
- 演示页内叙事顺序维持冻结设计：品牌 → 模式徽章 → 提问框 → 三张场景卡 →
  回答区/来源栏；「更多演示问题」与「演示操作」保持折叠（低视觉优先级）。
- 演示页侧边栏默认折叠且顶栏控件隐藏是**冻结行为**（runbook 录制清单要求
  不打开侧边栏）；队友返回管理面用浏览器后退或直接输 URL，不新增返回按钮。

## 5. 8501 Behavior（当前实际行为）

- `http://127.0.0.1:8501` → 首页（管理面）→ 一键进入比赛页；
  或直接打开 `http://127.0.0.1:8501/知识Agent`。
- 默认模式 = 「预置离线演示（推荐）」，右上角琥珀徽章 + 回答卡
  「预置演示数据」chip 双标识；模式说明文案明示“不是实时模型输出”。
- A/B/C 一键运行；引用 chips 可点击；右栏 viewer 仅安全元数据
  （标题/类型/位置/引用号 + 支持说明 + 演示完整性状态）；
  C 场景琥珀块含「不代表造假或被篡改」免责句。
- 失败/空态/限流/服务不可用均有封闭中文安全文案；重置只清会话状态。
- 冷启动到 health 200 约 1–9 秒；全程零网络、零 API Key、零真实 AI。

## 6. Known Limitations（不变，如实交接）

1. Mode 1 正文 `#N` 映射未公开（P1 DEMO-03，需契约评审）；
2. Mode 1 无 per-source 完整性字段（需 maintainer+DeepSeek 契约评审）；
3. Mode 1 需维护者手工启动本机 Hosted 服务；
4. 演示语料为 synthetic 预置案例，正式语料批准 PENDING（RC-02/03）；
5. 公开部署 DEFERRED（WP6A PAUSED / WP6B NOT STARTED）；
6. Mode 2 下 A/B 来源 viewer 的「来源状态」为通用说明（fixtures 未提供
   该状态时的诚实回退，受测试锁定）。

## 7. Screenshot Readiness（六张必截画面，1920×1080 实机核验）

| # | 画面 | 状态 | 备注 |
| --- | --- | --- | --- |
| 01 | 初始屏 | **READY** | 品牌/里程碑/模式徽章/3 卡/信任提示一屏齐备（本轮修复品牌行不可见后） |
| 02 | A 回答 | **READY** | ✓ 有依据回答 · 引用 2 条 · 正文 chips · 预置演示数据标识 |
| 03 | A 来源详情 | **READY** | 来源详情 · 已验证 / 标题/类型/页码/引用号/支持说明 |
| 04 | B 回答 | **READY** | 历史经验 + ⚠ 限制横幅 + 知识记忆来源 |
| 05 | C 完整性 | **READY** | 琥珀「来源发生变化」+ 免责 + 演示预置说明 |
| 06 | 诚实空态 | **READY** | ○ 未找到可支持资料 + 「不会编造答案」句 + 换题建议 |

复现步骤见 `docs/v0.6.1-competition-demo-runbook.md` 截图清单节。
截图暂不入库（未获维护者明确授权）。

## 8. Teammate Readiness Verdict

**READY_FOR_TEAMMATE。**

- 交接包 15 分钟可上岗（A–M 结构 + 分级文件图 + 检查清单）；
- 六个必截画面全部 READY；演示语义 KEEP_FROZEN 未动；
- 后端契约 / schema / Tool registry / Hosted API / v0.7 全部未触碰；
- 验证：聚焦测试 PASS，全量回归与 release_check 结果见提交记录与本节下方。

## 9. Validation Evidence（本轮实测）

- 聚焦三件套（demo contract + demo ui + agent page）：PASS（exit 0，2 skipped）。
- 全量回归 / Ruff / release_check：见 git 提交信息与最终汇报（本轮在收口
  提交后于干净树上执行）。
- `git diff --check`：PASS。
- 真实 AI 调用：completion=0 / embedding=0 / rerank=0。
