# v0.6.1 Competition Demo UI — Agent Workspace 实现记录

实施日期：2026-08-29。执行：GLM-5.3。工作包：`Competition Demo UI — Question,
Answer, Verified Sources & Fallback`（v0.6.1 P0）。

本文记录实际交付的演示 UI；入口冻结与契约基线见
`docs/v0.6.1-competition-demo-entry.md`（未改写）。

## Page Path

| 项 | 值 |
| --- | --- |
| Agent 比赛页 | `pages/0_知识Agent.py`（Streamlit 页名「知识Agent」，侧边栏第 1 位） |
| 展示标题 | 「知识 Agent 工作台」· `v0.6.1 · Competition Demo Experience · 本地 · 离线 · 信任感知` |
| 展示支持模块 | `src/demo_ui.py`（纯 view-model 映射，无 Streamlit 依赖，有测试断言） |
| Mode 1 传输 | `src/agent_client.py`（loopback-only HTTP client，冻结 `/v0.6` 契约） |
| 首页入口 | `app.py` 顶部「进入知识 Agent →」一键入口（未改动既有首页功能） |
| 本地端点 | `127.0.0.1:8501`（未变；`OFFICIAL_HOST/PORT` 未动） |
| 全局 app_version | **未改动**（保持 `0.5.3`，版本号变更是 release 动作，不属于本包） |

`0_` 前缀是 Streamlit 官方页面排序机制；历史页面文件名未重命名。

## Visual Hierarchy（1920×1080 desktop-first）

```
┌──────────────────────────────────────────────────────────────┐
│ EKB · Engineering Knowledge Base          [预置离线演示] 徽章 │
│ 知识 Agent 工作台                                             │
│ v0.6.1 · Competition Demo Experience · 本地 · 离线 · 信任感知 │
├──────────────────────────────────────────────────────────────┤
│ 运行模式（radio：预置离线演示 / 本机 Agent）+ 模式语义说明     │
│ 问题输入框 + [提问]（form；空输入有提示）                      │
│ 演示题卡 · 一键运行（3 主 + 2 备 + 1 排练，compact 按钮）      │
├───────────────────────────────┬──────────────────────────────┤
│ LEFT 1.62fr                   │ RIGHT 1fr                    │
│ 当前问题卡：问题 + 信任徽章行   │ 已验证来源（计数徽章）        │
│ （✓/⚠/○/× + 引用 N 条 + 预置   │ 来源按钮（选中高亮「正在查看」)│
│ 演示数据标识）                 │ 每源 caption：类型 · 位置     │
│ 警示横幅（warnings 非空时）    │ 来源详情 viewer：             │
│ 答案正文（16.5px，#N chips）   │ 标题 / 类型 / 位置 / 引用 #N  │
│ 引用来源按钮行（点击选源）     │ /引用位置/完整性状态块/说明   │
└───────────────────────────────┴──────────────────────────────┘
```

- 自定义 CSS 全部内联于页面 `ekb-aw-*` 命名空间，页面级作用域；未改
  `.streamlit/config.toml`，16 个既有管理页样式不受影响。
- 无外部字体 / CDN / 远程 JS / 外部图片；离线可用。
- 所有动态文本进入 HTML 前经 `demo_ui.escape_text` 转义。

## Modes

| 模式 | 传输 | 标识 |
| --- | --- | --- |
| Mode 2 预置离线演示（默认） | `src.demo.MockDemoClient`（冻结 fixtures） | 页头徽章「预置离线演示」+ 模式说明含 `mock_demo` + 答案徽章行「预置演示数据」 |
| Mode 1 本机 Agent | `HostedAgentClient`（loopback `http://127.0.0.1:8000`，可在「本机服务连接」expander 改端口，强制回环校验） | 徽章「本机 Agent」；连接失败不自动伪装成功 |

- 模式切换清空 结果/选中来源（`STATE_KEYS_CLEARED_ON_MODE_SWITCH`），新提问清空选中来源。
- 请求代次 `agent_request_seq` 单调递增；晚到结果不覆盖新结果（`is_stale_result`）。
- 提交中不可重复提交（form submit + 每次重跑只执行一个 pending ask）。
- 失败态的「切换到预置离线演示」按失败记录的模式显示，切换是显式动作，从不静默回退。

## State Matrix（全部实现并有 AppTest/单测覆盖）

| 状态 | 呈现 |
| --- | --- |
| idle | 「向你的工程知识库提问」说明卡 + 题卡 |
| submitting/loading | `st.status` 粗粒度演示状态（提交/等待/整理/加载来源），明确为 presentation states，无 fake CoT/trace |
| grounded | ✓ 有依据回答 + 答案 + #N chips + 来源列表 |
| warning | ✓ + ⚠ 来源存在限制 双徽章 + 警示横幅 |
| no-evidence | ○ 未找到可支持资料 + 虚线诚实空态卡 + 换题建议（正向信任特性） |
| failed（业务） | × 本次请求失败 + 封闭错误文案 + 重试 |
| rate-limited | ⏳ 请求过于频繁（429/concurrency_limited） |
| backend-unavailable | ⏻ 本机服务不可用 + 解释 + 重试 + 显式切换 mock（连接失败/503/runtime/provider） |
| source unavailable | 答案不受影响；来源项标注「该来源暂时不可检查」 |

禁止泄露核验：无 traceback/JSON/DTO/内部 ID/路径/provider 信息出现在主 UI。

## Citation Behavior

- **Mode 2**：`citations_detail` 显式提供 `#N` ↔ stable_id 映射 → 正文 `【来源 #N】`
  渲染为可点击蓝色 chip，`display_index` 从 1 开始（契约保证）。
- **Mode 1**：`citations_detail` 字段缺失 → 回退为独立已验证来源列表（数组序），
  `display_index=None`，不猜测正文 #N 对应关系，不解析答案文本（单测锁定该行为）。
- title/label 为 null 时回退 `safe_display_text` → 短格式「页面资料 #N」（不含 UUID）。

## Source Viewer

仅展示安全公开/演示字段：标题、类型产品语言（页面资料/知识对象/知识记忆/证据）、
位置、引用 #N（仅 mock）、anchor 说明（仅 mock）、完整性状态、demo 说明。
不暴露：文件路径、raw DB id、SQL、provider 响应、token 用量、绝对路径。

## Integrity Semantics（Scenario C）

- `changed` → 「来源发生变化」+ 解释「快照不一致……不代表造假或被篡改」
  （禁止 可信/不可信 二元道德断言；单测断言免责文案存在）。
- `demo_note` 明确标注「演示预置状态……不是实时核验结果」。
- Mode 1 无 per-source integrity → viewer 显示「当前模式未提供该来源的完整性状态，
  仅展示来源元数据」。
- 演示中不改真实数据、不用生产 DB。

## Manual Smoke Evidence（2026-08-29, 1920×1080, in-app browser）

| 项 | 结果 |
| --- | --- |
| health `127.0.0.1:8501` | 200 |
| 初始态（5 秒规则） | PASS：品牌/工作台/题卡/grounded 预期一屏可读 |
| Scenario A（grounded + 2 源 + viewer） | PASS |
| Scenario B（历史经验 + 限制横幅 + 知识记忆源） | PASS |
| Scenario C（完整性 changed + 免责 + 演示预置标注） | PASS |
| Empty（诚实空态 + 换题建议） | PASS |
| Failed（排练失败 + 重试 + 封闭文案） | PASS |
| Backend unavailable（Mode 1 拒连 + 显式切换） | PASS（切换后自动重跑 grounded） |
| 横向溢出 | 无（`scrollWidth=1920`，行右缘 ≤1760） |
| raw JSON/异常/私有路径 | 未出现 |
| 首页一键入口 | PASS（首页按钮 + 侧边栏第 1 位） |

## Tests

| 文件 | 覆盖 |
| --- | --- |
| `tests/test_demo_ui.py`（新增 28 项） | 状态映射、徽章、错误码/传输态映射、citation 双模式映射、#N 禁猜、null 回退、完整性语义与免责、安全转义、preset 映射、stale 守卫、无数字置信度、模块禁 import streamlit |
| `tests/test_agent_demo_page.py`（新增 16 项） | AppTest 页面集成（初始/A/B/C/空/失败/限流/不可用/模式切换清态/首页入口）+ Mode 1 客户端（冻结契约解析、429/503 归一化、拒连→runtime_unavailable、非回环 URL 拒绝） |
| `tests/test_demo_contract.py` | 既有契约测试保持 PASS（未削弱） |

## Known UI Gaps（不擅自改后端契约）

1. Mode 1 正文 `#N` 不可点击映射（原 DEMO-03，P1，需契约评审）；
2. Mode 1 无 per-source integrity 字段（viewer 显示通用说明，需 maintainer+DeepSeek 评审）；
3. Mode 1 需先手工启动 Hosted 服务（默认 `127.0.0.1:8000`；页内 expander 可改端口）。

## Deferred Polish（建议前端队友接力包）

- 比赛视觉品牌化（logo、深色模式）、动画节奏、源面板抽屉化、Mode 3 录像。
