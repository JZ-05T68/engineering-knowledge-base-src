# v0.5.0 Phase 10C 受控真实检索实验证据归档

- 日期：2026-08-16
- 分支：`kimi/v0.5.0-controlled-real-index`
- Closure HEAD：`c919390bc070e2dfbcbf9b00cd79243d974076ef`（`c919390`）
- Step 3 checkpoint commit：`c919390`（`feat(ai): add controlled real query probe (Phase 10C Step 3)`）
- 模型：`qwen3.7-text-embedding`，dimensions = 1024，config_version = 1
- 环境：隔离 staging 实例（`staging-data/`，端口 8502，gitignored）

本文档只归档实验事实与必要指纹。staging DB、embedding BLOB、API 原始响应、
API Key、`.env` 与 baseline backup 均不入 Git。

## 一、真实调用记账（Phase 10C 全程）

| 阶段 | embedding HTTP | LLM | rerank | retry | 说明 |
|---|---|---|---|---|---|
| Step 2 | 1 | 0 | 0 | 0 | page 18 单页 embedding，1024 维 |
| Step 3 | 1 | 0 | 0 | 0 | query embedding，usage 27 tokens |
| Step 4A | 0 | 0 | 0 | 0 | 纯本地数据准备（OCR 文本补充） |
| Step 4B | 2 | 0 | 0 | 0 | page-side 1（4 页 batch，usage 1746）+ query-side 1（usage 27）|
| **合计** | **4** | **0** | **0** | **0** | 全部单请求、`max_extra_attempts = 0` |

Closure 阶段（本文档）0 次真实 API 调用。

## 二、Staging 最终状态

- documents = 1
- pages = 78
- page_embeddings = 5
- embedding page IDs = `5, 17, 18, 19, 40`（全部 fresh = YES）
- model = `qwen3.7-text-embedding`，dimensions = 1024，config_version = 1
- staging DB sha256（最终）`015e8177…ac32`

## 三、冻结候选集合（Step 4A 批准）

全部来自同一文档：`STM32入门(标准库)(新78页版)`

| page_id | page | 分类 | source | chars | 冻结 fingerprint（searchable_content sha256） |
|---|---|---|---|---|---|
| 18 | 18 | strong positive | markdown | 542 | `f2a77afa…7894a9` |
| 17 | 17 | near-positive / strong competitor | ocr_text | 621 | `35c5f076…2063ea` |
| 19 | 19 | hard negative | ocr_text | 774 | `feaad60c…83ef255` |
| 5 | 5 | clear negative | ocr_text | 795 | `10ad5d4c…2d20d0a2` |
| 40 | 40 | clear negative | ocr_text | 701 | `a6f24b38…e06447c` |

四页（5/17/19/40）embedding 在 Step 4B 生成，指纹与 Step 4A 冻结内容逐一一致；
page 18 embedding 为 Step 2 生成、Step 4B 时保持 fresh 未被重建。

## 四、冻结假设

```
page 18 ≈ page 17 > page 19 > page 5 / page 40
```

## 五、实际排名（真实多候选 cosine，5 项全量）

| rank | page_id | page | classification | cosine similarity |
|---|---|---|---|---|
| 1 | 18 | 18 | strong positive | 0.761671 |
| 2 | 19 | 19 | hard negative | 0.683189 |
| 3 | 17 | 17 | near-positive | 0.646748 |
| 4 | 40 | 40 | clear negative | 0.440642 |
| 5 | 5 | 5 | clear negative | 0.429412 |

梯队：`[18] > [19, 17] > [40, 5]`

明确记录：该结果与冻结假设的 `page 18 ≈ page 17` 及 `page 19` 位置不同
（page 19 反超 page 17），**不**改写为 "hypothesis fully passed"。这是合法实验
结果，按规则原样保留，禁止重跑修正。

## 六、Proven（Phase 10C 最终确认的 13 项真实能力）

1. Controlled real page embedding generation
2. Persistent page embedding storage（schema v8，float32 1024 维）
3. Embedding fingerprint / freshness validation（5/5 fresh）
4. Controlled real query embedding
5. Query embedding non-persistence
6. Vector-only retrieval（lexical 无命中时纯 vector 路径）
7. Multi-candidate cosine ranking（5 候选全量输出）
8. Semantic discrimination between relevant and clear-negative pages
   （0.762 vs 0.43/0.44）
9. HybridSearch integration（RRF 融合 + fused score）
10. Hydration / citation integrity（title/filename/page/image/source/sha256/status 全有值）
11. Staging / production isolation（production sha256 全程不变）
12. Strict paid-call accounting（4 HTTP 全程记账）
13. Zero-retry failure boundary（max_extra_attempts = 0，无自动重试）

## 七、Known Limitations（Phase 10C 实验边界）

- 单文档（78 页）、单 query、5 个候选，不足以建立通用 retrieval quality benchmark。
- 该 query 在 FTS 无命中，HybridSearch 实际为纯 vector 路径，`lexical_rank` 全部为 None；
  **lexical + vector 同时有效的双路融合尚未被真实数据验证**。
- 尚未启用 rerank（rerank channel 未触碰）。
- 中间梯队区分度有限：page 17（0.647）与 page 19（0.683）差距仅 0.036。
- page 17 / 19 的中间梯队顺序与人工假设不同（page 19 反超 page 17），
  可解释为其与 query 存在真实定时器术语（PSC/ARR/CNT）与结构重叠。

## 八、Safety Evidence

- production documents = 0、page_embeddings = 0
- production DB 全程未变化（sha256 `91cd67ba…4882` 前后一致）
- query embedding 未持久化（无 query_embeddings 表，无新增表）
- 无 search history 写入
- no retry（4 次调用全部单请求成功）

## 九、验证记录（Closure 阶段）

- 完整 `pytest`：**1318 passed**（0 failed）
- `ruff check .`：通过
- baseline backup（`staging-data/backups/step4a-baseline-20260816-022138/`）保留，
  内容验证 documents=1 / pages=78 / page_embeddings=1（page 18），可恢复 Step 3 PASS 状态
