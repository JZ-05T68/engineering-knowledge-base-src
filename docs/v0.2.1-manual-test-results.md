# Engineering Knowledge Base v0.2.1 人工冒烟测试结果

## 测试信息

- 测试版本：v0.2.1
- 测试提交：5c047eaa68396f94320d7cde34f15f4078d6efff
- 测试日期：2026-07-25
- 隔离工作区：D:/ekb-isolated/v0.2.1-manual
- 隔离端口：127.0.0.1:8502
- 隔离数据库：data/database/knowledge.db
- 测试 PDF：v0.2.1-smoke-test.pdf（虚构三页测试文档）
- 测试 PDF SHA-256：40da61a02b7a7ed89cf1bd090cb52d7cafc97bd99e8455de247c382dd71cad1b
- 测试环境：Windows 本地环境、Python 3.11、Streamlit、SQLite schema v4
- 正式服务：http://127.0.0.1:8501 全程保持运行，不参与任何测试步骤

## 总体结论

PASS

九项隔离人工冒烟测试全部通过。v0.2.1 并发首访修复在真实浏览器多标签页、多并发访问
场景下表现正确，证据操作与证据包无回归，schema 保持 v4，主机绑定与离线边界不变，
正式服务与正式数据未受影响。

## 测试结果

| 编号 | 场景 | 结果 | 实际观察 |
|---|---|---|---|
| 1 | 版本与隔离环境 | PASS | 隔离实例正常启动，首页与系统维护页均显示 v0.2.1；隔离数据目录独立于正式环境 |
| 2 | 并发首次访问 | PASS | 多个并发首次访问同时进入证据篮，最终只创建一个默认证据篮，所有访问返回同一篮 ID |
| 3 | 多标签页刷新与页面往返 | PASS | 连续刷新与多标签页交替访问证据篮、检索页后，默认篮计数仍为 1，各页面显示一致 |
| 4 | 导入三页测试 PDF | PASS | v0.2.1-smoke-test.pdf 导入成功，3 页全部渲染并提取文本，导入状态 completed |
| 5 | 跨标签页查看同一证据 | PASS | 检索页添加的证据在证据篮页及新标签页中均可见，内容一致，未进入其他篮 |
| 6 | 重启持久性 | PASS | 停止并重新启动隔离实例后，默认篮与已添加证据完整保留 |
| 7 | 证据操作与证据包回归 | PASS | 证据排序、备注编辑、证据包生成导出、单条删除均正常；删除只影响目标条目 |
| 8 | schema 与数据库兼容性 | PASS | schema_migrations 精确为 [1, 2, 3, 4]；integrity_check 为 ok；外键检查无结果；无孤立记录 |
| 9 | 主机、端口与离线边界 | PASS | 8501、8502 均只监听 127.0.0.1；8503–8512 无监听；核心功能无需外部 API、账号或网络 |

## 并发首访专项核验

- 测试 2、3 覆盖并发首次访问与多标签页反复访问场景。
- 核验时 `evidence_baskets` 表仅 1 行：`id=1`，名称为“默认证据篮”。
- 同名篮重复检查：按名称分组计数，不存在 COUNT > 1 的记录。
- 所有证据条目均属于篮 ID=1，不存在指向其他篮的“影子”证据。

## 数据库核验

对隔离数据库 `D:/ekb-isolated/v0.2.1-manual/data/database/knowledge.db` 使用只读连接
（`mode=ro`）执行核验，结果：

- `PRAGMA integrity_check`：ok
- `PRAGMA foreign_key_check`：无结果
- `schema_migrations`：精确为 [1, 2, 3, 4]，无 v5 或其他新增迁移
- 文档总数：1（v0.2.1-smoke-test，page_count=3，import_status=completed）
- 页面总数：3（均属于文档 1，页码 1–3 完整）
- 证据篮总数：1（默认篮 ID=1，唯一）
- 证据条目最终数量：1（测试 7 删除一条后保留的一条，属于篮 1、页面 1）
- 孤立证据检查：指向不存在篮、页面或文档的证据均为 0；指向不存在文档的页面为 0
- 数据库内文档 SHA-256 与测试 PDF 实际 SHA-256 一致
- Git diff 确认 v0.2.1（相对 6426b39bba3bb6ad0a51c993b9587c03df3065af）未修改
  `src/migrations.py` 与 `src/database.py`，无 schema 或迁移变化

## 证据操作与证据包核验

- 测试 7 中完成证据顺序调整、备注编辑、证据包生成导出与单条删除，均符合预期。
- 证据条目 id=2 保留且完整（id=1 为测试 7 中删除的目标条目），证明删除只影响目标条目。
- 证据包导出内容与实际篮内证据一致，导出前来源完整性校验通过。

## 进程与端口核验

- 正式服务：PID 监听 127.0.0.1:8501，入口为 `D:/Projects/engineering-kb/app.py`，
  健康检查 `/_stcore/health` 返回 200 ok。
- 隔离服务：监听 127.0.0.1:8502，从 `D:/ekb-isolated/v0.2.1-manual` 目录以隔离代码与
  隔离数据启动，健康检查返回 200 ok；未监听 0.0.0.0。
- 8503–8512 全部无监听。
- 两个服务进程树独立，未发现未知监听或未知服务进程。
- 说明：本轮核验开始时 8502 隔离进程已不在运行（此前会话结束后未保留），遂以相同参数
  从隔离目录重新启动；重启后监听地址、入口路径与健康状态均核验无误，隔离数据与
  测试 7 结束后的现场一致。

## 正式环境隔离核验

- 全程未访问、复制或修改正式数据库；正式工作区 `D:/Projects/engineering-kb` 未被修改。
- 正式服务 8501 全程保持运行，未被停止或重启，健康检查持续 200 ok。
- 测试数据（测试 PDF、页面 PNG、数据库、证据）仅存在于隔离目录。
- 核心功能核验：不需要 API Key、登录、账号、密码、OAuth、JWT、VPN、云存储或云同步；
  不调用外部 AI、OCR、Embedding、向量搜索或模型 API；测试 PDF 导入、页面浏览、证据篮
  与证据包均由本地数据完成。v0.2.1 未新增任何网络依赖或外部服务要求
  （requirements.txt 与依赖清单相对 v0.2.0 无变化）。

## 遗留问题

- 既有 Streamlit `components.v1.html` 弃用提示与 v0.2.1 补丁无关，按既定结论记为
  非阻断项，不在本版本处理。
- 无其他遗留问题。
