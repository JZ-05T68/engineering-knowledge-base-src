# EKB v0.5.3 — Codex Release Handoff

- 生成日期：2026-08-25
- 状态：本地 Release Candidate 已冻结；**所有 Git 写操作尚未执行**（DeepSeek 未 add/commit/push/tag/Release）。
- 目标：由 Codex 在外部审核 PASS 后接管 Git 与 GitHub 发布。

## 1. 当前 HEAD / 分支

- HEAD：`57cecef1379b04c8cd5c31693873678119e4911c`
- 分支：`main`（与 `origin/main` 关联）
- 工作区：存在预期的 v0.5.3 未提交改动（87 个文件，见下）。

## 2. 允许提交文件清单（全部为源码/测试/正式文档）

- 应用与页面：`app.py`、`pages/`（16 个正式页面，含新增 `16_AI调用台账.py`）
- 源码：`src/__init__.py`、`src/config.py`、`src/database.py`、`src/migrations.py`、
  `src/models.py`、`src/runtime.py`、`src/evidence_basket_service.py`、
  `src/knowledge_context.py`、`src/knowledge_context_packager.py`、
  `src/knowledge_export_service.py`、`src/legacy_backup_upgrade_service.py`、
  `src/rag_answer_ui.py`、`src/experience_ui.py`、`src/ai_ledger_service.py`、
  `src/ai_ledger_ui.py`、`src/ai_ledger_export_service.py`、
  `src/ai/__init__.py`、`src/ai/provider.py`、`src/ai/qwen_client.py`、
  `src/ai/rag_prompt_builder.py`、`src/ai/rag_answer_service.py`、
  `src/ai/experience_prompt_builder.py`、`src/ai/experience_model_service.py`
- 脚本：`scripts/release_check.py`、`scripts/upgrade_backup.py`
- 测试：`tests/` 下 25 个测试文件（含本阶段新增的 citation/experience/ledger/export/roundtrip 测试）
- 正式文档：`README.md`、`README_EN.md`、`CHANGELOG.md`、
  `docs/v0.5.x-roadmap.md`、`docs/v0.5.3-phase0..6 报告与契约`、
  `docs/v0.5.3-release-notes.md`、`docs/v0.5.3-release-notes-en.md`

## 3. 必须排除文件清单（不得 git add）

- `data/`（用户数据库 `data/database/knowledge.db`、raw/pages/markdown 原文、
  `data/database/backups/` 全部旧备份，含真实 schema v8 备份）
- `backups/`（正式备份目录，含 `ekb-v0.5.3-*` 与 `release-v0.5.3-*` 备份）
- `logs/`、`runtime/`、`.env`、`__pycache__/`、`.pytest_cache/`、staging 临时目录
- 真人验收与升级输出（位于系统临时目录，不在仓库内）
- 任何含 API Key、Authorization header、私人正文、页面 PNG、raw PDF 的文件

## 4. 敏感信息审计结果

只读扫描全部 modified/untracked 文件：

- 未发现硬编码 API Key、Token、Authorization 头、`.env` 内容或私人数据库路径。
- 命中的 `sk-*` 均为测试夹具中的脱敏示例；`Authorization` 仅出现在
  `src/ai/qwen_client.py` 的运行时请求构造（从 `SecretStr` 读取，不落盘）。
- `data/database/knowledge.db`、`data/database/backups/**`、`.env` 均被 `.gitignore` 覆盖。
- 报告只保留脱敏哈希前缀、计数与结果，不复制私人正文。

## 5. 推荐 commit 分组（供 Codex 参考）

1. `feat(ai): v0.5.3 audited RAG answer, experience model and AI ledger`
   （src/ai、src/models、src/database、src/migrations、src/runtime、相关测试）
2. `feat(context): ContextItem projection and KnowledgeContextPackage`
   （src/knowledge_context*.py、src/models、测试）
3. `feat(export): knowledge export, AI ledger export and schema-v8 upgrade`
   （src/*export*、src/legacy_backup_upgrade_service.py、scripts/upgrade_backup.py、测试）
4. `docs(release): v0.5.3 bilingual README, release notes, changelog and roadmap`
   （README*.md、CHANGELOG.md、docs/v0.5.x-roadmap.md、docs/v0.5.3-*.md）
5. `chore(release): v0.5.3 version alignment and release_check upgrades`
   （src/config.py、src/__init__.py、app.py、pages/、scripts/release_check.py、tests/test_config.py）

## 6. 推荐 commit message

`release: v0.5.3 — Audited AI Integration`

## 7. 目标 tag

- `v0.5.3`（annotated；**本阶段不得创建**）

## 8. 中英文 Release Notes 路径

- `docs/v0.5.3-release-notes.md`
- `docs/v0.5.3-release-notes-en.md`

## 9. release_check 结果

见 `docs/v0.5.3-phase7-release-candidate-report.md`。预期：除 Git workspace 因
未提交的 v0.5.3 改动而 FAIL 外，其余全部 PASS。Codex 本地 commit 后需重跑
`python scripts/release_check.py --expect-service-stopped` 确认 Git workspace PASS。

## 10. 正式 release backup 路径和哈希

- 目录：`backups/ekb-v0.5.3-20260825-215701-708238`
- application_version：0.5.3；schema_version：12；validate_backup：通过
- 生产库迁移后 SHA-256：`59caf2cfc5e80d197ca02a64b702ea6d06b7c4eb66e02c7fefa272403a4c0ad9`
  （size 413696）

## 11. production 8501 状态

- 已按授权流程启动并保持运行：`127.0.0.1:8501`，health HTTP 200。
- 仅监听 `127.0.0.1:8501`；8502–8512 无监听。
- 全部 18 个官方入口（首页 + health + 16 个页面）HTTP 200。
- Smoke Test 前后生产库 size/mtime/SHA-256 一致。

## 12. GitHub 发布后应复验项目

- 本地 commit 后重跑完整 release_check（Git workspace PASS）。
- GitHub main / tag v0.5.3 / Release 页面内容与中英文 Release Notes 一致。
- production 8501 health HTTP 200 持续正常。

## 13. 声明

DeepSeek 未执行 git add / commit / push / merge / tag / GitHub Release。
v0.5.3 当前只是本地 Release Candidate，尚未 GitHub 发布，尚未正式 CLOSED。
