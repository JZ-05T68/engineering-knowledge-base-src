# v0.6.0 WP6A — Deployment Target Validation & P0 Preparation

## Status and authority

- **Result: PARTIAL. Ready for WP6B: NO. Public Agent exposure: NOT ALLOWED.**
- **Release-closure entry: Public deployment = PAUSED; WP6B = NOT STARTED;
  Public Agent exposed = NO; Actual demo corpus = PENDING. v0.6.0 is NOT RELEASED.**
  Archiving these documents does not complete WP6A or authorize deployment.
- **Current handoff:** maintainer submitted Standard support ticket
  [6a91903f19d208af60b18288](https://zeabur.cn/support/ticket/6a91903f19d208af60b18288).
  Submission is SCREENSHOT OBSERVED; platform response and non-root resolution
  remain PENDING. Ticket creation is not a deployment acceptance PASS.
- Official documentation accessed: **2026-08-28**.
- Sole workspace: `D:\Projects\ekb-dev`; branch `main`.
- Baseline / fetched `origin/main`: `92425fdc40afff43d56893b933a01398eab68765`.
- Original entry checks: clean working tree, ahead/behind `0/0`, expected branch
  and SHA. Cloud-resume entry retains the expected two untracked WP6A documents;
  HEAD and `origin/main` remain at the same baseline. No reset/stash/clean performed.
- Gate: **CONDITIONAL_GO / HIGH**, BLOCKER 0, MAJOR 0, MINOR 1, OBSERVATION 8.
- Scope: documentation, platform investigation and isolated synthetic compatibility
  checks. Production code, Dockerfile, dependencies and deployment configuration
  files unchanged. Codex performed no purchase, subscription, identity submission,
  deployment, registry push, commit or push. **The maintainer has purchased the
  server, manually created `ekb-staging` and registered its `data` → `/data`
  volume through the guided UI steps**, as recorded below. After the arbitrary-Git
  Dockerfile rejection and explicit-recipe correction instructions, the maintainer
   reports success; a service overview shows **运行中**, followed by deployment
   details showing **启动中** and completed build/image upload. A runtime
   capture showed **已移除**, repeated BackOff and `hosted_startup_failed`; the latest
   capture shows **运行中** and the temporary `ekb_maintenance_only` marker.
   These are build and failed-start evidence, not verified EKB health,
  readiness, exact deployed Dockerfile or storage persistence. WP6B was not executed.

Input: [Pre-public deployment gate](v0.6.0-pre-public-deployment-gate.md),
[WP5 packaging](v0.6.0-public-deployment-wp5-packaging.md), the WP6A maintainer
instructions, and frozen runtime/storage code at the baseline. This document
does not amend ADR-007 or any frozen Agent, storage or security decision.

**Evidence labels matter:** DOCUMENTED means current official platform material;
MAINTAINER CONFIRMED means a fact supplied by the maintainer from their console;
SCREENSHOT OBSERVED means visible content in a maintainer-supplied screenshot,
limited to the captured screen and time, not an automated runtime verification;
LOCAL PASS means an executed host probe; NOT VERIFIED / NOT RUN means no actual
cloud evidence. Neither documentation nor a local probe admits the cloud target.
An unverified acceptance item is not an observed platform failure, but it cannot
count as PASS. If the platform cannot guarantee one active instance, the decision
must become **TARGET_PLATFORM_REJECTED**, not a waiver.

## Maintainer cloud update and current resume evidence

The maintainer reports the following completed cloud state and explicitly authorizes
WP6A work in the existing project. These are **MAINTAINER CONFIRMED**, not invented
Codex console measurements or new purchase permissions.

| Field | Current state / evidence source |
| --- | --- |
| Provider / region | **Aliyun / Shanghai, Mainland China** — maintainer confirmation, corroborated by server-details screenshot showing **Alibaba Cloud / Shanghai, CN** |
| Compute / memory / OS | Maintainer reports **2 vCPU / 4 GiB / ZeaburOS**; server screenshot displays **2 CPU cores / 4 GB memory / ZeaburOS / K3s** (preserving the UI units) |
| Server price | **USD 6/month**, also SCREENSHOT OBSERVED; maintainer accounting rate 6.8 gives **RMB 40.8/month** |
| Server ID | **6a911c45af37eeef8fb27a27**, from the supplied server-details URL and displayed identifier |
| Disk / egress / expiry | Screenshot displays **48.8 GB disk**, outbound traffic **unlimited**, expiry **2026-09-29**; bandwidth rate and volume entitlement remain unverified |
| Account funding | **USD 20 deposited by maintainer**; not a statement of remaining balance, a three-month prepayment or permission for Codex to spend |
| Purchase | **COMPLETED BY MAINTAINER**; Codex payment automation **NONE** |
| Project | **EKBstaging**, existing on the purchased server per maintainer |
| Project purpose | **v0.6.0 staging / WP6A cloud validation**, explicitly confirmed by maintainer |
| Initial services | Before creation, the project overview and project card showed **NO SERVICES** |
| Current service | **ekb-staging**, ID **6a915d64cb6b9b31c9e68ad7**; latest screenshot **运行中**, maintenance metadata at **21:14:53** reports UID/GID **0:0**, mounted `/data` owner **0:0**, mode **0777**. Non-root gate unmet; no EKB health/readiness PASS |
| Volume configuration | Latest volume-list screenshot shows **data → /data**, usage **未知 / 无限**; control-plane registration observed, filesystem access and durability unverified |
| Public Agent / real AI secret / actual corpus upload | **NO / NO / NO**, per maintainer; unchanged by Codex |
| Additional resources | **DO NOT PURCHASE**; no additional server, subscription, paid volume or domain authorized |

Direct browser metadata observed an existing Zeabur project tab at
`https://zeabur.cn/projects/6a911d16b7ff62ee8d7fc5f1?envID=6a911d163bf3ef23ef4d4b4d`.
Observed project ID: `6a911d16b7ff62ee8d7fc5f1`; environment ID:
`6a911d163bf3ef23ef4d4b4d`. The project name/purpose comes from the maintainer's
explicit clarification. The tab URL alone does **not** establish the server ID,
server-project association, replica policy, service configuration or health.
This is a **control-plane URL**, not a staging application/public Agent URL.

The earlier in-app-browser resumption was blocked at reliable console interaction: navigation timed
out; recovery found the project tab, but its DOM and screenshot reads timed out.
No service creation or cloud settings mutation was submitted. No Zeabur connector
was available, and `Get-Command zeabur` found no CLI on the current PATH. No CLI,
browser extension or system software was installed; no cookies/session tokens or
real secrets were read. Restore a controllable project page, or provide a supported
authorized management connection, before cloud mutation. Do not infer a login
failure from these timeouts or ask for another server purchase.

### Maintainer-supplied screenshots — 2026-08-28

The supplied captures provide **SCREENSHOT OBSERVED** evidence. The maintainer
reused the filename `QQ20260828-174357.png` for successive screens; the descriptions
below distinguish the conversation captures rather than treating that filename as
an immutable artifact identifier:

- `QQ20260828-174357.png`: Zeabur project **EKBstaging**, matching the project
  and environment URL above, shows the linked server label **Aliyun Shanghai
  2C 4...** with a green indicator. The sidebar and center both report no services.
  A purple **新建服务** button is visible in the center; the sidebar has the same
  action. The displayed association is confirmed, but the truncated label and
  indicator do not independently establish a unique server ID, provider metadata,
  actual capacity, volume pricing, replica count or deployment strategy.
- `QQ20260828-174524.png`: the public GitHub repository
  **engineering-knowledge-base-src**, branch **main**, shows short commit
  **92425fd**, titled **docs: record v0.6.0 pre-public deployment gate**. This
  matches the short form of the frozen local baseline. It is the GitHub repository
  homepage, **not** Zeabur's repository-selection or build-configuration screen;
  no service source binding, Dockerfile detection or deployment is proved.
- Subsequent service-source menu: **连接 GitHub 账户**, **Git 仓库**, templates,
  database, small app, local project, Docker container image and Cursor are shown.
  The maintainer was directed to **Git 仓库** without connecting a new GitHub app.
- Git repository form capture: visible fields are **Git URL**, **服务名称**,
  **分支**, **根目录** (shown as `/`) and **认证方式**. **匿名**, described as
  suitable for public repositories, is selected. HTTPS Token and SSH key are
  alternative options, not selected or inspected. The form has a final **部署**
  button; no separate pre-deployment settings step is assumed. Git URL, service
  name and branch show placeholders, not submitted configuration. No replica,
  volume, included-capacity or cost information is visible in this form.
- Latest server-details capture: the **服务器信息** tab for **Aliyun Shanghai
  2C 4GB** is selected. URL: `https://zeabur.cn/servers/6a911c45af37eeef8fb27a27`;
  displayed identifier: `server-6a911c45af37eeef8fb27a27`. Its provider, region,
  capacity and price corroborate the maintainer's purchase report as detailed below.
- `1.png`: the global **我的项目** page at `https://zeabur.cn/projects` displays
  an **EKBstaging** card with its server explicitly labelled **Aliyun Shanghai
  2C 4GB** and services **没有服务**. This confirms the full project-to-server
  display-name association. It is the global project list, not a captured
  server-specific project list; the server link target/ID is not exposed in this
  image and must not be reported as extracted from it.
- Subsequent `1.png` (filename reused): the service page shows **ekb-staging**,
  created **Aug 28, 2026**, ID **6a915d64cb6b9b31c9e68ad7** in the URL and
  displayed identifier. It belongs to the same EKBstaging project/environment:
  `https://zeabur.cn/projects/6a911d16b7ff62ee8d7fc5f1/services/6a915d64cb6b9b31c9e68ad7?envID=6a911d163bf3ef23ef4d4b4d`.
  The status is **未部署**, with a truncated GitHub source link matching the
  expected owner/repository prefix and server **Aliyun Shanghai 2C 4GB**. The
  source branch, full resolved commit, Dockerfile detection and build result are
  not shown. One sidebar service entry is visible; this is not a replica count.
  Visible top tabs are **服务状态 / 环境变量 / 用量 / 网络 / 硬盘 / 设置**.
  **重新部署**, **重新启动**, **文件**, **日志** and **命令** controls are also visible,
  but their presence does not establish a prior successful deployment or running
  container. No control was operated by Codex.
- Earlier `1.png`, service **设置** upper section: **来源** displays the full URL
  `https://github.com/JZ-05T68/engineering-knowledge-base-src.git` and branch
  **main**. **编译规格** shows **Free / 2 vCPU / 4 GB** and an unused upgrade
  button. This establishes the selected source/branch and build tier, not the
  resolved deployment commit, Dockerfile detection, runtime resources or a build
  result. The configuration-file editor button is visible but unopened. The
  **启动命令 (ENTRYPOINT)** section is only partly visible at the bottom and its
  input shows an example placeholder; no override is evidenced in that capture.
- Latest settings captures `1.png` and `2.png`: the lower settings are now visible.
  **ENTRYPOINT / CMD** inputs show example placeholders, with no entered override
  visible. The Dockerfile override editor shows subdued Node example text;
  screenshot appearance suggests a placeholder but does not prove the underlying
  field value or deployed image. Do not save, load or edit that override. The
  actual root Dockerfile still requires build evidence. **定时自动重启** is off.
  Additional observed settings are recorded below.

| Service setting | SCREENSHOT OBSERVED state / limitation |
| --- | --- |
| 副本数 | **1** in a greyed field, with **升级方案** below. This is numeric UI configuration evidence, not a running-pod count or proof that no rollout overlaps |
| 崩溃后自动重启尝试次数 | **0** displayed; leave unchanged during initial inspection. The help text describes 0 as no restart and -1 as unlimited; no unlimited retry is enabled by Codex |
| 健康检查 | Toggle on, protocol **TCP**, adjacent selector has no visible selected text. No HTTP path or effective target port shown; this is not a `/health` or `/ready` check result |
| CPU / memory limits | Grey text **1000 m / 1024 Mi** in inputs; placeholder versus persisted values is not resolved from the image, so no effective limits are claimed |
| Deployment strategy | No explicit strategy field visible in the supplied settings captures. Volume-dependent Recreate behavior and stop/start chronology remain unverified |

### Volume workflow and registered mount — screenshot evidence

The next two maintainer captures (both supplied as `1.png`) show the same
`ekb-staging` service's **硬盘** page and then its **挂载硬盘** dialog:

- The list says **尚未挂载任何硬盘**. A **+ 挂载硬盘** button is visible.
- The dialog has **硬盘 ID** and **挂载目录** inputs with subdued `data` and
  `/data` text, apparently placeholders rather than submitted values, and a
  final **挂载硬盘** button. No capacity, new server, storage SKU, price or
  subscription choice is visible. The only notice concerns possible brief downtime.
- These captures establish an available mount workflow, not a completed mount,
  Linux filesystem identity, permissions, persistence or Recreate execution.

Under the existing maintainer authorization and the dedicated-server no-extra-usage
policy in the initial service creation decision below, the maintainer was directed to create **one** ordinary service volume:
**ID `data`, mount directory `/data`**, on the already-purchased server. Enter
both values explicitly. This uses the existing service's standard mount path;
no separate paid disk, expansion, add-on or server is authorized. Stop if the flow
introduces a charge, paid feature or another resource. Absence of a price in the
dialog alone is not the basis for the no-charge interpretation. Do not claim a
specific provisioned size: the observed form has no capacity field, and the 1 GiB
engineering allocation remains a planning allowance rather than a platform quota.

No cloud DB has been uploaded or successfully initialized in the observed workflow.
Mount before seeding, because mounting replaces the directory view. After submission,
record the resulting volume row/identifier and mount path; then verify actual disk
backing, UID/GID 10001 access, writable `/data` and a supported operator-only upload
path before any synthetic DB startup. Keep replica 1, real AI key absent and no
domain. This was an instruction, not execution evidence; the later screenshot
provides the control-plane result described next. No mount submission through a
Codex browser-control tool occurred.

The latest `1.png` now shows a single volume row **data**, **挂载目录 `/data`**,
with usage **未知 / 无限** and a delete/trash control. This establishes that the
maintainer submitted the mount and the console registered it for the existing
service. It does not establish a mounted Linux filesystem, writable permissions,
volume/PVC filesystem identity, unlimited physical storage or restart persistence.
Physical server capacity remains the previously observed **48.8 GB**; the green
usage bar must not be treated as a filesystem health/capacity test.

A **备份还原** tab is now visible and a lower-right recommendation says the service
has mounted a disk, with **跳过** and **创建备份** actions. This is a recommendation,
not evidence of a created backup, paid plan or automatic-backup activation. Skip
the recommendation; no backup or subscription purchase is authorized. Do not
delete the volume, click its trash icon, remount it or create a second volume.
The following service-status screenshot, again named `1.png`, still shows
**未部署** after the mount, with a relative label **18m ago** and visible
**重新部署 / 重新启动 / 文件 / 日志 / 命令** controls. No build log, deployment
commit, image or running container is shown. The relative label is not a measured
build duration, and no startup failure or successful build is inferred from it.
The backup recommendation is no longer visible; no backup creation is evidenced.

Before the next manual build action, an anonymous public GitHub ref check again
returned `92425fdc40afff43d56893b933a01398eab68765` for `main`, matching local HEAD
and the frozen baseline. Proceed with one observed **重新部署** action on this
existing service, within the previously authorized ordinary build scope. If it
opens a dialog, inspect that dialog before any further confirmation; if it starts
a build directly, capture the resulting build/status view. Do not repeat the
action or substitute **重新启动**. No job submission/result is yet observed.

Keep all runtime variables, including `EKB_RUNTIME_PROFILE`, absent during this
build inspection step. The earlier five-case local entrypoint test verifies
rejection before storage for a missing/invalid explicit Hosted profile; it is not
evidence of actual cloud execution. No production code or Dockerfile override is
changed to bypass startup. Linux permission, first-seed, `/health`, `/ready`,
restart and redeploy persistence checks remain **NOT RUN**; WP6A is **PARTIAL**.

### Server details — screenshot evidence

| Server detail | SCREENSHOT OBSERVED value / limitation |
| --- | --- |
| Status / OS | **Running / ZeaburOS / K3s**; not proof of service replica count or deployment strategy |
| Provider / region | **Alibaba Cloud / Shanghai, CN** |
| Machine size | **2 CPU cores / 4 GB memory**, as displayed; no assumed GB-to-GiB equivalence |
| Monthly server price / expiry | **US$6/month / 2026-09-29**; no renewal action taken and no three-month prepaid term inferred |
| Disk capacity / current use | **48.8 GB / 4.9 GB (10%)**, with operating system and K3s indicated; no separately allocated `/data` volume shown |
| Outbound traffic | **Unlimited**, as displayed; this is not a bandwidth-speed, fair-use or overage-contract verification |
| Current CPU / memory | **15% (300m / 2000m)** and **55% (1924 MB / 3499 MB)**; point-in-time server readings, not EKB workload measurements or guaranteed allocatable capacity |
| Server-to-Zeabur latency | **160 ms**, labelled good; not frontend-to-Agent latency or mainland application reachability |
| Alerts | **Not enabled**; no monitoring subscription or paid alert feature enabled by Codex |
| Topology cross-check | The later global project card explicitly associates EKBstaging with the full **Aliyun Shanghai 2C 4GB** server name, matching these server details and maintainer confirmation. The linked server ID is not independently extracted from that card |

The public IPv4 shown in the capture is not needed for this evidence and is not
transcribed or probed. Do not use the **SSH**, **升级**, **申请扩容**, **立即续约**
or firewall controls to compensate for an unverified deployment requirement.
The current resource readings warrant measuring container headroom later, not
changing the server reservations or purchasing an upgrade.

The existing local Git remote was matched to the public repository URL below
without outputting credentials or the raw remote. The original form instructions
were as follows. Service creation and saved source/main are now screenshot-observed;
these instructions alone do not prove all persisted settings or a deployment:

| Observed field | Prepared value |
| --- | --- |
| Git URL | `https://github.com/JZ-05T68/engineering-knowledge-base-src.git` |
| 服务名称 | `ekb-staging` |
| 分支 | `main` |
| 根目录 | `/` (repository root) |
| 认证方式 | **匿名**; no Token, SSH key or new GitHub authorization |

Current [Dockerfile documentation](https://zeabur.com/docs/en-US/deploy/methods/dockerfile)
describes automatic detection of a root Dockerfile. **The observed arbitrary-Git
path rejects that assumption**, as recorded below; this generic statement must
not be treated as support for automatic detection on this source type. The eventual
build must still show the correct Dockerfile and application commit. The generic
[server purchase documentation](https://zeabur.com/docs/en-US/server/purchase)
describes fixed monthly server billing. The Git form alone did not establish
included service/storage costs; the subsequent official-source check below
resolves the general usage-fee question without pretending the form showed prices.

### Observed redeploy rejection — explicit Dockerfile required for arbitrary Git

The maintainer supplied the following result after the instructed redeploy action:

```text
Error: Invalid input
Code: INVALID_ARGUMENT
Description: Dockerfile is required for arbitrary Git sources. Auto-detection is not supported yet.
Trace ID: f93dd975c07ec8521b41ac27b1c689af
```

Evidence class: **MAINTAINER CONFIRMED platform response**. This is a deployment
input rejection, not an observed Python startup exception, DB failure, missing AI
key error or completed image build. It establishes that this arbitrary Git source
needs explicit Dockerfile content. A fresh official-documentation search still
returned the generic auto-detection instructions, not documentation resolving the
source-type distinction; the actual response takes precedence for this workflow.

The repository already contains the required Dockerfile. Local HEAD remains
`92425fdc40afff43d56893b933a01398eab68765`; the working Dockerfile matches that
commit after LF normalization, with **33 lines** and normalized SHA-256
`aa3142de3ac4586060cb8738f825e3dec7fcd5f36f3f340f9c90f5e96caf9a45`.

Local verification for this correction: the existing packaging tests for the
Dockerfile positive inventory/non-root/health policy, Dockerignore private
exclusions, and positive-source-only build context all passed (**3 selected tests,
exit 0**, one existing Starlette test-client deprecation warning). No container
build, cloud deployment or real AI call was performed by these checks.

Correction within existing WP6A scope: copy the entire unchanged
`D:\Projects\ekb-dev\Dockerfile` into the already-observed service **设置 →
Dockerfile** editor and save it. This explicitly supplies the frozen build recipe;
it does not amend the repository Dockerfile or create a new architecture. Preserve
the Python base, positive COPY inventory, hosted requirements, `pip check`, UID/GID
10001, port 8000, `/health` healthcheck and `python -m src.hosted.server` entrypoint.
Do not replace it with a simplified recipe, add secrets/DB files, use COPY-all,
enable auto-detection overrides, connect another GitHub account or rebuild a new
service. Do not use the **从 GitHub 加载** button as an unverified alternate path.

The earlier instruction to leave the Dockerfile editor untouched was for inspection
and avoiding an accidental override; it is now superseded **only for an exact copy
of the frozen root Dockerfile**. ENTRYPOINT/CMD override fields remain empty, all
runtime variables and real AI key remain absent, replica remains 1 and `/data`
must be preserved. Save once; if that starts a build, do not trigger another one.
Otherwise make one **重新部署** attempt after a successful save. Stop at any
unexpected permission/payment prompt or changed source. The maintainer subsequently
reported success and supplied the platform-running evidence below. Exact saved
Dockerfile content, build-context handling and resulting image identity remain
**NOT VERIFIED**. No code, security control or system configuration is weakened.

### First platform-running screenshot — EKB readiness still unverified

After saying **成功**, the maintainer supplied a new `1.png` for the same service
**ekb-staging / 6a915d64cb6b9b31c9e68ad7**. The main card shows **运行中**, a green
indicator and **0m ago**. A **Deployments** entry also shows **运行中** and the
commit title **docs: record v0.6.0 pre-public deployment gate**, matching the
baseline's title. The source link is truncated; server name remains **Aliyun
Shanghai 2C 4GB**. Files, logs and command controls are visible. A **安全** tab is
also visible; its presence is not evidence of an enabled access policy or auth.

This is **SCREENSHOT OBSERVED platform running/deployment-record evidence**. It
does not expose the deployment SHA, image ID, base digest, entrypoint, logs,
listening port, `/health`, `/ready`, replica execution or the mounted filesystem.
The **0m** label is not a stability or restart test. Do not retain the old
**未部署** as the latest UI status, and do not upgrade this new status to Stage-1
PASS or public-exposure approval.

The maintained sequence has not configured runtime variables or uploaded a DB.
At the frozen source, `load_hosted_settings()` requires explicit Hosted;
`main()` calls it before storage bootstrap and server startup. Missing Hosted
must therefore fail before DB access, as the earlier local test demonstrates.
The platform's running label needs reconciliation with actual startup evidence
before preparing the cloud seed. Possible transient or platform-process status
must not be asserted as the cause without logs. Next inspect only this deployment's
small non-secret startup log excerpt; no environment dump, credentials, real key,
public domain or test Agent call is needed. All EKB runtime/storage acceptance
checks remain **NOT RUN**, overall status **PARTIAL**.

### Build completion screenshot — runtime output still pending

The subsequent deployment-details captures identify deployment
**6a9170f213d3d467215e5926** in the same project, environment and service.
The selected tab is **构建记录**, and current status is **启动中**. The branch
field displays a dash; the commit title is truncated. The visible log tail
records completed image/OCI uploads and **build completed, see runtime logs for
your application output** at displayed time **08/28 19:29:25**. The console time
zone was not independently established.

Build completion and registry upload are **SCREENSHOT OBSERVED**. These captures
do not show the recipe's build steps, full application SHA, full image/base digest
or runtime output. Short blob identifiers are not a resolved image digest. The
earlier overview's green **运行中** and later detail's **启动中** are separate
observations, not evidence of stable EKB health. Runtime and storage acceptance
remain **NOT RUN** at this point. The requested runtime-tab capture was then
supplied, as recorded below; no full log export or environment dump was requested.

### Runtime failure screenshot — selected deployment removed

The next capture selects **运行记录** for the same deployment
**6a9170f213d3d467215e5926**, now labelled **已移除**. Visible platform events
show successful image pulls, container creation/start and repeated **BackOff**
for pod `service-6a915d64cb6b9b31c9e68ad7-f6f8c8c9f-w8pt8`. The log includes
`hosted_startup_failed` at displayed **08/28 19:30:27**; further BackOff appears
at **19:30:28**, another start at **19:31:13**, and **Killing: Stopping container
ekb-staging** at **19:31:14**. These are console timestamps, not independently
verified wall-clock times. The image reference uses this deployment's tag; no
resolved image or base digest is shown.

The frozen `src/hosted/server.py::main` deliberately emits the same safe event
for multiple startup failures without raw exception text. Its first operation
requires explicit Hosted configuration before storage bootstrap. Given the
maintained sequence (runtime variables absent, synthetic seed not uploaded),
failure is consistent with that fail-closed gate. The single generic event does
**not uniquely prove** the failing setting or validate the cloud filesystem.
The existing `test_main_requires_explicit_hosted_without_storage` covers this
missing/invalid-profile behavior locally; no new test execution is claimed here.
Missing AI key alone is intended to preserve liveness after otherwise valid
configuration/storage, with `/ready=503 ai_not_configured`; do not add a real key
to resolve this startup failure or weaken logging to expose raw exceptions.

The observed platform restarts must not be mistaken for Agent retry behavior or
an effective crash-retry count of zero. A single pod name in an excerpt does not
prove replica enforcement or no overlap. The final stopping event is not proof
of graceful SQLite shutdown, Recreate or persistence. **已移除** refers to this
selected deployment; it does not establish deletion of the service or `/data`.
Current service state and the reason for removal remain unverified.

The requested Files inspection subsequently reached a directory view, followed
by a failed directory-creation attempt, as recorded below. Overall WP6A remains
**PARTIAL**, with first synthetic startup and the acceptance matrix pending.

### Files view reached; seed directory creation failed

After initially reporting Files unavailable, the maintainer supplied a Files
page screenshot showing the root directory tree and purple `data`. The legend
identifies purple entries as mounted volumes. A later expanded `data` view shows
no visible children, and its context menu contains **New File**, **New Folder**,
**Download**, **Delete**. This proves UI access and mount labelling, not a stable
container, successful directory enumeration, writable permissions or persistence.

Codex instructed creating only `/data/seed` through **New Folder**, with no DB
upload. The maintainer reports creation unsuccessful and a service that crashes
less than ten seconds after starting. Exact creation-error text, entered path and
the deployment identity for this latest restart are not available. Do not claim
the directory exists or that a volume was deleted. No synthetic artifact upload
is established. Viewing the tree did not justify assuming that writes would work;
the instruction to create a directory before stabilizing the runtime was premature.

Source reinspection confirms `main()` calls `load_hosted_settings()` before
storage or listening. With the instructed absent runtime profile, the formal
Hosted entry exits rather than falling back to Local. The same generic log can
also cover other failures. On this turn the selected existing test
`tests/test_hosted_server.py::test_main_requires_explicit_hosted_without_storage`
was rerun: **5 passed**, exit 0, one existing Starlette deprecation warning.
This confirms local missing/invalid-profile rejection before storage, not the
exact cloud exception or platform restart policy. Missing real AI key alone is
not this startup requirement; adding a key is neither needed nor authorized.

Directory creation failure may involve the short-lived container, the path or
volume permissions; no one cause has been established. Official
[File Management documentation](https://zeabur.com/docs/en-US/operations/data/file-management)
rechecked on 2026-08-28 requires a **full folder path**, `/data/seed` in this case.
The frozen image runs as UID/GID **10001:10001** and sets image-layer `/data` mode
700; that does not verify permissions of a platform volume mounted over it.
Do not use `chmod 777`, run EKB as root or alter the image to hide this issue.

Stop repeated starts and directory attempts. Obtain the exact non-secret error
and path from the failed creation without repeating the write. Before further
upload, establish a stable operator-controlled volume preparation route. A
temporary maintenance entrypoint remains a candidate only: no such override,
health-check change, listener, additional service or DB process was implemented.
A local read-only Docker availability check could not connect to
`dockerDesktopLinuxEngine`; no container test ran and no Docker/WSL/system change
was made. This is not a new cloud-build failure or a reversal of historical WP5
verification. Any maintenance route must preserve the original image and restore
the formal Hosted entrypoint and required health policy before EKB acceptance.

### Temporary maintenance instruction — marker subsequently observed

Use the **existing service and image** for a bounded standard-library maintenance
process. It does not run EKB, open SQLite, read secrets, write files or listen on a
port. Keep image UID/GID 10001, replica 1, the same volume and no public domain.

In the previously observed Settings controls, temporarily turn off **platform
health checks for maintenance only**. Set the complete **ENTRYPOINT** override
below and leave **CMD** empty. Save once; if saving already applies/restarts the
service, do not trigger a duplicate restart. Stop for missing/changed controls or
any payment prompt.

```text
python -B -u -c "import signal,sys,time; signal.signal(signal.SIGTERM,lambda *_:sys.exit(0)); print('ekb_maintenance_only',flush=True); time.sleep(1800)"
```

The observed UI describes direct execution with quotes preserving argument spaces.
Expected output is `ekb_maintenance_only`. The wait lasts 30 minutes; expiry is
not an EKB crash. A maintenance running status is **not** EKB health/readiness.
No fake HTTP endpoint or EKB security-control change is permitted.

This exact Python payload was compiled and launched locally on Windows: marker
observed, process alive after one second, then explicitly terminated/cleaned up,
no stderr. This verifies syntax and waiting behavior only, not Linux PID-1 signal
handling, Zeabur parsing/health settings or mounted-volume permissions. Local
Docker is unavailable; no local Linux/container test is claimed. The later
cloud marker evidence is recorded below, separately from this local test.

After the marker is observed, inspect runtime UID/GID and `/data` ownership/mode
before another directory-creation attempt. Do not run EKB as root, use `chmod 777`
or upload a DB yet. Keep real AI key and other runtime settings absent. Before
formal EKB startup, clear overrides to restore the frozen image entrypoint and
restore/configure platform HTTP `/health`; complete the seed, non-secret settings
and single-writer gates first. No repository/image changes or new resources are
required by this maintenance instruction. Permissions and EKB acceptance remain
pending despite the subsequently observed maintenance marker.

### Maintenance marker observed — next inspect identity and volume permissions

The latest runtime-log screenshot for deployment **6a9170f213d3d467215e5926**
shows **运行中** and `ekb_maintenance_only` at displayed **08/28 20:59:27**,
following a new container start. This establishes that the maintenance payload
executed. The earlier `hosted_startup_failed`/BackOff rows at **20:32–20:33**
precede this marker and do not establish a failure of the maintenance process.
Conversely, the deployment age **1h** is not maintenance uptime or proof that it
remained stable for an hour. Timestamp timezone remains unverified.

No EKB listener, `/health`, `/ready`, seed or persistence PASS follows from this
maintenance status. Full saved command, effective UID, health-check state and
absence of rollout overlap are not proven by the marker alone. The instructed
process has a 30-minute wait; do not interpret its expected expiry as EKB failure
or repeatedly restart/redeploy without checking the current state.

The operator was instructed to open the service overview's **命令** control, consistent with the official
[Execute Commands documentation](https://zeabur.com/docs/en-US/deploy/config/command-execution)
rechecked on 2026-08-28, and execute these read-only commands separately:

```text
id
```

```text
ls -ldn /data
```

Capture only these outputs or the exact control error. `id` reports the command
session identity, not necessarily PID 1's identity; compare with the formal
non-root requirement before drawing conclusions. `ls -ldn` reports the directory
itself with numeric ownership and permission bits, without listing contents or
opening a DB. Do not create directories, upload, change ownership/mode or inspect
environment values yet. No cloud command execution is claimed until results are
supplied. Maintenance marker observed; filesystem and Stage-1 gates still pending.

### Service terminal connection failed — metadata via maintenance stdout pending

The next screenshot shows Zeabur's `/omni/terminal` with **Service** selected and
the same `ekb-staging` service ID. Three **Connection failed. Press Enter to
reconnect...** notices and intervening new-session messages are visible. There
is no shell prompt or output from `id` / `ls -ldn /data`; neither command is
verified as executed. The screenshot does not establish whether the maintenance
process expired, whether a shell is unavailable or whether the platform terminal
transport failed. Do not keep reconnecting, switch to Server/SSH, request
credentials, modify login shells or increase privileges based on this evidence.

Use the existing, already-observed maintenance ENTRYPOINT path to emit only
allowlisted filesystem/identity metadata into runtime logs. Replace the previous
maintenance command with this complete command; CMD stays empty, the original
Dockerfile/image/user/volume stay unchanged, and platform health remains in its
temporary maintenance state until restored before formal EKB startup:

```text
python -B -u -c "import os,stat,signal,sys,time; signal.signal(signal.SIGTERM,lambda *_:sys.exit(0)); s=os.lstat('/data'); print('ekb_maintenance_only',flush=True); print('uid=',os.getuid(),'gid=',os.getgid(),'groups=',os.getgroups(),'data_owner=',s.st_uid,'data_group=',s.st_gid,'data_mode=',stat.filemode(s.st_mode),'data_mount=',os.path.ismount('/data'),'data_write_search=',os.access('/data',os.W_OK|os.X_OK),flush=True); time.sleep(1800)"
```

This payload reads `/data` metadata only, not its contents or any database,
environment value, secret, token or browser state. It does not change ownership,
permissions, files or mounts, and opens no listener. The UID/GID now describe the
maintenance process, rather than an unverified terminal session. Access checks
are observations, not a successful write probe or persistent-volume guarantee.
`lstat` mode also distinguishes a symlink from a directory; do not follow an
unexpected link for a later write. Syntax compilation passed on local Windows;
no local Linux or actual cloud execution of this revised payload is claimed.

Save once and inspect only the new runtime marker and metadata/error. If saving
already restarts the service, do not trigger another restart. If the observed
control no longer works, stop and report its exact message rather than seeking
another access channel. Upload, directory creation and EKB startup remain gated
on this evidence. The 30-minute maintenance limit and mandatory restoration of
the original entrypoint plus platform HTTP `/health` still apply.

### Copied command reveals conflicting quote delimiters

The maintainer supplied the current ENTRYPOINT as text and separately confirmed
**CMD is empty**. The supplied text surrounds the Python `-c` payload with single
quotes while also using unescaped single quotes inside it (`'/data'`, labels).
With POSIX-style argument parsing, this destroys the intended Python string
literals. Local `shlex.split` plus `compile` reproduced a SyntaxError for that
quote pattern. This is a concrete command-format defect to correct, not proof of
Zeabur's exact parser or a captured cloud Python exception. Markdown escapes in
the pasted message do not establish literal backslashes in the actual UI field.

Correction: replace the entire ENTRYPOINT with the diagnostic command using
**outer double quotes, inner single quotes**, and keep CMD empty. The replacement
uses `lambda signum,frame:sys.exit(0)` to avoid a starred argument in copied text.
Local validation confirmed exactly five arguments (`python`, `-B`, `-u`, `-c`,
one intact payload) and successful Python compilation. Cloud execution and UID/
volume results remain pending. Do not weaken validation or change permissions
based on the quote finding. Save once and inspect only fresh marker/metadata
after that application; do not repeatedly restart or use earlier marker output
as evidence that the corrected diagnostic command ran.

### Cloud metadata observed — root identity conflicts with frozen non-root policy

The latest screenshot supplies metadata at both **08/28 21:09:20** and
**21:14:53** for the same deployment. Both visible lines report:

```text
uid=0 gid=0 groups=[0]
data_owner=0 data_group=0 data_mode=drwxrwxrwx
data_mount=True data_write_search=True
```

The latest container starts at **21:14:52**, followed by the marker and metadata
at **21:14:53**. These are screenshot-observed runtime results from the maintenance
Python process: real UID/GID are 0, `/data` is a
directory with mode **0777**, numeric owner/group **0:0**, and is detected as a
mount point. More precisely `getuid/getgid` report real UID/GID; separate effective
ID/capability inspection has not been performed. Access reporting True under
root is not proof that UID/GID 10001 can safely use a correctly protected volume.
No actual write probe, seed validation, persistence or Recreate PASS is implied.

The frozen root Dockerfile was reread locally and still contains
`USER 10001:10001` and image-layer `/data` ownership 10001:10001/mode 700.
**The observed cloud maintenance identity does not meet the non-root gate.**
Do not upload data, configure secrets, restore the EKB application entrypoint or
approve Stage-1 acceptance until the discrepancy is resolved. The mount overlays
image-layer directory metadata; it is not yet known whether the saved cloud
recipe/image differs or a platform runtime-identity override is active. No
ownership/mode change, root EKB workaround or `chmod 777` was performed by Codex.

The newly visible **21:09:20** diagnostic line also predates the last quote
correction. Thus earlier absence of that line in a screenshot was not proof that
the diagnostic command failed to execute. POSIX-style quote validation identified
a portability concern, but it must **not** be presented as the established cloud
failure cause. Do not keep changing command quoting now that output is observed.

Official [Template Format documentation](https://zeabur.com/docs/en-US/template/template-format)
rechecked on 2026-08-28 documents `runAsUserID` for PREBUILT services. This is a
possible platform configuration mechanism, not evidence of its current setting
or availability on this existing arbitrary-Git service. A forum report/staff
response was found, but another service's behavior is not proof of this target's
configuration. No new template/service/resource, YAML deployment, privilege
change or undocumented API mutation is authorized as a workaround.

The maintainer subsequently supplied the final recipe section, as recorded below.
The exact built image and any platform user override remain unresolved. Keep
maintenance bounded, no additional restart or uploads, and preserve the existing
volume. Overall **PARTIAL**; non-root and volume-security gates remain unmet,
while WP6B/public exposure stay prohibited.

### Saved recipe excerpt confirmed; Security tab is an AI report feature

The maintainer's pasted cloud Dockerfile tail contains **USER 10001:10001**,
EXPOSE 8000, the expected health-check structure and the formal
`python -m src.hosted.server` entrypoint. This is maintainer-supplied recipe text,
not proof of the built image's Config.User or absence of a Pod-level override.
Rich-text URL/underscore escaping in the message is not evidence that the UI
recipe literally contains those formatting artifacts; do not rewrite it on that
basis. CMD was separately confirmed empty.

The later supplied `QQ20260828-212405.png` shows the same service's **安全** tab.
Its content is **安全洞察**, an explanation that Zeabur Agent analyses the GitHub
project, a **产生报告** button and **暂无报告**. It exposes no UID/GID or container
security-context control. The earlier suggestion to inspect it did not locate
the required setting. Do not generate a report: this is outside the current
diagnostic need, not authorization for an AI analysis or paid add-on. No report
was generated by Codex and no such execution was evidenced by the screenshot.

Official documentation rechecked on 2026-08-28 still documents `runAsUserID` in
the PREBUILT/template context. It does not establish a supported UI mutation for
this already-created arbitrary-Git/Dockerfile service. Do not infer that the
platform necessarily forces root, that the feature is unavailable everywhere,
or that a newly deployed template is the approved solution. The actual image
identity and runtime override need authoritative platform evidence.

The next action at this point was an operator-submitted platform support request,
without more guessed settings or privileged/system changes. Official
[Get Help documentation](https://zeabur.com/docs/en-US/get-started/faq-support/help)
links [Zeabur Support](https://zeabur.com/support), asks for relevant resource IDs
and permits ordinary requests without Express priority passes. The support page
itself was robots-denied to the web reader; at that point no ticket form or
submission had been observed. Later maintainer screenshots establish the manual
submission recorded below. Do not buy priority support, change a plan or perform identity
verification. If any of those is required, stop for maintainer approval.

Earlier support draft (not submitted by Codex; not a verbatim submitted-ticket transcript):

> Existing Aliyun Shanghai ZeaburOS/K3s server; project EKBstaging; service
> ekb-staging, created from an arbitrary Git URL with an explicit root Dockerfile.
> Project ID: 6a911d16b7ff62ee8d7fc5f1. Service ID: 6a915d64cb6b9b31c9e68ad7.
> Deployment ID: 6a9170f213d3d467215e5926.
> The configured Dockerfile contains USER 10001:10001, but a temporary Python
> maintenance entrypoint reports uid=0, gid=0, groups=[0]. The mounted /data
> reports owner/group 0:0, mode 0777, mount=True. The application is not running;
> no AI key, database upload or public domain has been configured in this sequence.
> Service-terminal connection attempts fail. The Security tab is only an AI report.
> Please explain how to verify the actual image Config.User and effective Pod
> securityContext, and how to persist UID/GID 10001:10001 for this EXISTING Git
> service across restart/redeploy. How should the existing /data ownership and
> restrictive permissions be provisioned and preserved for that user? Is a
> read-only container root with writable /data and temporary storage supported?
> Please provide supported configuration instructions only. Do not create/delete
> services, volumes or servers, upgrade plans, incur charges or request credentials.
> Do not return environment values, tokens, kubeconfig or any secret-bearing dump.

Formal EKB startup, corpus upload and Stage-1 acceptance remain on hold pending
the non-root/volume resolution; other WP6A checks remain pending. This is a
specific platform-configuration gate, not a new architecture decision or WP6B.

### Standard support ticket submitted by maintainer — 2026-08-28

The later maintainer screenshots (reusing `QQ20260828-212405.png`) first show the
support form, then the created ticket page. The created page is **SCREENSHOT
OBSERVED**, not a Codex browser submission:

- Ticket: [6a91903f19d208af60b18288](https://zeabur.cn/support/ticket/6a91903f19d208af60b18288).
- Title: **Dockerfile 设置 USER 10001:10001，但 Git 服务实际运行 UID/GID 为 0**.
- Tags: **Services / Bug / Standard**; the Bug category is a reported issue,
  not a confirmed diagnosis or platform acknowledgment.
- Project/service: **EKBstaging / ekb-staging**; author **杨洋**, displayed **Just now**.
  No exact submission timestamp was independently measured.
- Visible body includes the expected service/deployment IDs, configured USER and
  formal ENTRYPOINT, temporary maintenance override with empty CMD, UID/GID 0
  and `/data` metadata, and the terminal connection failure. The lower body is
  outside the capture; the entire saved ticket is not independently verified.
- **Standard** is visibly selected. No priority purchase, plan change or new
  resource action was performed by Codex; no billing transaction was inspected.

No platform reply or fix is visible. Await the response to this existing ticket;
do not create duplicates or repeatedly restart the service. Evaluate any proposed
solution against the existing-service, no-extra-charge and security constraints
before applying it. The diagnostic maintenance process is not EKB, and its
30-minute sleep expiry must not be treated as a formal application crash.
Do not upload a DB, configure an AI key or expose the Agent while the non-root
gate is unmet. No cloud settings, runtime tests or production files changed in
this ticket-recording step. WP6A remains **PARTIAL**, WP6B **NOT READY**.

### Initial service creation decision — before any successful cloud startup

Official sources rechecked on **2026-08-28**:

- [Server documentation](https://zeabur.com/docs/en-US/server) states that server
  billing is fixed monthly rather than resource-based, and deploying services on
  a server does not require a subscription plan.
- In an [official staff billing clarification](https://zeabur.com/forum/posts/6a196140318cd98c565e6ca9),
  Zeabur staff state that dedicated-server memory, bandwidth and storage usage do
  not incur additional usage fees. An existing optional platform subscription is
  billed separately; this does not authorize starting or changing one. The staff
  reply was available in the search result; a direct open was robots-denied.
- [Build Spec documentation](https://zeabur.com/docs/en-US/deploy/config/build-spec)
  lists **2 vCPU / 4 GB** build CI for the Free plan. Builds occur on Zeabur's
  shared CI and publish an image to its registry, separately from the purchased
  runtime server. This does not prove the account's current quota or its actual
  build result; do not select a paid build upgrade if the included path is blocked.

Together with the purchased-server details, the EKBstaging project card and the
maintainer's existing conditional authorization, this supports **one ordinary Git
service creation/build attempt in the existing project**, with the existing
server and included build allowance. It does not authorize a new server, disk
expansion, paid volume/add-on, subscription/trial, paid build upgrade or any charge.
Stop at any payment, upgrade, additional permission or different-target prompt.
The actual volume dialog is now screenshot-observed; provisioned disk backing
must still be checked before seeding. Generic billing documentation is not a
successful volume or persistence test.

No runtime variables, seed, volume or domain should be added during this initial
creation/build step. The root Dockerfile contains no configured EKB runtime
profile or DB. `load_hosted_settings()` requires explicit Hosted before storage;
`main()` invokes it before bootstrap or opening a listener. The existing test
`test_main_requires_explicit_hosted_without_storage` was rerun locally: **5 passed**
(one existing Starlette test-client deprecation warning). It covers missing,
empty, Local, case-variant and unknown profiles returning exit 1 without storage.
This is a local fail-closed check, not cloud runtime evidence. A configuration-less
deployment can therefore build but is expected to fail startup; examine actual
status before attributing a failure or retrying. Do not change the entrypoint or
weaken validation to make it green. No successful Stage-1 startup is claimed until
synthetic data, persistent `/data`, replica=1 and deployment strategy are verified.

An anonymous read of the public GitHub `refs/heads/main` API returned
`92425fdc40afff43d56893b933a01398eab68765`, matching the frozen baseline. No GitHub
credential or token was used. The cloud deployment must still record its resolved
commit; the branch may change after this check.

The captures remain external user attachments; they are not copied into the
repository. No secrets were requested or inspected. Overall status remains
**PARTIAL**, with cloud runtime and storage checks **NOT RUN**.

### Current Chrome status after maintainer enabled the extension

The maintainer reported the Chrome extension enabled. A fresh connection succeeded:
the supported Chrome selector returned an extension browser, the existing Zeabur
project tab was listed, and a subsequent controlled-tab listing confirmed that
the correct project tab had been claimed. **The previous missing-extension blocker
is resolved; do not ask the maintainer to install it again.**

The required page controls remain inaccessible: `playwright.domSnapshot()` timed
out after 20 seconds, and the documented alternative `dom_cua.get_visible_dom()`
timed out after 15 seconds. Tab metadata remains readable, but it cannot verify
server association, service options, replica count or volume charges. Later
supplied screenshots establish the displayed project association and actual server
details described above, but not effective service configuration. Following
the explicit maintainer instruction, Codex stopped rather than guessing clicks.
No service, volume, variable, domain or deployment was created or modified through
the unreliable browser-control connection. The later maintainer-operated service
creation is separately evidenced by the service screenshot above.

**Manual next steps — inspect the created service, runtime gates still pending:**

1. Maintenance metadata is now observed: UID/GID 0 and mounted `/data` mode 0777.
   The non-root gate is unmet. Preserve
   the image, mounted volume and Free build tier; do not rewrite the recipe,
   remount/delete the volume or enable backups.
2. Recipe USER 10001:10001 is maintainer-confirmed; the Security tab is an AI report,
   not runtime configuration. Do not repeat terminal attempts, generate a report
   or change quoting/permissions. The maintainer has submitted Standard ticket
   **6a91903f19d208af60b18288**; await its response, then validate the supported
   existing-service solution before writes or EKB startup. Do not duplicate the
   ticket. No ticket has been submitted by Codex.
3. Leave AI keys and runtime variables absent. Do not bind/generate a domain,
   enable public access, upload a DB or create any additional volume yet. Do not paste
   environment dumps, credentials, authorization headers or unreviewed logs.
4. Stop at any paid feature, upgrade, subscription/trial, new authorization or
   different-target prompt. Effective replica=1, Recreate and the `/data` workflow
   remain gates before the first synthetic DB startup. Later inspect build steps
   and exact deployment identity; do not weaken validation to obtain a green status.

The service-menu path was rechecked against current official documentation on
2026-08-28: [Create Service](https://zeabur.com/docs/en-US/deploy/create/create-service).
Instructions alone are not execution evidence: service creation is now established
by the maintainer's later screenshot; build completion is now screenshot-observed,
while the subsequent runtime capture establishes a failed start, not health. The
frozen first-seed and pre-exposure gates remain in force.

### Chrome authorization and earlier connection diagnosis

The maintainer explicitly authorized the already-authenticated **Chrome** session
for **EKBstaging / v0.6.0 staging / WP6A cloud validation**. Inspection, one staging
service from the existing GitHub repository/root Dockerfile, synthetic-only data,
non-secret settings and the minimum `/data` volume are allowed **only if covered
by the purchased server without a new charge**. This does not authorize a new
server, paid volume/add-on, upgrade, billing change, HA/autoscaling charge, identity
or ICP action, domain purchase, deletion of persistent data, or public Agent access.

Stage 1 keeps the real AI key absent, replica 1, persistent `/data`, CORS empty and
trusted proxies empty unless authoritative stable CIDRs are verified. No secret
value, password, cookie/session token or Authorization header may be inspected,
copied, displayed, logged or captured in a screenshot. Secret UI inspection is
limited to whether a server-side mechanism exists. Only after storage, restart,
redeploy and single-replica checks pass may the maintainer be asked about a later
secret-enabled stage. Actual corpus approval remains **PENDING**; WP6B is not allowed.

Before the extension was enabled, the authorized Chrome connection was attempted with the Chrome skill's supported
browser selector. Result: **`Browser is not available: chrome`**. The documented
read-only diagnostics then established:

| Diagnostic | Observed result |
| --- | --- |
| `chrome-is-running.js --browser chrome --check` | Chrome running: YES |
| `installed-browsers.js --json` | Google Chrome installed |
| `check-extension-installed.js --browser chrome --json` | Selected Chrome profile: `installed=false`, `enabled=false`; no installed extension version detected |
| `check-native-host-manifest.js --browser chrome --json` | Native-host manifest/registration: correct |
| Browser availability | No controllable Chrome connection advertised |

That earlier result diagnosed the missing usable **ChatGPT Chrome extension connection**, not a
Zeabur password or server problem. No Chrome page/control was read, no alternate
browser was substituted, and no extension, native-host or system configuration was
installed or repaired by Codex. No cloud mutation was submitted. Per maintainer
instruction, stop here instead of guessing service/volume controls.

**Historical maintainer connection steps — now resolved** (from the Chrome skill
and its packaged extension metadata; do not repeat installation now):

1. In Codex, open **Settings → Computer use** and use the Chrome extension setup
   entry. The packaged official listing is
   [ChatGPT for Chrome](https://chromewebstore.google.com/detail/chatgpt/hehggadaopoacecdllhhajmbjkdcmajg).
2. In the same Chrome profile used for Zeabur, open that listing, choose
   **Add to Chrome / 添加至 Chrome**, review the permission prompt and confirm
   **Add extension / 添加扩展程序** if acceptable. Codex has not clicked these controls.
3. Open `chrome://extensions` and confirm **ChatGPT** is installed and enabled.
   Do not bypass an organization/browser restriction if installation is blocked.
4. Keep the existing authenticated **EKBstaging** project tab open in that profile,
   then tell Codex the extension is enabled. No Zeabur password or API key is needed.
5. Resume with read-only server/project association and replica/volume checks.
   Do not manually create a paid volume, reveal secrets or start the Agent as a
   workaround for the browser connection problem.

## Candidate decision

| Candidate | Decision | Reason / qualification |
| --- | --- | --- |
| Zeabur-managed Aliyun (Alibaba Cloud), Shanghai | Selected and purchased by maintainer; deployment topology not yet admitted | Server screenshot corroborates provider/region, 2 cores / 4 GB, ZeaburOS/K3s, USD 6/month, 48.8 GB disk and unlimited outbound traffic. Bandwidth rate, volume entitlement, renewal terms and exact service/topology evidence remain pending. |
| Zeabur-managed Tencent Cloud server in mainland China | Acceptable fallback | Maintainer reports higher/multiple mainland 2C/4GB public SKU price points. Same live topology, storage and checkout validation required. |
| Zeabur-managed Volcengine mainland server | Lower priority | Managed mainland availability is documented, but pre-ICP support is still marked pending in the current compatibility table. |
| Zeabur-managed Hong Kong server | Fallback, provider/SKU pending | Consider if mainland availability or identity/domain requirements are unsuitable. Mainland reachability and latency require actual mainland-network tests. |
| EdgeOne Cloud Functions / Makers | NOT preferred; do not use for frozen backend | Per-request serverless execution and elastic scaling do not establish one persistent Uvicorn process sharing one durable local SQLite volume. Keep only as a possible frontend platform. |

EdgeOne's current documentation **does support Python/FastAPI**; rejecting it as
"Node-only" would be incorrect. It documents Python 3.10, a default 30-second and
maximum 120-second request duration, and recommends object storage for retained
data. No durable local SQLite mount contract was established. Its Cloud Functions
execution model is stateless and elastic. These are incompatible with the frozen
container topology without architecture changes, which are out of scope.
Sources: [Cloud Functions](https://pages.edgeone.ai/document/cloud-functions),
[Makers execution-model comparison](https://edgeone.ai/document/211789991131570176).

## Live official capability review

All sources below were accessed on **2026-08-28**. Console locations are documented
locations, not claims that these controls were successfully inspected in an account.

| Capability | Official evidence and control location | Actual service evidence |
| --- | --- | --- |
| Existing root Dockerfile | Generic [Dockerfile documentation](https://zeabur.com/docs/en-US/deploy/methods/dockerfile) describes auto-detection, but actual arbitrary-Git request requires explicit Dockerfile content. Keep frozen root recipe, `EXPOSE 8000` and no `ZBPACK_IGNORE_DOCKERFILE` | After **INVALID_ARGUMENT**, exact-content UI correction instructed; subsequent build-log tail confirms build/upload completion; earlier runtime capture shows **已移除**, BackOff and `hosted_startup_failed`; latest capture shows **运行中** with `ekb_maintenance_only`. Exact saved recipe, build steps and image identity NOT VERIFIED |
| GitHub integration | Link repository via service creation; pushes trigger deployment. [GitHub integration](https://zeabur.com/docs/en-US/deploy/methods/github-integration) | No integration changed; repository permissions not inspected |
| Persistent `/data` | Service → Volumes → Mount Volumes; persistent directory survives restart. Deleting/unmounting a volume destroys its data; never use that action for a restart test. [Volumes](https://zeabur.com/docs/en-US/operations/data/volumes) | Latest volume list shows **data → /data**, usage **未知 / 无限**. Control-plane registration observed; actual filesystem backing, permissions and persistence NOT VERIFIED |
| Recreate with volume | Old deployment stops before the new deployment starts; brief downtime. [Health checks](https://zeabur.com/docs/en-US/operations/monitoring/health-checks) | No observed stop/start timeline or service strategy |
| Replica / HA | HA documentation describes manual service copies, not automatic horizontal scaling. This is not proof of an enforced replica count. [High availability](https://zeabur.com/docs/en-US/deploy/networking/high-availability) | Settings screenshot shows numeric replica **1** in a greyed field with an upgrade-plan button. Effective instance count, no autoscaling and no rollout overlap NOT VERIFIED |
| Server environment | Service Variables control documented. Raw editing is described using dotenv; avoid raw/file upload for secrets. [Environment variables](https://zeabur.com/docs/en-US/deploy/config/environment-variables) | Runtime-only secret injection NOT VERIFIED; see security gate below |
| Public domain | Networking → generated domain or custom domain. [Public networking](https://zeabur.com/docs/en-US/deploy/networking/public-networking) | No domain generated |
| TLS | Automatic SSL provisioning documented for service-bound purchased domains; official staff also confirm automatic subdomain certificate issuance. [Domain provisioning](https://zeabur.com/changelogs/domain-registration), [subdomain support incident](https://zeabur.com/forum/posts/6a1d2566318cd98c5665ae67) | Actual hostname, chain, expiry, renewal and HTTPS reachability PENDING |
| Binary seed upload | Overview → Files; single-file upload supported; non-volume files are ephemeral. [File management](https://zeabur.com/docs/en-US/operations/data/file-management) | Directory UI and purple data mount label observed; `/data/seed` creation attempt reported unsuccessful during short-lived startup. Exact error, permissions and stable upload path NOT VERIFIED; no DB upload established |
| Health check | Settings → Health Check supports a custom HTTP path. Use `/health`, not `/ready`, for the platform's process-health check. | Screenshot shows enabled **TCP** probe with no visible target-port selection; HTTP `/health` configuration and actual result NOT VERIFIED. Independent readiness control NOT VERIFIED |
| Mainland providers / regions | Purchase page supports China/provider filters; actual city/SKU shown during selection. [Server purchase](https://zeabur.com/docs/en-US/server/purchase) | Server screenshot confirms ID 6a911c45af37eeef8fb27a27, Alibaba Cloud / Shanghai, CN and 2 cores / 4 GB. Earlier project screenshot has matching server label; exact project membership cross-check via server 项目 tab PENDING |
| Pre-ICP | Zeabur-purchased mainland server, mainland real-name verification, supported provider required; Tencent/Alibaba supported, Volcengine pending. Temporary testing use, not assumed approval for a permanent competition/commercial service. [Pre-ICP subdomains](https://zeabur.com/docs/en-US/networking/pre-icp-subdomain) | Identity / intended-use eligibility / final domain PENDING maintainer |
| Volume backup | Dev plan or above; generic application backup requires suspension, includes mounted volumes, documented retention seven days. Do not infer automatic online SQLite backup from database-specific features. [Backup and restore](https://zeabur.com/docs/en-US/operations/data/backup-restore) | Not enabled, no backup/upload/subscription performed |

### Earlier console access limitation

The `browser:control-in-app-browser` workflow reached a tab titled
`My Servers - Zeabur` at `https://zeabur.com/servers`. Navigation/DOM retrieval
timed out; recovery and another DOM attempt also failed. Only the tab title/URL
were observed. Login state, owned servers, billable resources, service controls
and prices were **not** established. No credentials were requested, extracted or
submitted; no purchase or account action was performed. A title is not account
or deployment evidence. Maintainer-provided non-secret console evidence, or a
working console session, is needed to continue actual validation. The newer
maintainer purchase confirmation and project URL observation above supersede the
earlier lack of server/project information, but not the interaction limitation.

## Cost gate and resource sizing

**Use only the server already purchased by the maintainer. No additional purchase
is authorized.** Stop if any next step requires another paid resource, plan or
identity submission. The historical pricing input below explains the planning
decision; the latest cloud update records the completed maintainer purchase.

### Historical maintainer pricing input — 2026-08-28, before purchase

The maintainer reports reviewing current official Zeabur public pricing and selects
**Aliyun, mainland China, 2 vCPU / 4 GiB, USD 6/month** as the preferred candidate.
This supersedes the earlier Tencent-first / 2 GiB planning estimate. The public
price is **maintainer-provided evidence**, not a checkout quote independently
retrieved by Codex. The console-access limitation above remains unresolved.

Using the maintainer's accounting rate of **1 USD = 6.8 RMB** (not a live FX quote):

| Planning duration | Server-only USD estimate | RMB accounting equivalent |
| --- | --- | --- |
| 1 month | 6 | 40.8 |
| 2 months | 12 | 81.6 |
| **3 months — preferred initial duration** | **18** | **122.4** |

At that stage, maximum expected initial **server-only** spend was **approximately
RMB 123**, subject to checkout confirmation and explicit maintainer approval.
Three months was the preferred planning/purchase duration. The newer update confirms
a USD 20 maintainer deposit and completed purchase at USD 6/month, **not** a verified
three-month prepaid order. Billing cadence, paid term and renewal details still need
recording. Codex executed no order, subscription, renewal, top-up or payment.

The maintainer prefers Aliyun because of the reported public price, managed mainland
availability and supported pre-ICP subdomain. Tencent remains an acceptable fallback;
its reported mainland 2C/4GB table has higher/multiple price points. Volcengine is
not preferred despite similar reported pricing because pre-ICP support is pending.
These are recorded maintainer inputs, not a new completed live SKU comparison.

| Approval field | Current confirmed state / missing input |
| --- | --- |
| Provider | **Aliyun (Alibaba Cloud)**, purchased through Zeabur by maintainer |
| Region / server ID | **Shanghai, CN / 6a911c45af37eeef8fb27a27**, SCREENSHOT OBSERVED; no separate provider SKU or region code inferred |
| CPU | Maintainer reports **2 vCPU**; screenshot shows **2 CPU cores** |
| RAM | Maintainer reports **4 GiB**; screenshot shows **4 GB**; preserve distinct reported units |
| Operating environment | **ZeaburOS / K3s**, SCREENSHOT OBSERVED |
| Storage | **48.8 GB**, screenshot observed, with **4.9 GB / 10%** used; earlier engineering floor remains **20 GiB server disk** plus **1 GiB `/data` allocation** for a small demo. No actual `/data` allocation or volume entitlement proved |
| Egress / bandwidth | Outbound traffic **unlimited**, SCREENSHOT OBSERVED; bandwidth speed, fair-use and overage terms still **PENDING** |
| Server monthly cost | **USD 6 / RMB 40.8**, server rate corroborated by screenshot; accounting conversion is not a live FX quote |
| Preferred initial duration / server spend | Earlier **3-month** planning horizon remains USD 18 / RMB 122.4; actual prepaid term/renewal commitment NOT VERIFIED |
| Account funding | **USD 20**, deposited by maintainer; remaining balance NOT VERIFIED |
| Payment / purchase | **COMPLETED BY MAINTAINER**; Codex payment automation **NONE** |
| Renewal terms | Screenshot shows monthly price and expiry **2026-09-29**; actual auto-renewal setting, renewal price, paid-term details and cancellation terms **PENDING**; no renewal submitted |
| Volume cost | Official dedicated-server usage-fee policy has no extra storage usage charge. Standard ID/path mount was submitted by maintainer and **data → /data** is listed; no payment or expansion result shown. Verify provisioned backing before seed; paid volume, resize and expansion remain forbidden |
| Subscription | Public pricing lists Free $0 and Dev $5/month; neither means a free managed server. No trial or subscription activated. |
| Backup cost | Optional Dev-plan requirement; confirm exact managed-server eligibility/charges before enabling |
| Domain / ICP implications | Mainland pre-ICP eligibility and real-name procedure require maintainer action; domain cost/renewal and final filing requirements PENDING |
| Total monthly estimate | Server component estimated above; **all-in total UNKNOWN** until storage + subscription + backup + egress + domain + applicable tax/payment conversion charges are confirmed; AI usage separate |
| Additional purchase approval | **NO**; do not buy any additional resource |

Sizing now uses the **maintainer-confirmed purchased server**, corroborated by the
server-details screenshot. Its 55% memory usage is a server snapshot before the
EKB deployment, not an application minimum, EKB peak or direct Codex measurement.
The prior WP5/Gate Linux-container evidence validates the small
Hosted dependency set (no OCR, ONNX, Streamlit or PyMuPDF), but records no usable
RSS/peak-memory measurement. The fresh Docker image query could not connect to
`dockerDesktopLinuxEngine` (named pipe absent). Docker/WSL/system configuration
was not changed, and no fresh container resource claim is made.

Zeabur documents new-server reservations of 500m CPU / 512Mi memory **for each**
of Kubernetes and the system. For the revised 2 vCPU / 4 GiB candidate, subtracting
those documented reservations leaves roughly 1 vCPU / 3 GiB before other workload
overhead; this is planning arithmetic, not measured allocatable capacity. Do not shrink those reservations
to force deployment. Confirm allocatable resources, cold-start/first-search RSS,
two-concurrent-request peak and disk/image headroom before admission. If measured
requirements exceed this purchased server, stop and report the incompatibility;
no upgrade or additional resource is authorized.
Sources: [Server resource reservation](https://zeabur.com/docs/en-US/server/operate),
[current pricing](https://zeabur.com/pricing).

Post-purchase evidence still needed; do not place another order:

- [x] Provider/city/compute/memory: Aliyun Shanghai 2 vCPU / 4 GiB, maintainer confirmed.
- [x] Project screenshot shows EKBstaging associated with the displayed server label Aliyun Shanghai 2C 4...; no services are shown.
- [x] Server ID 6a911c45af37eeef8fb27a27, provider Alibaba Cloud, Shanghai, CN and displayed 2-core / 4-GB machine size corroborated by server screenshot; exact provider SKU code not shown.
- [x] Global project card explicitly associates EKBstaging with full server name Aliyun Shanghai 2C 4GB and shows no services.
- [ ] Exact linked server ID independently captured from project configuration/link; display-name match and maintainer confirmation are recorded, not relabelled as ID extraction.
- [x] Server disk size 48.8 GB and outbound traffic marked unlimited, SCREENSHOT OBSERVED.
- [x] General dedicated-server usage-fee policy checked in official documentation/staff reply; no extra memory, bandwidth or storage usage charge, no required new subscription for ordinary service deployment.
- [x] Service settings screenshot confirms exact expected Git repository, main branch and selected Free / 2-vCPU / 4-GB build tier; no upgrade submitted.
- [x] Standard ID/path mount workflow and subsequent **data → /data** volume-list result observed; no paid expansion or backup activation evidenced.
- [ ] Provisioned filesystem backing/permissions, account-specific build quota if applicable, bandwidth rate and any fair-use terms checked; no paid expansion authorized.
- [x] Server monthly rate USD 6 and maintainer deposit USD 20, maintainer confirmed.
- [ ] Actual paid term, renewal terms/cadence and any additional recurring charges.
- [x] Purchase completed by maintainer; Codex made no payment.

Project use is explicitly **v0.6.0 staging / WP6A cloud validation**. Identity/domain
eligibility remains pending. The deposit and earlier approximately RMB 123 budget
must never be treated as permission to spend more. Use only EKBstaging on the existing
server; confirm `/data` can use its existing storage without buying another resource.

## Frozen deployment topology and acceptance evidence

Proposed topology: one Zeabur service, **one active replica**, one Uvicorn worker,
existing Dockerfile/entrypoint, writable persistent `/data` outside `/app`, no
separate DB service, no HA copies, no autoscaling, no added gateway service.
Runtime UID/GID remains `10001:10001`. Verify volume ownership/writability after
mount: Dockerfile ownership of an unmounted `/data` is not proof of volume ownership.

Before starting the database, record an authoritative effective configuration or
platform support confirmation for this exact service: replica count 1, HA disabled,
and **Recreate on restart and redeploy with the attached volume**. A single `ps`
snapshot, one service card, or an application worker setting is insufficient.
Capture old-instance termination and new-instance start events with instance IDs
and timestamps, plus volume identity, during both restart and same-commit redeploy.
Old instance termination must precede any new instance mounting/opening SQLite.
If this cannot be guaranteed, reject the target; do not test overlapping writers.

Current strategy: **DOCUMENTED RECREATE; actual service UNKNOWN**.
Concurrent overlap: **UNKNOWN**. Replica target: **1; actual NOT VERIFIED**.

Check platform support for read-only container root, a writable temporary area and
the persistent `/data` exception, preserving WP5's tested permission posture. No
platform control for this was verified. Do not claim that a non-root Dockerfile
alone makes the root filesystem read-only. Record any unsupported control for
maintainer resolution before admission; do not edit production code to compensate.

GitHub `main` is the intended source at the exact baseline. Pushes to the linked
branch normally trigger redeploy; record the actual auto-deploy/watch-path setting
and coordinate a maintenance window before later pushes. Volume Recreate implies
brief downtime. Use Dashboard **Redeploy** for same-commit checks; no empty commit.
Source: [Deployment triggers](https://zeabur.com/docs/en-US/deploy).

## Artifact delivery and executed local compatibility proof

Preferred delivery remains a separate read-only `/demo/demo.db` mount. If unavailable,
evaluate the explicitly authorized operator-only equivalent:

`/data/seed/demo.db` → frozen bootstrap validation → `/data/database/knowledge.db`.

There is **no code-level requirement that the seed be outside DATA_ROOT**. The
existing validator rejects Local private roots under `PROJECT_ROOT`, links,
non-regular files and sidecars, and checks SHA, UUID, exact-v12 structure,
integrity/foreign keys, sanitation and empty historical AI tables. Inside the
container, `/data` is outside `PROJECT_ROOT=/app`. No validation was weakened.

An executed **Windows host-only** probe on 2026-08-28 reused the existing
`tests/test_hosted_storage.py::demo` synthetic fixture and frozen production
composition. It cleared inherited EKB/DashScope variables without reading their
values, disabled dotenv, guarded private/repository DB access and repository writes,
and used the existing offline fixture to block application network calls.
Only generated test data lived in a unique OS temporary directory.

| Local probe assertion | Result |
| --- | --- |
| Seed at `<temporary DATA_ROOT>/seed/demo.db` | PASS |
| `validate_seed_artifact`, SHA unchanged, exact UUID, exact schema 12 | PASS |
| Bootstrap creates separate runtime DB; journal WAL | PASS |
| Frozen composition with absent real key and finite budgets | PASS |
| In-process HTTP `/health` | 200 |
| In-process HTTP `/ready` | 503, only `ai_not_configured` |
| `ai_calls` / `ai_outputs` rows | 0 / 0 |
| Close storage, remove only this test's seed, restart without artifact settings | PASS |
| Same runtime DB file identity and UUID reused, storage readiness valid | PASS |
| Real completion / embedding / rerank | 0 / 0 / 0 |

Probe Python: **3.11.9**; SQLite: **3.45.1**. Synthetic-only UUID:
`11111111-1111-4111-8111-111111111111`; synthetic-only SHA-256:
`1b8bab163d09bac84669afd98452abadd4760afd57c1076025b3a9efde5a4a98`.
Neither identifier is approval for the actual demo corpus.
Local result: `C:\Users\Administrator\AppData\Local\Temp\ekb-wp6a-synthetic-sd839o49\result.json`.
Invocation used an inline probe piped to `.\.venv\Scripts\python.exe -B -`.
An initial attempt with system Python stopped at missing FastAPI before fixture
creation; no dependency was installed. The corrected venv run passed with the
already-known Starlette TestClient deprecation warning.

This closes the **application layout compatibility question only**. It does not
prove Linux mount permissions, cloud upload, platform restart, container RSS,
read-only root or actual-corpus approval. No deployment artifact mount code change
is needed based on this result.

### Operator-controlled first-seed sequence — NOT EXECUTED IN CLOUD

1. Complete cost/account/topology gates. Keep real AI key absent and staging
   unadvertised. Before uploading, prove an operator-only path to populate the
   mounted volume while the application is stopped. If Files requires a live
   container, validate a platform-supported maintenance command override that
   opens no DB/listener; never start a second DB process or change the image.
   The maintenance marker is now observed, but a secure non-root maintenance and
   binary-upload path remains unverified; stop until that gate is resolved.
2. Mount `/data` **before** upload. Record volume identity and inspect UID 10001
   permissions. Prefer read-only `/demo`; otherwise use the separate seed directory.
3. Prepare only synthetic data for initial staging, or separately human-approved
   actual corpus. Validate with `validate_seed_artifact` at the baseline. Record
   SHA, UUID and validator result **before first start**. Reject links, sidecars,
   unknown schema, unsanitized metadata or nonempty historical AI audit tables.
4. Operator uploads the binary artifact through Files into the intended directory;
   never use the text editor for a DB. No HTTP upload/source endpoint may select
   its path. Recheck its server-side hash and frozen validator before start.
   Make it read-only to the runtime where the platform supports this; server-only
   control plus digest verification remains mandatory for the temporary pattern.
5. Configure non-secret settings below and restore the original
   `python -m src.hosted.server` entrypoint if a maintenance override was used.
   Start once. Observe exact schema 12, expected UUID, WAL, separate runtime DB,
   `/health=200`, `/ready=503 ai_not_configured`, no historical AI rows.
6. After verified first seed, retain expected UUID. In a controlled maintenance
   window, remove artifact path/digest settings and optionally only the exact seed
   file; never delete/unmount `/data` or delete the runtime DB/WAL/SHM. Restart and
   verify reuse, no reseed/migration, expected readiness and safe shutdown.
7. Same-commit redeploy under Recreate; verify preserved volume, same DB identity,
   schema/UUID, no overlapping instance, no reseed and expected readiness.

Actual synthetic cloud staging: **NOT RUN**. Actual corpus: **PENDING**, with
approval workflow in [Demo corpus approval](v0.6.0-demo-corpus-approval.md).

### Synthetic delivery package prepared during cloud resume — NOT UPLOADED

The earlier fixture artifact was revalidated with the unchanged WP4
`validate_seed_artifact`, matched to its recorded SHA/UUID and copied only from
that known synthetic temporary directory into a separate delivery directory:

`C:\Users\Administrator\AppData\Local\Temp\ekb-wp6a-cloud-seed-jal_1oq9\`

Files: **`demo.db`** and **`manifest.json`**. Upload only the synthetic DB through
the verified operator workflow; the manifest is local non-secret evidence.
Artifact size: **413696 bytes**; SHA and UUID are the synthetic-only values above.
Exact-v12 structure, integrity, sanitation, empty seed AI audit tables and absence
of sidecars passed validation again. No production DB, actual corpus, dotenv or
real key was used. Equal file sizes do not establish DB identity: the synthetic
SHA differs from the protected production SHA.

Cloud upload, first startup, HTTPS, health/readiness and restart/redeploy remain
**NOT RUN**. This prepared file is not a substitute for actual cloud evidence or
human approval of the competition corpus.

## Server-side secrets gate

The existing application is env-only and disables dotenv. That does not establish
the platform's secret storage/build behavior. Zeabur's Dockerfile page says it
automatically adds environment variables for single/final stages. Therefore
**do not treat the Variables tab alone as proof that a secret stays out of an image**.

Before any real key, use a clearly non-secret canary in the intended secret field
and obtain a documented runtime-only mechanism for this service/build method.
Verify canary absence from built image config/history/layers, build arguments,
build logs, uploaded files, `.env`, repository and DB. Check only a boolean for its
runtime presence. A passing canary plus exact configuration evidence is required;
if no compliant mechanism exists, stop for target/deployment-method resolution.
Do not introduce a registry push or rebuild workaround without authorization.

For the latest authorized Stage-1 pass, **the canary plan is deferred**: inspect
only mechanism existence without revealing values, leave `EKB_AI_API_KEY` absent,
and do not insert a canary into that key field. UI existence alone does not close
the separate image/build secret-exclusion P0. Seek the maintainer's next-stage
decision only after the required storage/topology/restart/redeploy checks pass.

Stage 1: real `EKB_AI_API_KEY` **not configured**. Stage 2, only after gates and
private staging controls: maintainer injects the key through the verified runtime
secret control. Record **configured YES/NO only**, never read/display/copy/screenshot
the value. Do not dump environment, settings, Docker container environment or
secret-bearing diagnostics. No `.env`, build args, Dockerfile, uploaded text config
or corpus may contain the key. A readiness check is not a paid model call and does
not prove provider-key validity. Do not run Agent during WP6A.

## Exact configuration plan — NOT applied to a cloud service

Names below were read from `src/hosted_config.py` and `src/hosted/server.py`.
These are deployment overrides; frozen code defaults remain unchanged.

| Variable | Planned value / rule |
| --- | --- |
| `EKB_RUNTIME_PROFILE` | `hosted`, explicit exact opt-in |
| `EKB_DATA_ROOT` | `/data` |
| `EKB_DEMO_DB_ARTIFACT` | `/demo/demo.db` preferred; `/data/seed/demo.db` approved pattern under evaluation |
| `EKB_DEMO_DB_SHA256` | Operator-reviewed artifact SHA, before first seed; actual value PENDING |
| `EKB_DEMO_KB_UUID` | Canonical UUID matching approved artifact; keep on restart; actual value PENDING |
| `EKB_AI_API_KEY` | Absent in Stage 1; verified runtime secret injection only in Stage 2 |
| `EKB_AI_DAILY_TOKEN_BUDGET` | **20,000 tokens**, selected planning proposal; maintainer approval PENDING |
| `EKB_AI_MONTHLY_TOKEN_BUDGET` | **200,000 tokens**, selected planning proposal; maintainer approval PENDING |
| `EKB_MAX_ACTIVE_AGENT_RUNS` | **2**; deployment override of frozen default 4 |
| `EKB_AGENT_RATE_LIMIT_PER_MINUTE` | **10**, unchanged |
| `EKB_SOURCE_RATE_LIMIT_PER_MINUTE` | **60**, unchanged |
| `EKB_AI_MAX_EXTRA_ATTEMPTS` | **0**, unchanged |
| `EKB_AI_TIMEOUT_SECONDS` | **30**, unchanged |
| `EKB_CORS_ALLOWED_ORIGINS` | Absent/empty until exact frontend HTTPS origin approved |
| `EKB_TRUSTED_PROXY_CIDRS` | Absent/empty; no wildcard, broad CIDR or inferred trust |
| `EKB_HOSTED_PORT` | **8000**, match platform route/health port and existing EXPOSE |
| `WEB_CONCURRENCY`, `UVICORN_WORKERS` | Absent or exact `1`; never >1 or heuristic worker count |

Missing profile preserves Local default only for an absent variable. Empty,
case variants and unknown values fail closed; Hosted configuration failure never
falls back to Local. No cloud inference is permitted.

Budget values are recorded-token admission limits, **not reservations or strict
financial caps** (Gate M1). Two admitted runs may incur usage before either is
recorded; one Agent run can contain multiple model calls. Do not describe overshoot
as exactly two tokens/calls or assign an unproved hard bound. Before Stage 2,
maintainer must approve a separate provider-spend allowance with headroom above
these token thresholds and the pause procedure. Provider billing/model prices and
actual multi-call usage determine that allowance; no real key, spend approval or
payment limit was inspected. Lower concurrency and zero retries reduce exposure
but do not remove this limitation.

## Networking, trust, frontend and health

Provisional request path: browser → platform/edge TLS ingress → service port 8000
→ existing Uvicorn → frozen WP3 effective-IP middleware. Actual gateway hops and
immediate socket peer are **NOT VERIFIED**. The reviewed HA/networking material
does not provide an authoritative stable trusted-proxy CIDR list for this service.
No support confirmation was obtained. A single observed peer would not suffice.

Keep trusted proxy list **EMPTY**. Do not trust arbitrary `X-Forwarded-For`, `*`,
`0.0.0.0/0` or `::/0`; keep existing Uvicorn proxy interpretation disabled.
All clients may share one ingress rate bucket: a usability limitation, not a bypass.
Obtain peer-only platform evidence without logging query bodies, prompts or secrets;
record the actual result and maintainer acceptance before exposure. Do not relax
rate defaults merely because shared buckets are possible.

CORS remains **EMPTY**. Proposed responsibility: maintainer 杨洋 assigns the frontend
owner, who supplies the exact stable HTTPS origin and chosen hosting platform;
assignment/origin approval is **PENDING**, not presumed complete. EdgeOne frontend
hosting remains an option. Freeze an exact origin without path/trailing slash;
no wildcard or preview-domain pattern. CORS is not an access-control mechanism.

TLS terminates at platform/edge, not Uvicorn. Test the generated/custom hostname
with normal certificate validation, exact hostname and HTTPS redirects. Do not
disable TLS verification or weaken a CDN origin TLS setting to obtain a pass.
For mainland hosting, maintainer handles provider/Zeabur real-name and applicable
ICP/pre-ICP requirements; no identity documents belong in this repository or chat.
Hong Kong fallback requires actual mainland ISP/mobile-network reachability tests,
with location, network, timestamp, status and latency recorded, not just DNS success.

Health: set platform HTTP probe to `/health` on 8000, expected **200**. `/ready`
must not be the sole kill/restart check. Independently configured readiness has
not been established; use manual `/ready` checks before opening access. Stage 1
expects **503 / ai_not_configured**; private Stage 2 expects **200**, no Agent run.
Record cold-start and restart timing and tune only supported platform probe settings.

Gate edge duties also remain open: TLS/security headers, request-size limits and
slow-client/header/body timeout protection. Verify actual platform controls; no
new EKB authentication, endpoint or limiter is authorized. If no private edge
restriction can be demonstrated, keep the key absent throughout WP6A.

Cloud DashScope connectivity: **NOT RUN**. Later, from the selected service, test
DNS for `dashscope.aliyuncs.com` and TLS/unauthenticated HEAD to the existing base
URL `https://dashscope.aliyuncs.com/compatible-mode/v1`, with normal certificate
verification and a bounded timeout. A 401/404/405 can establish network reachability,
not model availability or valid credentials. No Authorization header, POST,
completion, embedding or rerank call. Do not substitute this workstation's route
for a cloud-to-provider result.

## Log capacity, retention and backup policy — proposed, approval pending

Initial `/data` capacity planning: **at least 1 GiB**, only if corpus/runtime size
plus WAL, temporary first-seed copy and logs fit with margin. Account for artifact,
temporary seed and runtime copies during first seed. Reassess after corpus size is
known; the 1 GiB figure is not an asserted configured quota. Log file:
`/data/logs/engineering-kb.log`, existing append-only behavior unchanged.

- Maintainer assigns an operator (PENDING). Check free space, DB/WAL and log sizes
  daily, before/after redeploy and **hourly during a live demo**.
- Retain logs for **at most seven days or 100 MiB total**, whichever occurs first.
  Enforce manually in maintenance windows; this is not automatic rotation.
- Warn at **70% volume used** or **50 MiB logs**. Suspend public traffic and arrange
  cleanup at **85% volume used**, **100 MiB logs**, or **<256 MiB free**, whichever
  occurs first. Check predicted growth between inspections; reduce the interval
  or stop service if manual monitoring cannot prevent exhaustion.
- Cleanup only after traffic is blocked and the sole process has gracefully stopped.
  Verify resolved paths and exact log files; retain only operator-approved bounded
  diagnostic records, then explicitly rotate/truncate the selected log. No recursive
  wildcard deletion, no DB/WAL/SHM deletion and no live rename that leaves an open
  file descriptor consuming space. Restart and check health/readiness/storage.
- Never silently purge SQLite AI audit rows to recover disk space. Escalate unexpected
  database growth and pause public usage. No application code change is authorized.

Platform log retention is separate from application-file retention. Optional
platform volume backup is allowed, but enabling a paid plan requires approval.
For SQLite use a **quiesced, offline volume snapshot/backup**, then restore only
under an explicit maintenance plan with the old writer stopped. No extra EKB
backup endpoint and no assumption of online automatic SQLite consistency.
The documented seven-day backup retention must be confirmed for the selected plan.
No important volume was deleted, unmounted, backed up or restored during WP6A.

## Deployment identity and privacy evidence

| Evidence | Value |
| --- | --- |
| Planned application commit | `92425fdc40afff43d56893b933a01398eab68765` |
| Configured service source / build tier | Full expected GitHub repository URL, **main**, **Free / 2 vCPU / 4 GB** SCREENSHOT OBSERVED in service settings; no resolved build commit or runtime-resource claim |
| Actual cloud deployment ID / build status | **6a9170f213d3d467215e5926**; build/upload completed, latest maintenance metadata **21:14:53** shows root identity and `/data` mode 0777. Image/base digests still pending |
| Actual cloud deployment commit / image ID / resolved base digest | Earlier overview deployment title matches baseline title; latest build-log tail does not expose full SHA, image or base digest: **PENDING**. Build completion is not verified EKB startup |
| Historical WP5/Gate resolved base | `python:3.11-slim@sha256:1042b61448fef4ba92d16a8c7eb4996d027568ce64792a7877fd88511e0af7c6` |
| Historical container versions | Python 3.11.16 / SQLite 3.46.1, not this host probe's versions |
| Current base tag | Mutable `python:3.11-slim`; unchanged |
| Cloud image/layer/credential/private-file audit | NOT RUN |
| Public staging URL | None created |
| Existing cloud project / service | Existing EKBstaging / ekb-staging identities unchanged in latest screenshot; selected deployment now **运行中** with maintenance marker, not formal EKB startup |
| Actual AI key exposed | NO; no actual key read, configured or displayed |
| Public Agent exposed by this work | NO |
| Production DB uploaded/copied/mounted/opened as SQLite | NO / NO / NO / NO |
| Actual corpus SHA / UUID / structural result | PENDING / PENDING / PENDING |
| Real completion / embedding / rerank | **0 / 0 / 0** |

Future actual build evidence must record the resolved base digest, application SHA
and image digest from that deployment. Do not reuse historical digests as proof of
a mutable-tag cloud rebuild. Inspect only non-secret deployment metadata and
boolean privacy checks. Verify no Local DB, private PDFs/PNGs, `.env`, Git credentials
or personal files enter build context, final image or cloud filesystem.

Protected Local DB: `D:\Projects\ekb-dev\data\database\knowledge.db`.
Start and end size: **413696 bytes**. Start and end SHA-256:
`59CAF2CFC5E80D197CA02A64B702EA6D06B7C4EB66E02C7FEFA272403A4C0AD9`.
Only file size/hash were read for this protection check; no content was displayed.

## Remaining P0 and maintainer handoff

| Item | Required evidence / decision | Current state |
| --- | --- | --- |
| Account, target, cost | Existing purchased Alibaba Cloud Shanghai server 6a911c45af37eeef8fb27a27, displayed 2 cores / 4 GB, ZeaburOS/K3s, USD 6/month, 48.8 GB disk, unlimited outbound traffic, expiry 2026-09-29; USD 20 funding maintainer-confirmed. Exact project link-ID, volume backing, any account-specific build quota, bandwidth and renewal details remain pending | Project/server association and ekb-staging observed; build completion observed, latest selected deployment 运行中 with maintenance marker after earlier startup failure; EKB acceptance still pending. Settings confirm expected source/main and Free build tier. Official fixed-fee/no-extra-usage policy checked; no invoice or changed bill observed. Additional purchases FORBIDDEN |
| Mainland domain/identity | Maintainer confirms eligible temporary use or final domain/filing route; completes required identity steps personally | PENDING |
| Single writer | Effective replica=1, no HA/autoscale, Recreate configuration and old-stop-before-new-start evidence | UI replica **1** SCREENSHOT OBSERVED; effective running count and Recreate timeline still PENDING. Reject target if not guaranteed |
| Container and storage | Existing Dockerfile build, non-root identity, protected persistent volume, synthetic first seed, restart/redeploy, graceful stop and privacy audit | Maintenance metadata observed: UID/GID 0, mounted `/data` owner 0:0/mode 0777. **NON-ROOT GATE UNMET**; inspect saved recipe/image/platform identity settings before any DB upload or EKB start. No persistence or EKB acceptance PASS |
| Runtime-only secret | Resolve ENV/build ambiguity with canary and platform evidence; privately inject actual key only afterwards | PENDING |
| Actual corpus | Human content/ownership/privacy approval, exact SHA/UUID and structural validator PASS | PENDING; synthetic proof is not approval |
| Budget and concurrency | Approve planned 20,000/day, 200,000/month, concurrency 2 and separate spend headroom/pause policy | Proposed, not frozen/configured |
| Proxy and rate usability | Empty trust retained; verify route/peer, accept shared-bucket effect or obtain authoritative stable CIDR | Secure plan recorded; live evidence/acceptance PENDING |
| Frontend/CORS | Assign frontend owner; approve exact HTTPS origin | PENDING; CORS empty |
| Edge/HTTPS/mainland access | Actual certificate, edge limits/headers/timeouts, mainland accessibility and stable staging/domain evidence | PENDING |
| Health/provider path | Platform HTTP `/health`, independent/manual `/ready`, no-paid-call cloud DNS/TLS validation | UI currently shows TCP health check enabled; HTTP path/port and all live health/readiness/provider-path results PENDING |
| Logs/backup | Assign operator; approve capacity/retention/threshold/maintenance policy; optional backup cost decision | PENDING |
| Stage 2 and WP6B | Private readiness only after gates; separately authorized controlled real smoke belongs to WP6B | NOT RUN; WP6B NOT READY |

No unresolved item has been marked approved by Codex. Research and local layout
compatibility are complete; live target validation is incomplete. No full pytest
rerun is claimed for this documentation-only work; historical Gate results remain
historical.

### Release-closure documentation checkpoint — 2026-08-28

The paused WP6A handoff originally left only this file and
`v0.6.0-demo-corpus-approval.md` uncommitted. The subsequent Release Closure
instruction authorizes a documentation-only checkpoint with message
`docs: record paused v0.6.0 deployment validation`, and a normal push only if
`origin/main` has not advanced. Entry inspection and fetch found local HEAD and
`origin/main` both at `92425fdc40afff43d56893b933a01398eab68765`, ahead/behind 0/0,
with exactly those two untracked documents. No user changes were discarded.

This checkpoint records the pause; it does not certify a cloud runtime, change
the historical code/test baseline, cancel public deployment or release v0.6.0.
Commit/push and the required clean-tree checkpoint are to be verified separately
in the release-closure inventory. Cloud observations above are historical records,
not a fresh console inspection. No deployment resume is authorized by this archive.
