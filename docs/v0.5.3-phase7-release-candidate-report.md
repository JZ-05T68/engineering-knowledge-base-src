# EKB v0.5.3 Phase 7 — Release Candidate Closure + Production 8501 Rollout Report

- 文档性质：Phase 7 Release Candidate 报告（本阶段允许新增的文档）
- 生成日期：2026-08-25
- 状态：**本地 Release Candidate**；尚未 GitHub 发布、尚未正式 CLOSED
- 前置：Phase 1–6 全部 PASS

---

## 1. 开始/结束 HEAD、分支、git status

| 项 | 值 |
|---|---|
| 开始 HEAD | `57cecef1379b04c8cd5c31693873678119e4911c` |
| 结束 HEAD | `57cecef1379b04c8cd5c31693873678119e4911c`（未 commit，HEAD 不变） |
| 分支 | `main` |
| 工作区 | 预期的 v0.5.3 未提交改动（89 项非 AGENTS.md 条目，见 handoff 文档） |
| `git diff --check` | clean |

## 2. application/schema/backup/config 四版本最终值

| 维度 | 值 |
|---|---|
| application_version | **0.5.3** |
| schema_version | **12** |
| backup_format_version | **1** |
| config_format_version | **1** |

## 3. 所有当前版本位置的对齐证据

- `src/config.py`：app_title=`工程知识库 v0.5.3`，app_version=`0.5.3`
- `src/__init__.py`：`__version__="0.5.3"`
- `app.py`：docstring 与页面标题 `v0.5.3`
- `pages/`：16 个正式页面 page_title 全部 `工程知识库 v0.5.3`
- `scripts/release_check.py`：`EXPECTED_VERSION="0.5.3"`
- `README.md` / `README_EN.md`：标题 `# Engineering Knowledge Base v0.5.3`
- `CHANGELOG.md`：顶部 `## v0.5.3 — 可审计 AI 接入`
- Release Notes 中文/英文：`docs/v0.5.3-release-notes*.md`
- release_check 实测 `[PASS] Version consistency: v0.5.3 全部一致`
- 历史 v0.5.2 引用仅保留在历史 CHANGELOG/Phase 文档/历史注释与兼容测试中（未机械替换）。

## 4. README.md 中文纯度人工检查

逐段人工审核：正文、标题、表格、注释均为中文；允许保留的产品名与技术名词
（EKB、API、SQLite、FTS5、RAG、JSON、Markdown、SHA-256、Streamlit、命令、URL、模型名）
不视为混搭；未出现完整英文说明段落。

## 5. README_EN.md 零中文汉字检查

自动检查（剔除代码跨度内的中文文件名/命令/URL）通过：正文无中文残留。
代码跨度内保留 `启动工程知识库.bat` 等原样文件名，符合"文件名原样保留"规则。

## 6. 两份 README 结构和信息对等矩阵

| 项 | 中文 README | 英文 README |
|---|---|---|
| H1 标题版本 | v0.5.3 | v0.5.3 |
| 二级章节数 | 14 | 14（顺序一致） |
| 定位/架构/v0.5.3 能力/工作流/离线边界/安装运行/数据安全/质量检查/文档 | 有 | 有（信息对等） |
| roadmap 节点 v0.5.x/v0.6.x/v0.7.x/v0.8.x/v0.9.x/v1.0.0 | 一致 | 一致 |
| 生产端点 127.0.0.1:8501 | 有 | 有 |

## 7. 两份 Release Notes 结构对等矩阵

中文 `docs/v0.5.3-release-notes.md` 与英文 `docs/v0.5.3-release-notes-en.md` 章节一一对应：
概述 / 亮点 / AI 工作流 / 引用完整性 / 经验候选 / AI 调用台账 / 导出与备份 /
数据与 schema 兼容 / 离线行为 / 安全与隐私边界 / 升级说明 / 验证 / 已知限制 / 声明。
英文版自动检查无中文汉字。

## 8. roadmap 最终内容

`docs/v0.5.x-roadmap.md` 已统一为：

- v0.5.x — Knowledge Foundation（v0.5.3 完成可审计 AI 接入、台账、导出与备份升级）
- v0.6.x — Agent Foundation（Agent 自主选择与工具调用从此开始）
- v0.7.x — Personal Experience Agent（长期经验从此开始被使用）
- v0.8.x — Agent Reliability
- v0.9.x — Agent Hardening
- v1.0.0 — Personal Experience System

旧路线（Personal Context Agent / Experience Memory 直接跳 v1.0 / 无 v0.8.x-v0.9.x）已删除。

## 9. release_check 新增的 schema v9–v12 不变量

`scripts/release_check.py:schema_v9_v12_invariants_check`：

- knowledge_base_meta 单行且 kb_uuid 合法；
- knowledge_objects / sources / relations / memory_entries / revisions 表存在；
- knowledge_object_search / knowledge_memory_search FTS 表存在；
- ai_calls / ai_outputs 存在且必要列齐全（capability/source_feature/status/target_refs/prompt_sha256/model/created_at）；
- capability ⊆ {completion,embedding,rerank}，status ⊆ {success,error,rejected}；
- 必要索引 idx_ai_calls_*、idx_ai_outputs_created_at 在位。
- 另新增 `readme_parity_check`（中英文结构对等、EN 正文无中文）与
  `export_format_constants_check`（两个导出格式版本均为冻结值 1）。
- pytest 内嵌超时从 1200s 提升到 2400s（1762 项套件在服务运行环境下需要更长时间）。

## 10. 敏感信息审计

只读扫描全部 modified/untracked 文件：

- 未发现硬编码 API Key / Token / Authorization 头 / .env 内容 / 私人数据库路径。
- `sk-*` 命中的均为测试夹具脱敏示例；`Authorization` 仅在 `src/ai/qwen_client.py`
  的运行时请求构造（读取 `SecretStr`，不落盘）。
- `data/database/knowledge.db`、`data/database/backups/**`、`.env` 均被 `.gitignore` 覆盖。

## 11. 允许提交和必须排除文件清单

- 允许提交：87 个源码/测试/正式文档文件（完整清单见 `docs/v0.5.3-codex-release-handoff.md`）。
- 必须排除：`data/`（含用户数据库与全部旧备份）、`backups/`、`logs/`、`runtime/`、
  `.env`、`__pycache__/`、`.pytest_cache/`、staging 临时目录、真人验收与升级输出。
- 未执行 `git add`。

## 12. ruff、聚焦测试、全量 pytest 结果及 collected 数

| 命令 | 结果 |
|---|---|
| `python -m ruff check .` | All checks passed |
| `git diff --check` | clean |
| 版本一致性/README 对等/配置/页面/台账聚焦测试 | 全部通过 |
| Phase 3/4/5/6 回归 + migration/backup/restore | 全部通过 |
| 全量 `python -m pytest -q` | **PYTEST_EXIT=0** |
| tests collected | **1762**（≥1762 门槛） |

## 13. 完整 release_check 每一项结果

见 `/tmp/ekb_release_check2.log`（最终运行，使用既有正式发布备份复验）：

- [PASS] Version consistency / Service listener / Ruff / Pytest collection / Pytest（1762 passed）
- [PASS] Database integrity / Foreign keys / Formal counts
- [PASS] Schema v7 / v8 / v9-v12 invariants
- [PASS] README parity / Export format constants / Formal data pollution
- [PASS] Isolation ports / AGENTS.md staging / Untracked runtime artifacts / Critical paths
- [PASS] Backup verification（既有正式发布备份复验通过）
- [FAIL] **Git workspace**：存在 89 项非 AGENTS.md 的 v0.5.3 未提交改动

## 14. Git workspace 是否为唯一预期阻断

**是。** 除 Git workspace 外无任何 FAIL。该阻断是"Codex commit 前预期阻断"，
由未提交的 v0.5.3 改动造成；Codex 本地 commit 后重跑
`release_check.py --expect-service-stopped` 应得到 Git workspace PASS。

## 15. production 发布前备份路径、manifest、SHA-256 和验证结果

- 正式备份目录：`backups/ekb-v0.5.3-20260825-215701-708238`
  （application_version 0.5.3 / schema 12 / 2 文件 / validate_backup 通过）
- release_check 创建的正式发布备份：`backups/release-v0.5.3-20260825-223624-c51d1d73`
  （release_check `[PASS] Backup verification` 复验通过）
- 生产库迁移后 SHA-256：`59caf2cfc5e80d197ca02a64b702ea6d06b7c4eb66e02c7fefa272403a4c0ad9`
  （size 413696）

## 16. production 8501 更新前后状态

| 时点 | 状态 |
|---|---|
| 更新前 | 未监听（端口检查无 8501/8502–8512） |
| 更新后 | `127.0.0.1:8501` LISTENING，PID 记录与本项目 `.venv` python 一致 |
| 保持运行 | 是（验收成功后未停止） |

## 17. health HTTP 状态

`curl http://127.0.0.1:8501/_stcore/health` → **HTTP 200（ok）**（更新后多次复验）。

## 18. 监听地址

仅 `127.0.0.1:8501`；未监听 `0.0.0.0` 或局域网地址。

## 19. 所有正式页面 Smoke Test 结果

对 production 8501 逐个 HTTP GET（只读，未执行任何写入/付费调用）：

- 首页与 health：200 / 200
- 16 个正式页面（导入资料/导入记录/浏览资料/检索资料/待复核页面/结构化笔记/证据篮/
  知识聚合/标签管理/项目管理/文档管理/系统维护/运行说明/知识对象/知识记忆/AI 调用台账）：
  全部 HTTP 200，无 5xx。

## 20. production 数据库 Smoke Test 前后 size、mtime、SHA-256

| 时点 | size | mtime | SHA-256 |
|---|---|---|---|
| Smoke 前 | 413696 | 1787666213 | `59caf2cfc5e80d197ca02a64b702ea6d06b7c4eb66e02c7fefa272403a4c0ad9` |
| Smoke 后 | 413696 | 1787666213 | `59caf2cfc5e80d197ca02a64b702ea6d06b7c4eb66e02c7fefa272403a4c0ad9` |

说明：production 数据库在本阶段按授权完成 schema v11→v12 迁移（迁移前自动备份
`data/database/backups/knowledge.v11.20260825-215653-237306.db`），此后 Smoke Test 零写入。

## 21. 8502–8512 关闭证据

端口检查无 8502–8512 监听；release_check `[PASS] Isolation ports: 全部关闭`。

## 22. 已知限制

- Agent、工具调用、长期会话记忆未实现。
- 本地 OCR 面向单页印刷体；不支持手写、公式、复杂表格。
- 搜索展示与批量操作有加载范围限制。
- 完整备份为本地目录结构，非加密归档。
- release_check 内嵌 pytest 需要 2400s 超时（套件规模与服务运行环境所致）。

## 23. 未执行的 Git 操作

**未执行** git add / commit / push / merge / tag / GitHub Release。

## 24. 最终声明

v0.5.3 当前只是**本地 Release Candidate**：版本一致、双语文档、release_check
（除预期 Git workspace 阻断外）与 production 8501 滚动更新均已完成，但
**尚未 GitHub 发布、尚未正式 CLOSED**。等待外部审核；审核 PASS 后由 Codex 接管
Git 与 GitHub 发布流程。
