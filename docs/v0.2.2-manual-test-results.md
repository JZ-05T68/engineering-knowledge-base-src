# Engineering Knowledge Base v0.2.2 人工测试结果

## 测试信息

- 测试版本：v0.2.2
- 功能开发 HEAD：`1a78ff8`（`feat: add single-page OCR controls`）
- 测试日期：2026-07-26
- 测试环境：Windows 本地环境、Python 3.11.9、Streamlit、SQLite schema v4、
  rapidocr 3.9.2、onnxruntime 1.27.0
- 隔离方式：隔离测试目录 + 临时数据库 + 专门生成的三页测试 PDF；
  数据、日志与运行路径全部指向隔离测试目录
- 隔离端口：127.0.0.1:8503（8502 被既有进程占用，按预案改用 8503；正式端口不变）
- 测试 PDF：临时三页测试文档（第 1 页纯图像英文数字、第 2 页纯图像中文、
  第 3 页可靠文本层 62 字符），SHA-256 已记录并于测试结束时复核不变
- 正式环境：正式服务 8501 全程未运行、未被访问；正式数据库与正式资料目录零接触

## 总体结论

PASS

MT-01～MT-10 十项隔离人工测试全部通过。v0.2.2 本地单页 OCR 在真实离线引擎下正确识别
英文与中文扫描页，OCR 初稿与人工稿分离，搜索与证据警告闭环正确，数据保护、重复执行
保护和离线边界全部满足。用户亲自确认首次执行时的 spinner 展示与约 2.53 秒等待体验：
PASS。

## 测试结果

| 编号 | 场景 | 结果 | 实际观察 |
|---|---|---|---|
| MT-01 | 启动与非 OCR 功能 | PASS | 隔离实例正常启动（HTTP 200）；主页与导入页无异常；启动后 rapidocr/onnxruntime 均未加载（惰性初始化成立） |
| MT-02 | 导入临时扫描 PDF | PASS | 三页全部导入成功并渲染 PNG；原始测试 PDF 未被覆盖；第 1 页显示“执行本地 OCR” |
| MT-03 | 英文数字单页 OCR | PASS | 首次识别（含模型初始化）约 2.53 秒；显示“本页 OCR 已完成。”；OCR 初稿精确识别为 `STM32H750 ADC TEST 21MPa`；初稿只读并标注“未经人工核验”；执行后按钮消失；Markdown、review_status、PNG hash 不变 |
| MT-04 | 中文单页 OCR | PASS | 第 2 页精确识别为 `本地OCR测试`；第 1 页结果不变；无串页；界面无本机路径显示 |
| MT-05 | 可靠文本层页 | PASS | 第 3 页点击后提示“当前页面不符合 OCR 条件”；真实引擎调用计数未增加；62 字符文本层不变；无系统错误 |
| MT-06 | OCR 搜索 | PASS | `STM32` 仅命中第 1 页、来源字段为 ocr_text；中文关键词仅命中第 2 页、来源字段为 ocr_text；无误命中 |
| MT-07 | OCR 证据警告 | PASS | 证据包显示 OCR 初稿警告（本地 OCR 初稿、未经人工核验、以原始页面图像为准）；文档与页码引用正常 |
| MT-08 | 数据保护 | PASS | 只读核验：第 1、2 页 extracted_text 为空不变、ocr_text 已写入、markdown 为空不变、状态 ocr_completed、review_status 仍为 pending；第 3 页无 ocr_text；PDF/PNG SHA-256 不变 |
| MT-09 | 重复执行保护 | PASS | 已有 OCR 页面不显示执行按钮；引擎调用数为零增长；第一次 OCR 结果保持不变 |
| MT-10 | 日志与网络 | PASS | 隔离日志无 HTTP、无模型下载、无 API Key；引擎初始化仅发生于首次点击时；界面无 traceback 与本机路径；正式 8501 未被访问 |

## 自动化验证汇总

- Ruff 全量检查通过。
- 自动化测试 456 项全部通过（含 RapidOCR 适配器单元测试、真实离线 OCR 识别测试、
  OCR 搜索与证据闭环测试、单页 OCR 界面测试与两种执行顺序的隔离回归）。
- 断网进程验证通过：屏蔽网络入口后 import、初始化、首次识别全部离线完成，模型从
  本地安装包模型目录加载，无仓库写入，输入图片哈希不变。

## 正式环境隔离核验

- 全程未访问、复制或修改正式数据库与正式资料目录；主工作区与其他 worktree 未被修改。
- 正式服务 8501 未受影响；隔离实例停止后测试端口无监听、无残留测试进程。
- 测试数据（测试 PDF、页面 PNG、临时数据库、证据、日志）仅存在于隔离测试目录，
  未纳入 Git；验收现场保留供发布收口复核。

## 遗留问题

- 证据包按既有设计包含原始 PDF 与页面图像的绝对路径行（指向本地资料路径），
  属证据溯源既有行为，与 v0.2.2 无关；OCR 反馈与警告文案不含任何路径。
- 具有可靠文本层的页面仍显示“执行本地 OCR”按钮，点击后正确提示不符合条件且不调用
  引擎；属当前设计行为。
- 既有 Streamlit `components.v1.html` 弃用提示与 v0.2.2 无关，按既定结论记为非阻断项。
