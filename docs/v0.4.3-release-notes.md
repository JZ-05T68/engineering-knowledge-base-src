# Engineering Knowledge Base v0.4.3 发布说明

发布日期：2026-08-14

## 版本定位

v0.4.3 是真实问题验证与 AI 就绪决策门收口版本。它验证现有 v0.4.x 证据基础是否具备可追溯、
可人工检查、过期失效且失败关闭的真实工程工作流。

## 已完成范围

- RPV-01～11：全部 PASS。
- H1～H7：全部 PASS。
- FIX-A～FIX-D：图片区域响应式完整显示、手动坐标备用入口、笔记重要程度排序和中性边框。
- GH-01～GH-04：源码树、中文一致性、README 历史层级和 GitHub milestone 治理。
- 版本元数据归一为 v0.4.3；schema 保持 v7，无数据库迁移、无新依赖。

## 边界与延后项

- RPV-12 按 DF-AMEND-02 延后至 v0.5.0，不属于 v0.4.3 退出条件。
- OBS-E 仍是 MEDIUM 显式技术债：图片区域拖拽中无实时矩形，鼠标释放后可检查并重选；目标为 v0.5.x。
- 决策门结论是 **v0.5.0 CONDITIONAL GO**，只表示允许开始后续 AI 集成，不表示 AI 集成已实现或验证。

## 产品与数据边界

- 用户资料默认仅保存在本地，核心知识管理功能可离线使用。
- 不需要账号、云同步、VPN、API Key 或付费 AI 服务。
- 不自动覆盖原始资料，不自动修改用户笔记，不自动删除原始 PDF 或页面图像。
- 正式服务仅绑定 `127.0.0.1:8501`。
