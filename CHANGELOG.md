# Changelog

## v0.4.2 — Prompt Freshness & Stale Output Guard\r
\r
发布日期：2026-08-12\r
\r
### Changed\r
\r
- 引用提示词包新增 freshness 校验：已生成 Prompt 与当前问题及当前 confirmed\r
  evidence 的实际 Prompt 输入绑定。\r
- 问题、confirmed evidence 集合、确认状态、有效排序、confirmed 用户备注、\r
  整页证据当前文本或来源有效性变化后，旧 Prompt 不再作为当前结果显示，\r
  并提示重新生成。\r
- 无关字段变化（不进入 Prompt 的标签、项目、复核状态、未确认证据信息）\r
  不会造成无意义失效。\r
\r
### Trust / Safety\r
\r
- freshness fingerprint 直接基于当前 `export_prompt_package()` 输出的\r
  SHA-256，与 Prompt 生成共用同一 source validation 路径；无法证明旧结果\r
  仍 fresh 时 fail closed。\r
- stale 结果真正从 session state 清除，不只是隐藏；改回原输入不会复活旧结果。\r
- confirmed 来源失效继续触发既有 `EvidenceSourceError`，不静默跳过、不自动修复。\r
- 0 条已确认证据时回到既有空态并禁用生成。\r
- 本版本无 schema 迁移（保持 schema v7），无新增依赖，无网络请求与 API Key。\r
\r
### Known limitations\r
\r
- 普通 Markdown 证据包仍为一次生成的静态结果；证据变化后不会自动失效。\r
  这是既有行为，登记为 future candidate，不属于 v0.4.2 Prompt freshness 范围。\r
\r
### Tests\r
\r
- 独立代码审计：PASS（BLOCKER 0 / HIGH 0 / MEDIUM 0 / LOW 1，即上述已知限制）。\r
- 真人 Smoke Test：S-01～S-08 全部 PASS（含 stale 复活与过度失效反向场景）。\r
- Ruff 全量通过；完整 pytest 收集 1050 项，exit 0。\r
- `git diff --check` 通过；schema v7；requirements 不变；无网络 / AI API 要求。\r
\r
## v0.4.1 — Evidence-grounded Prompt Package\r
\r
发布日期：2026-08-12\r
\r
### Added\r
\r
- 证据篮新增“生成引用提示词包”：以已人工确认的证据为来源，在本地生成\r
  citation-grounded 提示词包，用户自行复制给外部 AI 工具；EKB 不调用任何 AI 服务。\r
- 只有 `confirmed` 状态的证据进入 Prompt Package；未确认证据不得混入。\r
- 三类证据按真实语义输出：文字选区输出选区正文与可信度标识；整页证据输出\r
  当前整页文本并明确标识为当前文本（无文本页明确说明）；图片区域证据只输出\r
  文档、页码、图像尺寸与区域坐标，并明确纯文本提示词包不包含图片像素。\r
- 证据篮页面显示证据总数、已确认与未确认数量；无已确认证据时生成入口禁用并\r
  显示明确空态。\r
\r
### Changed\r
\r
- `PromptBuilder` 的默认问题与 grounding policy 抽取为共享定义\r
  （`DEFAULT_QUESTION` / `GROUNDING_RULES`），供搜索结果提示词与证据提示词包\r
  共同复用；既有 SearchResult Prompt 输出逐字节保持兼容。\r
- 应用、页面标题、备份恢复工具与统一发布检查版本更新为 v0.4.1。\r
\r
### Trust / Safety\r
\r
- 生成前对每条参与生成的已确认证据执行既有 source validation；任一已确认\r
  来源失效（文本哈希变化、区域图像锚点失效、文档或页面缺失）时整包\r
  fail closed，不静默跳过、不自动修复。\r
- 用户备注在 Prompt 中独立标识，不等同于来源事实。\r
- 图片区域证据不得根据区域坐标、图像尺寸或用户备注猜测图片内容。\r
- 本版本无 schema 迁移（保持 schema v7），无新增依赖，无网络请求与 API Key。\r
\r
### Known limitations\r
\r
- 生成后的 Prompt Package 是一次生成的静态结果；后续证据增删或确认状态变化\r
  不会自动清除旧结果。真人 Smoke Test 判定为 ACCEPTABLE，不阻断本版本；\r
  “证据变化后自动清除已生成 Prompt”仅登记为 v0.4.2 candidate，不属于\r
  已承诺范围。\r
\r
### Tests\r
\r
- 独立代码审计：PASS（BLOCKER 0 / HIGH 0 / MEDIUM 1，即上述已知限制）。\r
- 真人 Smoke Test：PASS（含 stale Prompt 体验判定 ACCEPTABLE）。\r
- Ruff 全量通过；完整 pytest 收集 1035 项，exit 0（1034 passed、1 skipped；\r
  跳过项为 Windows 符号链接策略限制的既有条件跳过）。\r
- `git diff --check` 通过；schema v7；无新增依赖；无网络 / AI API 要求。\r
\r
## v0.4.0 — Evidence Objects & Source Model

发布日期：2026-08-12

### Added

- 统一 Evidence Object / Source Model：证据篮支持整页、文字选区与图片区域三类
  证据对象，并保留文档、页面、来源定位和采集时的校验信息。
- 图片区域证据使用原始页面 PNG 的 SHA-256、像素尺寸与归一化坐标作为来源锚点；
  来源图像变化时拒绝继续作为有效区域证据处理。
- 每条证据新增未确认 / 已确认的人工确认状态与确认时间；状态转换由用户显式触发。
- 结构化笔记的页面、文字选区与图片区域入口接入证据篮，保留笔记与证据对象的
  业务边界，不自动修改原始资料或用户笔记。

### Changed

- 侧边栏按导入、阅读整理、知识组织与维护工作流重新排序，相关内部跳转同步更新。
- 数据库升级为 schema v7；既有证据逐条保留并迁移为未确认的文字选区证据。
- 应用、页面标题、备份恢复工具与统一 release check 版本更新为 v0.4.0；
  release check 增加 schema v7 证据类型、区域锚点和人工确认结构检查。

### Tests

- Ruff 全量通过；完整 pytest 收集 1020 项，1019 passed、1 skipped；唯一跳过项为
  Windows 当前策略不允许创建符号链接时的安全测试条件跳过。
- 真人 Smoke Test 通过：Streamlit `http://localhost:8501` 正常，浏览资料与
  结构化笔记最终确认正常。
- 首次页面无法打开由所用 Python 环境缺少 `requirements.txt` 已声明依赖导致；
  补齐运行环境后通过，未因此产生代码修改。
- 本版不包含 v0.4.1+ 功能、账号、云同步或必需的外部 AI 服务。

## v0.3.3 — 文档管理与数据安全

发布日期：2026-08-11

### Added

- 独立「文档管理」页：全量文档清单（标题 / 原始文件名 / 页数 / 状态 /
  导入时间），按 document id 选择目标文档并复用共享删除确认链执行
  删除；删除成功后清单自动刷新、失效选择自动清理。
- 删除服务层标题确认不变量：`DocumentDeletionService.delete_document()`
  新增必填 keyword-only 参数 `expected_title`，在执行时重新读取的标题
  上做严格相等校验（不去空白、不折叠大小写），不匹配立即中止且不产生
  任何副作用，与 UI 层确认形成双层纵深防御。

### Changed

- 浏览页「文档管理」内的删除入口收敛：删除确认链迁移至「文档管理」页，
  原位置保留迁移说明与跳转按钮；「保存文档分类」区块不变。
- 删除警告文案按备份语义修正：明确删除作用于当前知识库中的文档及关联
  数据和工作文件，历史备份中可能仍保留旧版本。

### Tests

- 新增 expected_title 契约定向测试（精确匹配成功；错误 / 大小写 /
  前后空格 / 空字符串一律拒绝且零副作用；省略参数为 TypeError；重复
  删除被拒且无新增隔离操作；预览后状态变化时删除基于执行时真实状态）。
- 新增「文档管理」页 AppTest（清单展示、空态、目标绑定、确认链、删除后
  刷新与 session_state 清理），并固定浏览页不再渲染删除确认链。

## v0.3.2 — 跨文档知识聚合与文档删除生命周期加固

发布日期：2026-08-11

### Added

- 跨文档知识聚合：独立「知识聚合」页，以**项目轴 / 标签轴**把分散在多份
  资料中的结构化笔记与证据汇总为统一只读视图；支持重要性（重点 / 次重点 /
  一般）与笔记类型筛选、稳定排序与分页（20/50/100）；每条目保留文档、页码、
  类型、等级与标签/项目上下文，可一键「查看原文」回到对应文档页面，证据
  条目可直达证据篮。
- 聚合沿用搜索层既有 effective 关联语义（页面命中 = 页面直接关联或所属
  文档直接关联）；证据无重要性等级，筛选时不伪装等级、按规则不出现。
- 导入文档删除增强：入口上移至阅读页「文档管理」；影响预览新增**知识聚合
  影响**区块（删除将改变哪些项目/标签聚合视图，按实际知识条目判断而非
  仅看关联）；证据篮条目 >0 时必须通过独立高风险确认（明确提示摘录、快照
  与批注将永久删除）方可执行。
- crash-safe 删除恢复：每次删除使用独立操作目录与原子写入的 manifest
  （operation / document / 文件清单 / SHA-256）；进程中断后启动自动对账——
  未提交的删除把文件恢复原位，已提交的完成隔离区清理；manifest 损坏、
  路径非法、内容冲突等不明确状态一律 fail-closed（保留现场 + 系统维护页
  告警），绝不自动删除或覆盖用户文件。

### Changed

- 文档的业务级永久删除统一收口到 `DocumentDeletionService`；移除可被误用的
  裸级联 API `Database.delete_document()` 与 `DocumentService.delete_document()`
  兼容包装。
- 依赖约束：Streamlit 暂时限制为 `<1.61`（`streamlit-image-coordinates`
  0.4.0 上游仍依赖 1.61 移除的内部符号），属依赖兼容限制，非功能回退。
- 数据库保持 schema v6，无迁移；v0.3.1 数据库可直接打开。

### Safety / Integrity

- 删除为 document lifecycle deletion：预览逐项披露页面、四类笔记、证据、
  聚合影响与文件清单；checkbox + 完整标题 +（条件）证据独立确认后才可执行。
- 删除在单事务内完成并做残留校验与 `foreign_key_check`；任一阶段失败整体
  回滚、文件复位，聚合结果保持原样。
- 聚合为实时动态查询：无物化表、无缓存，删除成功后任何聚合视图不会出现
  悬空条目。
- `import_records` 保留审计（`document_id` 置 NULL）；标签、项目、证据篮
  本体永不随单文档删除而消失；标签/项目与搜索历史不被聚合页写入。

### Tests

- 自动化测试 985 项全部通过（含 AppTest 与真实 RapidOCR 离线识别）；
  Ruff 全量通过；release_check 通过。
- 本版不包含 Trash / Restore、批量删除、detached notes、AI 聚合或任何
  账号 / 云功能。

## v0.3.1 — 笔记重要程度与可自定义视觉映射

发布日期：2026-08-10

### Added

- 四类结构化笔记（文档级 / 页面级 / 文字选区 / 图片区域）统一支持三级
  **重要程度**业务语义：重点（`primary`）/ 次重点（`secondary`）/
  一般（`normal`）。
- 创建笔记时可指定等级；编辑笔记时可修改等级；编辑时不改等级则严格保留
  数据库原值（`importance=None` 即 preserve 契约，绝不隐式重置）。
- 阅读页与列表页以**文字徽章**显示等级（重点 / 次重点 / 一般），
  颜色永远不是唯一语义载体。
- 列表页支持单维度等级筛选（与文档、类型筛选和分页正确组合），
  不提供等级排序。
- 列表页「显示设置」：用户可自定义三级徽章背景色并一键恢复默认；
  文字前景色按背景亮度自动深 / 浅切换；配色规范化为小写 `#rrggbb`，
  存于数据库 `note_display_preferences` 单行表，随正式数据库备份。

### Changed（schema v5 → v6）

- `notes` 新增 `importance` 列（CHECK 枚举，常量默认 `'normal'`）；
  新建 `note_display_preferences` 单行表与 `idx_notes_importance` 索引。
- 迁移沿用既有契约：迁移前自动备份、单事务、核心数据指纹自证、
  失败整体回滚；旧笔记升级后等级默认为「一般」。
- 恢复契约不变（同 schema 恢复）：v0.3.1 不直接恢复 schema v5 备份，
  两阶段路径为「v0.3.0 恢复 → v0.3.1 自动迁移」；降级会丢失升级后新建的
  重要程度与配色设置，该事实已写入运行说明。

### Fixed（设计评审驱动的契约修复）

- 更新 API 默认值不得隐式重置已有等级：update 的 `importance` 参数以
  `None` 表示"保留原值"，非法值使整个 update 回滚（全字段不变）。
- 徽章配色可读性：前景文字颜色自动按背景亮度深 / 浅切换，文本标签始终在场。

### Tests

- 自动化测试 899 项全部通过（含全部 AppTest 与真实 RapidOCR 离线识别）；
  Ruff 全量通过；release_check 增加 schema v6 不变量检查。
- 不接入 AI 判级、等级排序、批量改级、跨文档能力或任何账号 / 云功能。

## v0.3.0 — 结构化笔记基础

发布日期：2026-08-10

### Added

- 四类结构化笔记完整 CRUD 闭环：文档级、页面级、文字选区、图片区域；统一 `notes`
  表（schema v5），归属互斥与锚点组合合法性由 CHECK 约束声明式保证。
- 文字选区笔记：单一来源规则（PDF 文本层优先、OCR 初稿兜底）、唯一命中锚定、
  原文快照 / 用户摘录 / 个人笔记三分离、来源哈希变化检测、单事务七字段重新绑定
  （含预览确认流程）。
- 图片区域笔记：streamlit-image-coordinates 0.4.0 拖拽框选（数值输入仅作高级兜底）、
  原始 PNG 宽高与 SHA-256 固化、失效检测、单事务八字段重新框选（含确认流程）。
- 入口严格为二：阅读页右栏「结构化笔记」tab 与独立「结构化笔记」列表页
  （按文档/类型筛选、分页、返回来源）。
- 导入文档删除：影响预览、标题二次确认、隔离区两段式执行、notes 双路径级联、
  逐表残留校验与 `foreign_key_check`，失败整体回滚并恢复文件。
- 迁移 v4→v5：纯增量（一表三索引）、迁移前自动备份、核心数据指纹自证零改动、
  失败整体回滚；无 down-migration，降级路径为 v0.2.4 恢复升级前备份。

### Fixed（发布收口审计修复）

- 文字选区重新绑定的执行严格绑定最后一次确认的预览：预览后修改输入会作废旧预览与
  确认勾选，禁止按陈旧摘要执行；预览失败不再保留上一次成功预览。
- 页面 PNG 校验强制完整像素解码：头部可读但像素截断的图像不再被误判为有效锚点，
  统一进入既有 unreadable 语义。
- 阅读页主图、缩略图与全部区域 overlay 渲染边界增加异常隔离：单张损坏图片只降级
  局部显示并给出中文提示，不再导致整个 Streamlit 页面崩溃。
- AppTest  fixture 补齐 `application_document_deletion_service` stub，并新增全 suite
  护栏：任何测试触碰真实应用运行时（真实 settings / 日志 / checkout 目录）即失败。
- `test_v008_ui` 维护页空备份提示期望值改为从 `settings.app_version` 派生，
  消除旧版本硬编码。

### Tests

- 自动化测试 761 项全部通过（含真实 RapidOCR 离线识别与全部 AppTest）；Ruff 全量通过。
- §18 人工测试门禁 12 项全部 PASS（逐项证据与可复跑脚本见
  `docs/v0.3.0-manual-test-results.md` 与 `runtime/v030-manual-gate/`）。
- 框选组件兼容性实验（12 项门禁）此前已 PASS，结果见
  `experiments/streamlit_image_coordinates_compat/results.md`。
- 不接入 LangChain、LangGraph、真实 AI、账号或云同步。

## v0.2.4 — 发布与本地部署一致性补丁

发布日期：2026-07-30

- 修正 `scripts/release_check.py` 和 `scripts/restore_backup.py` 中遗留的 v0.2.2 硬编码。
- 将当前应用、页面、配置和发布文档版本统一为 v0.2.4。
- 同步正式 `127.0.0.1:8501` 部署与实际存在的本地运维入口。
- 不包含业务功能、数据库 schema 或 AI 变更。

## v0.2.3 — v0.2.x 超长文档底座最终收口

发布日期：2026-07-29

### Fixed

- 重复导入完全零增量：对已成功导入的相同文件再次导入时，不再新增 import record；
  documents、pages、page_search、import_records、raw、PNG 和 Markdown 全部零增量，
  既有成功导入记录不被修改（修复提交 `8df0e85`）。
- 重复探测失败可诊断：SHA 查询或页面列表查询在 import record 创建之前发生异常时，
  在数据库可写的前提下尽力留下一条 failed 导入记录，不掩盖原始异常（回退修正提交
  `b7416f9`）。
- 浏览大量搜索结果时，阅读页页码、查询参数与内部导航状态保持同步（`9754531`）。
- 导入被强制中断后，raw PDF 与页面 PNG 经临时文件原子替换写入，重试可自动恢复
  （`09c49a4`、`2d63fb6`）。

### Tests

- 自动化测试共 553 项全部通过；Ruff 全量检查通过。
- 规模基线：300 页混合页面与 1000～3000 页逐级导入、强杀中断恢复及终态不变量核验
  全部通过（S3 系列结果文档）。
- S4 人工测试矩阵结束：S4-12 APPROVED PASS；S4-13 首次 FAIL（重复导入新增
  completed import record），经定向修复与审计回退修正后，全新隔离现场人工复测
  RETEST APPROVED PASS；S4-14 按止损决策取消，不再执行。
- schema 保持 v4，无迁移；不接入 LangChain、LangGraph 或真实 AI。

## v0.2.2 — 扫描页与缺失文本层的本地 OCR 补充

发布日期：2026-07-26

### Added

- 待整理页面新增“执行本地 OCR”按钮：一次只处理当前页，用户显式点击才执行；已有有效
  OCR 结果的页面不显示执行按钮，不默认重复识别。
- 新增离线 RapidOCR 适配器（RapidOCR + ONNX Runtime），模型随安装包提供，无云端 OCR、
  无 API Key、无运行时模型下载；引擎延迟加载，应用启动与普通浏览不加载模型。
- OCR 结果写入独立 `ocr_text` 字段并以“OCR 初稿（未经人工核验）”只读展示；搜索 FTS
  同步检索 OCR 文本；OCR 证据导出固定显示未经人工核验、以原始页面图像为准的警告。
- OCR 资格判定：仅文本层不足且页面图像可用的页面符合条件；已有可靠文本层的页面不执行。

### Safety

- 原始 PDF、页面 PNG、`extracted_text`、人工 Markdown 与 `review_status` 均不被 OCR
  修改；单页失败隔离；持久化与界面展示的 OCR 错误统一脱敏，不含本机路径或 traceback。
- schema 保持 v4，无迁移；无批量、后台或自动 OCR；引擎不可用时核心功能继续可用。

### Tests

- 新增 RapidOCR 适配器单元测试、真实离线 OCR 识别测试（含断网硬屏蔽）、OCR 搜索与
  证据闭环测试和单页 OCR 界面测试；自动化测试共 456 项全部通过。
- 隔离人工测试 MT-01～MT-10 全部通过：英文与中文扫描页真实识别正确，数据保护与
  重复执行保护有效，正式环境零接触。

## v0.2.1 — 默认证据篮并发首访修复

发布日期：2026-07-25

### Fixed

- 修复多个并发首次访问同时创建默认证据篮时，可能生成重复篮并使部分证据进入“影子篮”的
  问题：在 `get_or_create_basket()` 首次查询前执行 `BEGIN IMMEDIATE`，将“检查是否存在 +
  必要时创建”置于同一短事务中，并发调用最终返回同一个篮 ID。

### Tests

- 新增基于真实线程与真实 SQLite 连接的并发回归测试，覆盖两个线程同时首次访问默认证据篮的
  竞争场景。

### Safety

- schema 保持 v4，无迁移；不修改既有证据篮与证据数据；无 UI 功能变化，无新产品能力。

## v0.2.0 — 超长工程文档与非标准页面处理底座

发布日期：2026-07-24

### Added

- 新增单页 PDF 处理接口 `PdfService.process_page()`，页面重新处理（`reprocess_page`）只渲染
  目标页，不再整份重新渲染，也不修改非目标页的记录与图片。
- 新增确定性页面诊断 `PageDiagnostics`：显示宽高、归一化旋转角、有效字符数，以及空白、
  短文本、横向、旋转四个独立标志；与 needs_review 共用同一份有效字符统计。
- 新增文档级诊断摘要 `DocumentDiagnosticsSummary` 与唯一聚合入口
  `summarize_page_diagnostics()`：总页数、成功/失败/空白/短文本/横向/旋转/待复核计数及
  升序去重页码列表；聚合为纯函数，只做服务层瞬态结果，不持久化。
- 新增 120 页真实长文档自动测试基线与 6 页诊断样例基线，PDF 均由 PyMuPDF 运行时生成，
  不提交二进制。
- 新增页级失败隔离测试：单页渲染或文本提取失败不会中断后续页面处理。

### Changed

- `ImportResult` 新增带安全默认值的 `diagnostics` 字段，既有调用方与返回字段不受影响。

### Safety

- 失败页只计入 failed 统计，不会因默认诊断被误计为空白页；部分失败导入使用既有
  `PARTIALLY_COMPLETED` 状态，不会显示为完全成功。
- 未修改数据库 schema（仍为 v4）、未新增迁移、未改变任何页面状态与导入状态语义；
  PDF 图片 DPI、PNG 命名、覆盖/复用规则和正常导入流程均未改变。
- 完成 300 页混合 PDF（横向、旋转、空白、短文本、受控失败页）实际规模验收：无缺页、
  假成功、跨页覆盖、数据丢失或句柄泄漏。

### Known limitations

- `reprocess_page` 成功后不会自动刷新文档级 `import_status` 和统计；曾经部分失败的文档
  可能继续显示 `partially_completed`（偏保守，不是假成功）。
- 诊断摘要不持久化；重复导入路径返回既有记录，不重新生成摘要。
- 文本提取失败时，该失败页已生成的 PNG 会保留，供单页重处理复用。
- 没有断点续传、后台导入或自动重试；没有诊断 UI、OCR 或 AI 页面识别；500 页以上未纳入
  本版本验收。

## v0.1.2 — 批量操作与整理效率

发布日期：2026-07-21

### Added

- 新增原子化页面批量操作服务，支持批量修改页面状态，以及批量添加和移除标签、项目。
- 搜索页和待整理页新增当前可见页面批量工具栏。
- 新增预检摘要、明确确认和一次性 action token。
- 新增数据库级待整理队列分页与统一分类元数据读取服务。

### Changed

- 搜索页批量范围严格限制为当前分页可见页面，最多 10 页；待整理页严格限制为当前数据库批次，
  最多 20 页，不支持跨分页、跨批次或选择全部匹配结果。
- 待整理队列由全量读取后 UI 切片改为数据库 `count + LIMIT/OFFSET`。
- 页面范围签名绑定来源、文档、筛选、排序、分页或批次及有序页面集合；等价的无序筛选值会先规范化。
- 分类元数据在每轮页面运行中统一读取，不再维护重复页面逻辑。

### Fixed / Hardened

- 修复秒级时间戳碰撞可能导致 stale 漏检，以及 stale 或执行失败后可能残留隐藏选择的问题。
- 增加 execute 异常安全反馈和同轮编辑与预检之间的服务端脏编辑守卫。
- 修正文案并消除 Streamlit checkbox 状态警告。

### Safety

- 所有批量写入保持单事务原子性；protected、missing、stale 或 SQL 异常均不会形成部分提交。
- action token 在执行前消费，不能重复使用；选中页面存在未保存编辑时禁止批量操作。
- 成功、unchanged、protected、missing、stale 与回滚反馈明确区分。

### Known limitations

- 另一个浏览器标签页中的未保存编辑仍不可检测。
- 待整理分页仍使用 `OFFSET`，超大资料库的深分页性能需要后续观察。
- 分类元数据每次 rerun 会执行文档、标签和项目三次明确查询。
- 现有 `components.v1.html` 弃用警告不属于本版本阻断项。
- 本版本不包含证据篮批量操作或新的连续复核导航。

## v0.1.1 — 稳定性与体验补丁

发布日期：2026-07-20

### Scope

- 固定正式服务和发布门禁为 `127.0.0.1:8501`，测试内部仍可显式注入临时端口。
- 修正 Windows 11 诊断标签，并保留原始 release、version 与 build 信息。
- 在 PDF 导入结果中提供用户主动触发的“进入复核”入口。
- 补充同页不同选区可并存、规范化重复选区被拒绝的自动化回归测试。
- 保持 schema v4，不进行数据库迁移；不加入 AI、OCR 模型、Embedding、向量/语义搜索或网络 API。
- 不改变搜索排序、文档级项目/标签的广义 OR 语义或 Markdown 与数据库的跨存储保存策略。

### Implemented

- 正式配置入口拒绝非回环地址和非 8501 端口；启动脚本与正式发布检查提供明确错误，
  自动化测试仍可直接构造隔离配置使用临时端口。
- Windows build `19045` 显示为 Windows 10，build `22000` 及以上显示为 Windows 11；
  无法解析 build 时安全回退，非 Windows 保持原有标签格式。
- 正常导入和部分失败但仍有待复核页时显示“进入复核”；完整重复导入及无待复核页不显示该入口。
- 同页不同证据选区新增服务层与数据库约束回归测试。
- v0.1.1 可继续验证并恢复同属 `0.1` 系列、schema v4 的 v0.1.0 备份，拒绝未来补丁版本。

### Documentation

- v0.1.0 人工测试记录中的发布检查备份仍为
  `release-v0.1.0-20260720-110528-51056810`；不改写该历史记录。
- 最终 tag 前备份另为 `release-v0.1.0-20260720-111033-d6f93038`；v0.1.1 发布前只读预检通过。
- 当前仓库没有 remote；不声称 v0.1.0 tag 已在远程核验或推送。

## v0.1.0 — 第一次完整人工测试与正式发布

发布日期：2026-07-20

### Fixed

- 修复待复核页快捷键显示保存成功但正文实际丢失：编辑器失焦完成状态同步后再触发可见保存动作。
- 修复证据篮“返回原始页”可能进入错误文档：用一次性、经阅读页重新校验的会话参数交接精确来源。
- 修复 Windows junction 绕过备份路径检查并被诊断遍历：备份、恢复和诊断在解析路径前统一拒绝
  符号链接与 Windows 重解析点。
- 修复成功保存后仍残留“未保存”警告，并在导航确认区提供“保存草稿并留在当前页”。
- 修复连续中文查询被“的”“不”等低信息单字误命中，同时保留用户主动执行的单字查询。
- 修复浏览器前进/后退后 URL 与搜索结果不同步：搜索页在本地历史变化后从白名单 URL 重建状态。
- 当数据库仍有非空笔记但对应 Markdown 文件缺失或未记录时，阅读页现在明确警告且不自动覆盖资料。
- 浏览页统计文案由“已处理”改为“导入已处理页”，避免与人工复核进度混淆。

### Validation

- 完成 v0.1.0 第一次完整人工测试：A–J 共 65 项，修复复测后全部 PASS。
- Ruff 通过；Pytest 167/167 通过；真实用户“搜索—判断—收集—导出—来源追溯—退出恢复”闭环
  已跑通。
- 未解决 BLOCKER 为 0，未解决核心 HIGH 为 0。
- 正式数据库保持 schema v4，无数据库迁移；integrity、外键、文档/页面/FTS/证据统计及
  PDF/PNG/Markdown 文件数量通过检查，正式库核心统计未被测试数据污染。

### Known non-blocking observations

- 诊断报告把 Windows 11 显示为 Windows 10。
- 导入完成后没有直接“前往复核”按钮。
- 文档级项目/标签参与广义 OR 查询时结果较多。
- 以上三项仅记录，不阻断 v0.1.0，也不在本轮扩展功能；发布路线从 v0.0.8 直接进入 v0.1.0，
  不存在 v0.0.9。

## v0.0.8 — 完整备份、系统诊断与发布收口

发布日期：2026-07-18

### Added

- 新增完整目录备份：使用 SQLite 在线备份 API 创建一致性快照，保存原 PDF、页面 PNG、页面
  Markdown、全部 schema v4 元数据和白名单恢复配置；`manifest.json` 记录版本、schema、时间、
  数据统计、目录统计、数据库检查、关键相对路径，以及每个文件的大小和 SHA-256。
- 备份先写入同一目标根下的临时目录，完成清单、哈希、数据库、外键、统计和数据库文件引用验证后
  才原子完成；同名已有备份不会覆盖，失败目录会清理或明确标记为不完整。
- 新增停服后的独立恢复脚本 `scripts/restore_backup.py`。恢复会先完整预检，再为现有正式资料创建
  恢复前完整备份；从临时目录重建资料并安全重定位数据库路径，切换后再次验证，失败时回滚原目录。
- 新增“系统维护”页面，显示应用/schema/Python、正式路径、服务地址、文档/页面/FTS/证据统计、
  可用空间和备份状态；可创建备份、只读预检恢复来源、执行完整只读诊断并下载脱敏报告。
- 新增数据库、外键、数量、PDF/PNG/Markdown、孤立文件、权限、磁盘、配置路径、正式监听、隔离
  端口和最近备份检查。结果统一分为正常、警告和错误，诊断本身不自动修复或删除资料。
- 新增默认脱敏 Markdown 诊断报告：不包含 PDF、Markdown、笔记、证据正文、环境变量值、密钥、
  代理凭据和完整用户目录；最近导入失败只输出计数和时间，日志只输出时间、级别和记录器。
- 新增统一发布检查 `scripts/release_check.py`，覆盖版本、Ruff、Pytest 收集与执行、正式数据库、
  FTS、数据污染、服务健康和监听、隔离端口、Git、`AGENTS.md` 暂存保护、关键目录及发布前备份。
- 首页、导入、浏览、检索、待复核、项目、标签、证据篮和系统维护页新增可操作的首次使用与空状态。

### Changed

- 应用版本更新为 v0.0.8；升级路线从 v0.0.7 直接进入 v0.0.8，schema 继续保持 v4，无数据库
  迁移、字段变更、索引重建或现有正式资料重写。
- 完整备份默认使用项目根目录下的 `backups/`，与正式 `data/`、历史迁移数据库备份、日志和运行
  状态分离，避免递归纳入和 Git 污染。
- 正式恢复不在 Streamlit 数据库仍被占用时追求一键覆盖；页面只做预检并给出明确停服命令，独立
  脚本负责恢复、回滚和恢复后检查。隔离恢复可指定独立 data 目录而不触碰正式资料。

### Security

- manifest 和恢复配置使用严格类型与字段白名单；备份相对路径拒绝绝对路径、反斜杠、盘符、空字节
  和 `..`，复制目标再次规范化并检查目录边界。
- 备份和恢复不跟随符号链接；SQLite 路径更新继续参数化；备份数据库内的绝对路径只用于验证原始
  布局，不会直接决定恢复写入位置。
- 恢复前未完成验证、服务仍运行、格式/应用/schema 不兼容、数据库损坏、文件缺失或哈希不一致时
  一律拒绝恢复，不提供强制绕过；切换后失败保留原资料和恢复前备份。
- 正式服务继续只绑定 `127.0.0.1`；核心备份、恢复、诊断、报告和发布检查均不需要网络、API Key、
  云账号或付费服务。

### Tests

- 新增空库/正常/中断/排除项/manifest/数量/哈希/损坏/不完整备份测试，以及正常恢复、不兼容、
  恢复前备份、路径重定位、服务占用、恢复后统计和失败回滚测试。
- 新增正常诊断、外键异常、PDF/PNG/Markdown 缺失、孤立文件、目录权限、磁盘阈值、监听、残留端口、
  损坏备份、路径脱敏、正文/环境变量/日志密钥不泄漏测试。
- 新增发布检查 PASS/WARNING/FAIL、版本不一致、错误监听、误暂存 `AGENTS.md`、未忽略验收产物测试，
  以及九个主要页面的隔离空状态和系统维护交互测试。

### Known limitations and release path

- 完整备份是未压缩、未加密的本地目录格式；不提供云备份、云同步或备份介质访问控制。
- 正式恢复必须先停止服务并运行独立脚本；这是为了避免 Windows 上替换仍被占用的 SQLite 数据库。
- 目录读写诊断使用操作系统权限判断，不写探测文件；统一发布检查会创建并立即删除零字节探测文件。
- v0.0.8 发布后进行第一次完整人工测试，只修复人工测试发现的问题。下一正式版本为 v0.1.0。

## v0.0.7 — 搜索命中理解与连续阅读优化

发布日期：2026-07-18

### Added

- 搜索结果新增“按页面显示”和“按文档分组显示”；默认保留原页面卡片习惯。文档组显示来源、
  完整命中页数、最相关页、主要命中字段、项目和标签，并可展开当前文档的已加载命中页。
- 阅读页新增全文结果上一项/下一项、当前全局位置、完整结果数和加载范围提示；新增本文件上一/下一
  命中页、按页码排列的命中页摘要列表及大量命中时的每组 10 页控件。
- 搜索卡片新增 1～3 个来源标注的去重命中片段、非重叠字面出现次数，以及按需加载单页图像和
  长文本的快速预览；预览内可继续添加证据、编辑备注、移除证据和生成单页证据包。
- 普通相邻页导航新增当前文档位置、相邻页是否命中当前搜索及是否已加入证据篮的文字反馈。
- 阅读页顶部新增经过类型解析的关键词、筛选和排序摘要；条件较多时只显示前四项并提供折叠详情。

### Changed

- 搜索 URL 状态新增白名单 `view`、`expanded_document`、`preview_page` 和 `focus_result`；只保存
  一个展开组和一个预览页，不写入完整结果 ID 列表。旧 URL 未携带这些参数时继续使用页面视图。
- 文档组按首个页面在当前全局结果中的位置排序，组内保持当前全局排序；本文件命中导航按页码排序，
  按钮文案明确区分全文结果、本文件命中和普通相邻页。
- 阅读页以 `search_return` 中的同一关键词、筛选、排序和加载上限重建导航范围；超过上限时同时显示
  已加载范围和完整页面总数，不再把搜索页当前 10 条分页当成导航全集。

### Performance

- 文档分组直接复用已加载的主搜索结果；完整文档命中数由一次参数化聚合查询生成，不为每个文档
  重复搜索。
- 多片段在每个已加载结果上对已有字段值做一次有界扫描，最多保留三个片段；快速预览只在用户打开
  单页后读取该页图像，不预加载全部结果大图。
- 搜索主查询与批量标签/项目元数据查询仍保持固定两条；阅读页证据状态仍一次读取，未重新引入 2N。

### Fixed

- 修复真实 Streamlit `switch_page` 会清空切换前新查询参数的问题：检索页和阅读页现在通过当前本地会话传递一次性交接参数，目标页面仍按 URL 白名单重新解析并规范化。
- 当前页面不再因不属于重建结果集、结果记录删除、旧 URL 或非法视图/展开/预览参数而崩溃；对应
  命中导航会安全停用并保留返回入口。
- 多来源摘要会去除重复或互相包含的窗口；短文本、无上下文、缺图和缺 Markdown 均显示稳定回退。
- 文档分组对重复页面 ID 去重，并对标题、文件名或异常文档元数据缺失使用安全占位文本。

### Security

- 查询值继续全部参数化；排序、视图、字段和导航状态继续使用枚举或正整数白名单及长度限制。
- 所有命中片段先整体 HTML 转义，再只插入固定 `<mark>`；标题、文件名、正文、OCR、Markdown、
  标签、项目和 URL 原始文本都不能注入额外 HTML。
- schema 继续为 v4，无迁移、无结构写入、无无意义备份；核心功能继续离线并只监听 `127.0.0.1`。

### Known limitations

- 每次最多加载 100 个有序结果，因此连续导航最多覆盖当前加载范围；界面会明确显示范围，更多结果
  需提高上限或继续筛选。
- Streamlit 没有可靠的像素级滚动恢复接口。本版本恢复视图、展开组、预览页和结果焦点文字标识，
  不注入脆弱 JavaScript 模拟滚动。
- “最近查看”排序会随阅读动作写入 `last_viewed_at` 而动态变化；阅读页按每次重建时的最新本地数据
  解释顺序，不持久化旧结果 ID 快照。

## v0.0.6 — 更快的搜索筛选与完整状态恢复

发布日期：2026-07-18

### 查找流程

- 搜索筛选默认收起并按高频条件、搜索范围、排序和低频条件重新分层；当前生效条件集中显示，
  支持单项移除、仅清除筛选并保留关键词、清除全部条件及撤销最近一次筛选。
- 文档、项目和标签新增 Unicode 本地快速查找；中文、全角文字、括号、方括号、加号、百分号
  等均作为普通文字处理，无匹配时显示明确提示，清空后恢复全部候选。
- 新增仅看已复核、仅看待复核、仅看有笔记、仅看当前证据篮、最近查看和最近修改快捷入口；
  所有入口复用普通 `SearchFilters` 与排序枚举。
- 无结果反馈区分关键词未命中、筛选组合过严和搜索范围过窄，并提供撤销或清除入口。

### 状态、分面与性能

- 新增白名单 URL 搜索状态，完整保留关键词、文档/项目/标签/状态/范围、有笔记/证据篮条件、
  排序、加载上限、分页和面板开合；刷新、浏览器历史、阅读往返和证据操作均可恢复。
- 文档和状态采用忽略自身 OR 维度的候选计数；项目和标签在当前 AND 条件上计算新增候选后的
  精确剩余页数，避免有效选项因自身维度被错误显示为 0。所有分面仍由单条聚合 SQL 完成。
- 搜索结果的有效标签和项目由逐结果 2N 次读取改为一次批量元数据查询；搜索主查询加元数据
  查询保持固定两条，不随结果数量增长。

### 兼容性与安全

- schema 保持 v4，不新增字段、索引或迁移；v0.0.5 数据库、原 PDF、页面图像、Markdown、
  标签、项目、复核状态和证据篮原样保留。
- URL 只接受白名单枚举、正整数和长度限制后的关键词；非法、缺失及旧版参数安全降级。所有
  SQL 值继续参数化，排序继续使用白名单，摘要和高亮继续先转义后插入固定 `mark` 标签。
- 不新增第三方依赖、账号、登录、云同步、API Key、外部网络或云服务；默认继续只绑定
  `127.0.0.1`。

## v0.0.5 — 多页面证据收集与引用工作流增强

发布日期：2026-07-18

### 证据篮与选区可信度

- 新增 SQLite 持久化默认证据篮；搜索页和阅读页均可编辑具体选区后加入，服务重启后恢复。
- 支持相同页面的不同选区、防止相同选区重复加入、稳定排序、上下移动、删除单条、确认清空、
  编辑用户备注及返回原始页面。
- 选区在 PDF 文本层或 OCR 原文中匹配时标为原始材料；无法匹配时明确标为未经原文匹配确认
  的用户摘录。每条证据保留来源定位、原始文本哈希、上下文来源及加入时元数据。
- 导出前重新检查文档、页面、所属关系、页码、PDF、页面图像和原始文本哈希；缺失或变化时
  安全停止，不生成错误来源。

### 多页面证据包

- 新增按证据篮顺序生成的多文档、多页面 Markdown 证据包，包含生成时间、证据数、文档数、
  编号、文档/文件/页码、项目、标签、复核状态、绝对本地路径与稳定内部定位。
- 原始材料、用户摘录、用户笔记、系统生成上下文和用户提供上下文分别展示；非 `reviewed`
  证据逐条警告，两条及以上未复核证据触发包顶部整体警告。
- 用户内容通过 Markdown 缩进代码块隔离，标题和元数据安全转义；HTML、Markdown 和 SQL 特殊
  字符不会成为可执行 HTML、破坏证据包结构或拼接进 SQL。

### 搜索质量与界面

- 项目、标签和五种复核状态显示与当前关键词及完整筛选条件一致的计数，并显示组合条件总数；
  所有 facet 由一条聚合 SQL 计算，计数包含自身维度当前已选条件。
- 在 FTS5 BM25 上增加可解释的标题、原文件名、标签、项目、笔记、OCR 和正文权重；完整连续
  短语和多字段同时命中获得提升，原有 OR 召回、特殊字符安全和其他排序模式保持不变。
- 新增独立“证据篮”页面与首页入口；搜索卡片和阅读页显示已加入数量及证据篮入口。

### 数据库与兼容性

- schema 升级到 v4，新增 `evidence_baskets` 与 `evidence_items`，使用外键级联、重复选区约束、
  唯一排序位置和 4000 字符备注限制；结构预留未来多个证据篮。
- schema v3 升级前自动创建 SQLite 一致性备份；迁移不重建页面或 FTS，事务内核对文档、页面、
  FTS、复核状态及源文件路径指纹，并执行数据库完整性和外键检查。
- 继续只监听 `127.0.0.1`，不新增依赖；核心搜索、证据篮、导出和迁移均不需要网络、AI Key、
  云账户、向量数据库或外部模型。

## v0.0.4 — 可追溯页面检索与引用证据包

发布日期：2026-07-18

### 新增

- 搜索结果卡片集中展示文档标题、原文件名、页码、页面复核状态、项目、标签、
  命中摘要、全部命中字段、页面直达入口和引用证据包入口。
- 新增文档、项目、标签、页面复核状态和命中字段组合筛选；多个标签和多个项目采用
  AND 逻辑，并在界面中明确标注。
- 新增相关度、文档与页码、最近查看和最近修改排序；搜索结果按每组 10 条分页展示。
- 搜索直达携带精确文档、页码和查询提示，阅读页可返回并保留搜索关键词、筛选、排序与结果分组。
- 新增独立 Markdown 证据包生成器，包含绝对本地源路径、页面图像路径、稳定内部引用和生成时间；
  原始材料与用户笔记分区展示，未人工复核完成的状态附带明确警示。

### 检索质量与安全

- 保留 schema v3 与现有 jieba + FTS5 索引，增加原始字段字面量匹配以准确识别命中来源，
  不需要重建或修改正式数据。
- 中文连续文本与多个关键词均可检索；引号、括号、加减号、斜杠、FTS 操作符和超长输入
  经过规范化与长度限制，不直接拼接到 SQL。
- 搜索摘要截取关键词附近上下文；无命中位置时稳定回退到可用内容开头。
- 高亮逻辑在独立工具模块中实现，先转义完整页面内容，再插入固定 `mark` 标签，
  防止 HTML 或脚本内容作为可执行标记进入页面。
- 搜索记录缺失、页码不一致、原始 PDF 或页面图像缺失时停止跳转并显示中文说明，
  不自动跳到相邻页面。

### 测试与兼容性

- 新增搜索输入、中文连续文本、命中字段、组合筛选、标签 AND、排序白名单、SQL 注入防护、
  HTML 转义、证据包五种复核状态、中文与空格路径、精确导航、返回状态和 Streamlit 卡片工作流测试。
- v0.0.3 连续复核、保存并下一页、未保存提醒、跳过队列、失败内容保护、中文 FTS5 和
  后台服务管理继续保留。
- 继续只监听 `127.0.0.1`；核心功能不需要 API Key、有效外网代理、云账户或远程服务。

### 未包含

- 复杂 OCR、手写识别、公式 OCR。
- 向量搜索、语义搜索或 Embedding。
- 大模型自动问答。
- 联网资料搜索。

## v0.0.3 — 连续待复核页面整理

发布日期：2026-07-18

### 新增

- 统一人工复核状态：待处理、草稿待复核、人工复核完成、暂不整理和处理失败。
- 新增 `review_status`、`note_updated_at`、`reviewed_at`、`last_viewed_at`。
- 待复核页新增保存草稿、保存并复核、保存复核后进入下一页、暂时跳过、
  上一待处理页和下一待处理页。
- 顶部显示文档名称、当前页码、完整中文状态、已处理数、总页数和剩余待处理数。
- 首页、浏览资料页和待复核页新增“继续处理下一待复核页”入口。
- 新增可选快捷键：`Ctrl+S`、`Ctrl+Enter`、`Alt+Left` 和 `Alt+Right`；
  方向快捷键在文本输入时不触发。
- 记录页面最后查看时间；复核完成时间与笔记更新时间分别保存。

### 可靠性

- 未保存内容切换页面前需要确认；保存失败不清空编辑框。
- 相同内容与状态的连续保存为幂等操作，不重复改写 Markdown 文件或数据库时间戳。
- 没有下一待处理页时显示明确提示。
- 保存或跳过后重新读取实时队列与统计，首页不使用旧缓存。
- 新增 schema v3 迁移失败事务回滚测试、真实规模 1 文档/78 页迁移测试、
  持久化重启测试、连续复核测试、跳过队列测试和 Streamlit 交互测试。

### 兼容性与迁移

- v0.0.2 数据库升级前使用 SQLite 在线备份 API 自动创建 `knowledge.v2.<时间戳>.db`。
- 旧处理状态列保留，规范化人工状态写入 `review_status`，旧页面、FTS5、标签、项目和路径均保留。
- 旧待复核页迁移为 `pending`；旧非空 Markdown 保守迁移为 `draft`，不自动标记为
  `reviewed`；明确的无笔记人工确认页迁移为 `reviewed`；失败页迁移为 `failed`。
- schema v3 在事务中执行；失败时恢复 v2 schema，回滚方式为停服后显式恢复迁移前备份。

### 安全边界

- 继续只绑定 `127.0.0.1`，核心功能可在无网络、无 API Key 环境运行。
- 不新增账号、登录、权限、多用户、云同步、外部网络依赖、向量数据库或大模型依赖。

## v0.0.2 — 页面级知识管理与后台运行

发布日期：2026-07-12

### 新增

- 页面级 Markdown 笔记、保存状态、清空与持久化。
- 清晰可滚动/缩放的双栏页面阅读、编辑和预览。
- 文档与页面标签系统，同名标签规范化复用。
- 本地项目系统及文档/页面关联。
- 文档排序、标签/项目/导入状态筛选和页面缩略图。
- 搜索范围扩展到文档标题、文件名、OCR 字段、Markdown、标签和项目。
- 搜索结果匹配类型、标签、项目与页面直达。
- PDF 重复导入检测、持久化导入记录、进度和结果统计。
- 单页失败隔离、失败原因、待复核筛选、人工整理标记和本地重试。
- schema v2 正式迁移、迁移前自动一致性备份和失败回滚。
- 一键启动、停止、状态查看与静默后台入口。
- PID 身份校验、重复启动检测、回环健康检查和端口冲突识别。
- 应用、服务管理和启动控制台本地轮转日志。
- Windows 当前用户登录后自动启动；任务计划程序不可用时回退启动文件夹。

### 兼容性

- 保留 v0.0.1 的文档、页面、图片、提取文本、Markdown 路径和 FTS 数据。
- 沿用 `data/raw/` 原 PDF 路径，避免破坏已有数据库引用。
- 旧 `ready` 状态迁移为 `text_extracted`，旧 `pending` 状态迁移为 `pending_review`；已有非空 Markdown 页面迁移为 `manually_reviewed`。

### 安全边界

- 继续只绑定 `127.0.0.1`。
- 不新增账号、登录、权限、云同步、付费 API、云 OCR、向量数据库或联网必需功能。
- 删除标签和项目不删除资料；删除文档需要明确二次确认。
